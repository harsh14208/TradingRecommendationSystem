fastapi==0.136.3
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
aiosqlite==0.20.0
asyncpg>=0.29.0             # PostgreSQL async driver — used when DATABASE_URL=postgresql://
openpyxl>=3.1.0             # Signal history Excel export
redis>=5.0.0                # Optional: Event-Driven worker bus backend (activate via REDIS_URL env var)
greenlet>=3.0.0
certifi>=2024.0.0
yfinance>=1.0.0
curl_cffi>=0.7.0
finnhub-python==2.4.20
ta==0.11.0
pandas==2.2.3
numpy==1.26.4
twilio==9.3.4
python-dotenv==1.2.2
pydantic-settings==2.5.2
aiohttp==3.14.0
massive>=2.7.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
stripe>=7.0.0
python-multipart>=0.0.9
aiosmtplib>=3.0.0
slowapi>=0.1.9              # rate limiting for FastAPI (Limiter, _rate_limit_exceeded_handler)
playwright>=1.44.0          # optional: Finviz JS fallback; run: playwright install chromium
pywebpush>=2.0.0            # web push notifications (VAPID)

# ── ML ──
xgboost>=2.0.0
scikit-learn>=1.4.0
scipy>=1.11.0,<1.16   # exact t-distribution p-values in calc_tbd_metrics; <1.16 keeps numpy-1.26 compat (1.16+ needs numpy 2.x → ModuleNotFoundError: numpy.char)
shap>=0.45.0        # SHAP feature audit (eval_ml.py §7; offline analysis only)
arch>=7.0,<9.0      # GARCH(1,1) conditional-vol forecast for triple-barrier widths (train_metalabel_model.py --barriers garch); numpy-1.26 safe
statsmodels>=0.14   # Engle-Granger step-2 ADF cointegration test (§63, services/technicals.py); numpy-1.26 safe
hmmlearn>=0.3       # 2-state Gaussian HMM for macro regime (services/macro_regime.py); numpy-1.26 safe

# ── Migrations ──
alembic>=1.13.0
psycopg2-binary>=2.9.0   # sync driver for Alembic (asyncpg used at runtime)

# ── Dev / CI ──
pip-audit>=2.7.0
email-validator
pytest-timeout>=2.3.1
pytest-asyncio>=0.23.0    # async test support
httpx>=0.27.0              # starlette.testclient dependency
anyio>=4.0.0               # async runtime for tests
ruff>=0.9.0
pre-commit>=4.0.0
