"""
SEC EDGAR Form 4 insider-trading scraper.
Uses EDGAR's free public JSON API — no API key required.
Required by EDGAR ToS: always send a descriptive User-Agent.
"""

import asyncio
import logging
import ssl
import time
import xml.etree.ElementTree as ET
from datetime import datetime, timedelta
from typing import Optional

from services.redis_cache import cache_get, cache_set

log = logging.getLogger("signal.trade.edgar")

import aiohttp
import certifi
from services.http_client import shared_session

HEADERS = {"User-Agent": "SignalTrade research@signaltrade.com", "Accept-Encoding": "gzip"}
_ssl_ctx = ssl.create_default_context(cafile=certifi.where())

# Pre-seeded CIKs for the default watchlist — avoids the 3 MB tickers.json download
# on every cold-start.  Unknown tickers fall back to the live lookup.
_KNOWN_CIKS: dict[str, str] = {
    # Top 10 by market cap
    "AAPL": "0000320193",
    "MSFT": "0000789019",
    "NVDA": "0001045810",
    "AMZN": "0001018724",
    "GOOGL": "0001652044",
    "GOOG": "0001652044",
    "META": "0001326801",
    "TSLA": "0001318605",
    "AVGO": "0001730168",
    "BRK-B": "0001067983",
    "JPM": "0000019617",
    # Next tier
    "V": "0001403161",
    "LLY": "0000059478",
    "WMT": "0000104169",
    "XOM": "0000034088",
    "UNH": "0000731766",
    "MA": "0001141391",
    "JNJ": "0000200406",
    "COST": "0000909832",
    "ORCL": "0001341439",
    # Tech / growth
    "AMD": "0000002488",
    "NFLX": "0001065280",
    "PLTR": "0001321655",
    "SMCI": "0000866374",
    "CRM": "0001108524",
    "SNOW": "0001640147",
}

_cik_map: dict[str, str] = dict(_KNOWN_CIKS)
_cik_map_ts: float = 0.0
_activity_cache: dict[str, tuple[dict, float]] = {}
CACHE_TTL = 3600  # 1 hour


_cik_map_lock = asyncio.Lock()


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__ensure_cik_map__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__ensure_cik_map__mutmut)
async def _ensure_cik_map() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_orig() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_1() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts >= 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_2() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 1:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_3() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts >= 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_4() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 1:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_5() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = None
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_6() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=None)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_7() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=None) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_8() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    None,
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_9() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=None,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_10() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=None,
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_11() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_12() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_13() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_14() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "XXhttps://www.sec.gov/files/company_tickers.jsonXX",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_15() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "HTTPS://WWW.SEC.GOV/FILES/COMPANY_TICKERS.JSON",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_16() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=None),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_17() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=21),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_18() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status != 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_19() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 201:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_20() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = None
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_21() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = None
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_22() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["XXtickerXX"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_23() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["TICKER"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_24() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(None)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_25() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(None).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_26() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["XXcik_strXX"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_27() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["CIK_STR"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_28() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(11)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_29() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = None
        except Exception as e:
            log.warning(f"[edgar] tickers.json fetch failed: {e}")


async def x__ensure_cik_map__mutmut_30() -> None:
    """Lazy-load the full ticker→CIK map from EDGAR (one 3 MB download, cached forever)."""
    global _cik_map_ts
    if _cik_map_ts > 0:
        return
    async with _cik_map_lock:
        if _cik_map_ts > 0:
            return
        try:
            connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
            async with aiohttp.ClientSession(connector=connector) as s:
                async with s.get(
                    "https://www.sec.gov/files/company_tickers.json",
                    headers=HEADERS,
                    timeout=aiohttp.ClientTimeout(total=20),
                ) as r:
                    if r.status == 200:
                        data = await r.json(content_type=None)
                        for entry in data.values():
                            _cik_map[entry["ticker"]] = str(entry["cik_str"]).zfill(10)
                        _cik_map_ts = time.time()
        except Exception as e:
            log.warning(None)

mutants_x__ensure_cik_map__mutmut['_mutmut_orig'] = x__ensure_cik_map__mutmut_orig # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_1'] = x__ensure_cik_map__mutmut_1 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_2'] = x__ensure_cik_map__mutmut_2 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_3'] = x__ensure_cik_map__mutmut_3 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_4'] = x__ensure_cik_map__mutmut_4 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_5'] = x__ensure_cik_map__mutmut_5 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_6'] = x__ensure_cik_map__mutmut_6 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_7'] = x__ensure_cik_map__mutmut_7 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_8'] = x__ensure_cik_map__mutmut_8 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_9'] = x__ensure_cik_map__mutmut_9 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_10'] = x__ensure_cik_map__mutmut_10 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_11'] = x__ensure_cik_map__mutmut_11 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_12'] = x__ensure_cik_map__mutmut_12 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_13'] = x__ensure_cik_map__mutmut_13 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_14'] = x__ensure_cik_map__mutmut_14 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_15'] = x__ensure_cik_map__mutmut_15 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_16'] = x__ensure_cik_map__mutmut_16 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_17'] = x__ensure_cik_map__mutmut_17 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_18'] = x__ensure_cik_map__mutmut_18 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_19'] = x__ensure_cik_map__mutmut_19 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_20'] = x__ensure_cik_map__mutmut_20 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_21'] = x__ensure_cik_map__mutmut_21 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_22'] = x__ensure_cik_map__mutmut_22 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_23'] = x__ensure_cik_map__mutmut_23 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_24'] = x__ensure_cik_map__mutmut_24 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_25'] = x__ensure_cik_map__mutmut_25 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_26'] = x__ensure_cik_map__mutmut_26 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_27'] = x__ensure_cik_map__mutmut_27 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_28'] = x__ensure_cik_map__mutmut_28 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_29'] = x__ensure_cik_map__mutmut_29 # type: ignore # mutmut generated
mutants_x__ensure_cik_map__mutmut['x__ensure_cik_map__mutmut_30'] = x__ensure_cik_map__mutmut_30 # type: ignore # mutmut generated
mutants_x__get_cik__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__get_cik__mutmut)
async def _get_cik(ticker: str) -> Optional[str]:
    if ticker in _cik_map:
        return _cik_map[ticker]
    await _ensure_cik_map()
    return _cik_map.get(ticker)


async def x__get_cik__mutmut_orig(ticker: str) -> Optional[str]:
    if ticker in _cik_map:
        return _cik_map[ticker]
    await _ensure_cik_map()
    return _cik_map.get(ticker)


async def x__get_cik__mutmut_1(ticker: str) -> Optional[str]:
    if ticker not in _cik_map:
        return _cik_map[ticker]
    await _ensure_cik_map()
    return _cik_map.get(ticker)


async def x__get_cik__mutmut_2(ticker: str) -> Optional[str]:
    if ticker in _cik_map:
        return _cik_map[ticker]
    await _ensure_cik_map()
    return _cik_map.get(None)

mutants_x__get_cik__mutmut['_mutmut_orig'] = x__get_cik__mutmut_orig # type: ignore # mutmut generated
mutants_x__get_cik__mutmut['x__get_cik__mutmut_1'] = x__get_cik__mutmut_1 # type: ignore # mutmut generated
mutants_x__get_cik__mutmut['x__get_cik__mutmut_2'] = x__get_cik__mutmut_2 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__parse_form4__mutmut)
def _parse_form4(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_orig(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_1(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = None
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_2(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 1
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_3(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = None
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_4(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 1.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_5(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = None
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_6(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(None)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_7(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "XXnonDerivativeTransactionXX" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_8(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonderivativetransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_9(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "NONDERIVATIVETRANSACTION" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_10(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_11(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                break
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_12(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = ""
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_13(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = None  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_14(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split(None)[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_15(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("XX}XX")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_16(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[+1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_17(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-2]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_18(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag != "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_19(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "XXtransactionAcquiredDisposedCodeXX":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_20(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionacquireddisposedcode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_21(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "TRANSACTIONACQUIREDDISPOSEDCODE":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_22(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split(None)[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_23(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("XX}XX")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_24(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[+1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_25(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-2] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_26(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] != "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_27(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "XXvalueXX":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_28(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "VALUE":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_29(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = None
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_30(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().lower()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_31(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text and "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_32(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "XXXX").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_33(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag != "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_34(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "XXtransactionSharesXX":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_35(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionshares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_36(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "TRANSACTIONSHARES":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_37(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split(None)[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_38(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("XX}XX")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_39(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[+1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_40(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-2] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_41(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] != "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_42(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "XXvalueXX":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_43(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "VALUE":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_44(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = None
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_45(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(None)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_46(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag != "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_47(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "XXtransactionPricePerShareXX":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_48(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionpricepershare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_49(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "TRANSACTIONPRICEPERSHARE":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_50(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split(None)[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_51(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("XX}XX")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_52(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[+1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_53(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-2] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_54(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] != "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_55(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "XXvalueXX":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_56(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "VALUE":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_57(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = None
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_58(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(None)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_59(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" or shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_60(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code != "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_61(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "XXAXX" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_62(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "a" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_63(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys = int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_64(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys -= int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_65(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(None)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_66(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val = shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_67(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val -= shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_68(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares / (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_69(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price and 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_70(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 1)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_71(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" or shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_72(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code != "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_73(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "XXDXX" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_74(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "d" and shares:
                sells += int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_75(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells = int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_76(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells -= int(shares)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_77(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(None)
                sell_val += shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_78(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val = shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_79(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val -= shares * (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_80(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares / (price or 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_81(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price and 0)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val


def x__parse_form4__mutmut_82(xml_text: str) -> tuple[int, int, float, float]:
    """Return (buy_shares, sell_shares, buy_value, sell_value)."""
    buys = sells = 0
    buy_val = sell_val = 0.0
    try:
        root = ET.fromstring(xml_text)
        for trans in root.iter():
            if "nonDerivativeTransaction" not in trans.tag:
                continue
            code = shares = price = None
            for child in trans.iter():
                tag = child.tag.split("}")[-1]  # strip namespace
                if tag == "transactionAcquiredDisposedCode":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            code = (v.text or "").strip().upper()
                elif tag == "transactionShares":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                shares = float(v.text)
                            except (TypeError, ValueError):
                                pass
                elif tag == "transactionPricePerShare":
                    for v in child:
                        if v.tag.split("}")[-1] == "value":
                            try:
                                price = float(v.text)
                            except (TypeError, ValueError):
                                pass
            if code == "A" and shares:
                buys += int(shares)
                buy_val += shares * (price or 0)
            elif code == "D" and shares:
                sells += int(shares)
                sell_val += shares * (price or 1)
    except ET.ParseError:
        pass
    return buys, sells, buy_val, sell_val

mutants_x__parse_form4__mutmut['_mutmut_orig'] = x__parse_form4__mutmut_orig # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_1'] = x__parse_form4__mutmut_1 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_2'] = x__parse_form4__mutmut_2 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_3'] = x__parse_form4__mutmut_3 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_4'] = x__parse_form4__mutmut_4 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_5'] = x__parse_form4__mutmut_5 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_6'] = x__parse_form4__mutmut_6 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_7'] = x__parse_form4__mutmut_7 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_8'] = x__parse_form4__mutmut_8 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_9'] = x__parse_form4__mutmut_9 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_10'] = x__parse_form4__mutmut_10 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_11'] = x__parse_form4__mutmut_11 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_12'] = x__parse_form4__mutmut_12 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_13'] = x__parse_form4__mutmut_13 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_14'] = x__parse_form4__mutmut_14 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_15'] = x__parse_form4__mutmut_15 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_16'] = x__parse_form4__mutmut_16 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_17'] = x__parse_form4__mutmut_17 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_18'] = x__parse_form4__mutmut_18 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_19'] = x__parse_form4__mutmut_19 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_20'] = x__parse_form4__mutmut_20 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_21'] = x__parse_form4__mutmut_21 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_22'] = x__parse_form4__mutmut_22 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_23'] = x__parse_form4__mutmut_23 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_24'] = x__parse_form4__mutmut_24 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_25'] = x__parse_form4__mutmut_25 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_26'] = x__parse_form4__mutmut_26 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_27'] = x__parse_form4__mutmut_27 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_28'] = x__parse_form4__mutmut_28 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_29'] = x__parse_form4__mutmut_29 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_30'] = x__parse_form4__mutmut_30 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_31'] = x__parse_form4__mutmut_31 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_32'] = x__parse_form4__mutmut_32 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_33'] = x__parse_form4__mutmut_33 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_34'] = x__parse_form4__mutmut_34 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_35'] = x__parse_form4__mutmut_35 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_36'] = x__parse_form4__mutmut_36 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_37'] = x__parse_form4__mutmut_37 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_38'] = x__parse_form4__mutmut_38 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_39'] = x__parse_form4__mutmut_39 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_40'] = x__parse_form4__mutmut_40 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_41'] = x__parse_form4__mutmut_41 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_42'] = x__parse_form4__mutmut_42 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_43'] = x__parse_form4__mutmut_43 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_44'] = x__parse_form4__mutmut_44 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_45'] = x__parse_form4__mutmut_45 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_46'] = x__parse_form4__mutmut_46 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_47'] = x__parse_form4__mutmut_47 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_48'] = x__parse_form4__mutmut_48 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_49'] = x__parse_form4__mutmut_49 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_50'] = x__parse_form4__mutmut_50 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_51'] = x__parse_form4__mutmut_51 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_52'] = x__parse_form4__mutmut_52 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_53'] = x__parse_form4__mutmut_53 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_54'] = x__parse_form4__mutmut_54 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_55'] = x__parse_form4__mutmut_55 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_56'] = x__parse_form4__mutmut_56 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_57'] = x__parse_form4__mutmut_57 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_58'] = x__parse_form4__mutmut_58 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_59'] = x__parse_form4__mutmut_59 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_60'] = x__parse_form4__mutmut_60 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_61'] = x__parse_form4__mutmut_61 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_62'] = x__parse_form4__mutmut_62 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_63'] = x__parse_form4__mutmut_63 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_64'] = x__parse_form4__mutmut_64 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_65'] = x__parse_form4__mutmut_65 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_66'] = x__parse_form4__mutmut_66 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_67'] = x__parse_form4__mutmut_67 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_68'] = x__parse_form4__mutmut_68 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_69'] = x__parse_form4__mutmut_69 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_70'] = x__parse_form4__mutmut_70 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_71'] = x__parse_form4__mutmut_71 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_72'] = x__parse_form4__mutmut_72 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_73'] = x__parse_form4__mutmut_73 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_74'] = x__parse_form4__mutmut_74 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_75'] = x__parse_form4__mutmut_75 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_76'] = x__parse_form4__mutmut_76 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_77'] = x__parse_form4__mutmut_77 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_78'] = x__parse_form4__mutmut_78 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_79'] = x__parse_form4__mutmut_79 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_80'] = x__parse_form4__mutmut_80 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_81'] = x__parse_form4__mutmut_81 # type: ignore # mutmut generated
mutants_x__parse_form4__mutmut['x__parse_form4__mutmut_82'] = x__parse_form4__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_insider_activity__mutmut)
async def get_insider_activity(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_orig(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_1(ticker: str, days: int = 31) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_2(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = None
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_3(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.lower()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_4(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = None
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_5(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = None
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_6(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(None)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_7(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_8(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = None
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_9(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(None)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_10(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached or time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_11(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() + cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_12(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[2] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_13(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] <= CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_14(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[1]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_15(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = None
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_16(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(None)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_17(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_18(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = None

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_19(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "XXbuysXX": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_20(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "BUYS": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_21(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 1,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_22(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "XXsellsXX": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_23(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "SELLS": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_24(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 1,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_25(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "XXbuy_valueXX": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_26(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "BUY_VALUE": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_27(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 1,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_28(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "XXsell_valueXX": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_29(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "SELL_VALUE": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_30(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 1,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_31(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "XXnet_sharesXX": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_32(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "NET_SHARES": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_33(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 1,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_34(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "XXscoreXX": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_35(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "SCORE": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_36(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 1.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_37(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "XXfilingsXX": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_38(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "FILINGS": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_39(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 1,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_40(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "XXunique_buyersXX": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_41(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "UNIQUE_BUYERS": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_42(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 1,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_43(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = None
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_44(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=None)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_45(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=None) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_46(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = None
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_47(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(None, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_48(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=None, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_49(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=None) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_50(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_51(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_52(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, ) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_53(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=None)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_54(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=16)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_55(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status == 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_56(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 201:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_57(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = None

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_58(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = None
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_59(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get(None, {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_60(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", None)
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_61(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get({})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_62(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", )
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_63(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get(None, {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_64(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", None).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_65(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get({}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_66(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", ).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_67(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("XXfilingsXX", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_68(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("FILINGS", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_69(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("XXrecentXX", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_70(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("RECENT", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_71(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = None
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_72(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get(None, [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_73(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", None)
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_74(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get([])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_75(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", )
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_76(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("XXformXX", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_77(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("FORM", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_78(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = None
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_79(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get(None, [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_80(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", None)
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_81(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get([])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_82(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", )
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_83(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("XXfilingDateXX", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_84(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingdate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_85(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("FILINGDATE", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_86(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = None
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_87(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get(None, [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_88(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", None)
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_89(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get([])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_90(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", )
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_91(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("XXaccessionNumberXX", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_92(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionnumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_93(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("ACCESSIONNUMBER", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_94(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = None

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_95(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get(None, [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_96(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", None)

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_97(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get([])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_98(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", )

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_99(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("XXprimaryDocumentXX", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_100(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primarydocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_101(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("PRIMARYDOCUMENT", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_102(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = None
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_103(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() + timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_104(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=None)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_105(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = None
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_106(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(None):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_107(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form == "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_108(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "XX4XX":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_109(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    break
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_110(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(None, "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_111(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], None) >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_112(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime("%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_113(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], ) >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_114(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "XX%Y-%m-%dXX") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_115(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_116(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%M-%D") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_117(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") > cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_118(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            None
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_119(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "XXaccXX": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_120(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "ACC": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_121(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace(None, ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_122(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", None),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_123(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace(""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_124(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_125(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("XX-XX", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_126(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", "XXXX"),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_127(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "XXdocXX": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_128(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "DOC": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_129(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    break

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_130(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_131(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(None, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_132(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, None, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_133(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=None)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_134(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_135(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_136(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, )
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_137(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3601)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_138(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = None
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_139(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = None
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_140(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 1
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_141(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = None
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_142(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 1.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_143(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = None

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_144(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:6]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_145(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = None
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_146(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(None)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_147(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = None
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_148(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['XXaccXX']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_149(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['ACC']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_150(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['XXdocXX']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_151(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['DOC']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_152(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(None, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_153(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=None, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_154(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=None) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_155(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_156(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_157(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, ) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_158(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=None)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_159(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=11)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_160(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status == 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_161(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 201:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_162(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            break
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_163(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = None
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_164(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = None
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_165(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(None)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_166(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys = b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_167(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys -= b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_168(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv = bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_169(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv -= bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_170(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells = s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_171(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells -= s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_172(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv = sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_173(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv -= sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_174(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b >= 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_175(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 1:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_176(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(None)
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_177(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["XXaccXX"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_178(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["ACC"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_179(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    break

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_180(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = None
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_181(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys + total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_182(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = None
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_183(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys - total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_184(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = None

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_185(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round(None, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_186(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, None)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_187(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round(1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_188(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, )

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_189(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom / 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_190(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net * denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_191(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 16) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_192(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom >= 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_193(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 1 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_194(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 1.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_195(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 2)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_196(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = None
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_197(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "XXbuysXX": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_198(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "BUYS": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_199(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "XXsellsXX": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_200(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "SELLS": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_201(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "XXbuy_valueXX": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_202(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "BUY_VALUE": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_203(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(None, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_204(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, None),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_205(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_206(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, ),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_207(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 1),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_208(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "XXsell_valueXX": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_209(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "SELL_VALUE": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_210(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(None, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_211(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, None),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_212(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_213(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, ),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_214(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 1),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_215(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "XXnet_sharesXX": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_216(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "NET_SHARES": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_217(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "XXscoreXX": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_218(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "SCORE": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_219(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "XXfilingsXX": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_220(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "FILINGS": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_221(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "XXunique_buyersXX": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_222(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "UNIQUE_BUYERS": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_223(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(None, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_224(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, None, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_225(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=None)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_226(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_227(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_228(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, )
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_229(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3601)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_230(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = None
            return result

    except Exception as e:
        log.warning(f"[edgar] {ticker_upper}: {e}")
        return None


async def x_get_insider_activity__mutmut_231(ticker: str, days: int = 30) -> Optional[dict]:
    ticker_upper = ticker.upper()
    cache_key = f"edgar:insider_activity:{ticker_upper}:{days}"
    redis_cached = await cache_get(cache_key)
    if redis_cached is not None:
        return redis_cached

    cached = _activity_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        return None

    empty = {
        "buys": 0,
        "sells": 0,
        "buy_value": 0,
        "sell_value": 0,
        "net_shares": 0,
        "score": 0.0,
        "filings": 0,
        "unique_buyers": 0,
    }

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            # ── Fetch submission history ────────────────────────────────
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=15)) as r:
                if r.status != 200:
                    return None
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])

            cutoff = datetime.now() - timedelta(days=days)
            form4s = []
            for i, form in enumerate(forms):
                if form != "4":
                    continue
                try:
                    if datetime.strptime(dates[i], "%Y-%m-%d") >= cutoff:
                        form4s.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not form4s:
                await cache_set(cache_key, empty, ttl=3600)
                _activity_cache[ticker_upper] = (empty, time.time())
                return empty

            # ── Parse up to 5 most recent Form 4 XML docs ──────────────
            total_buys = total_sells = 0
            total_bv = total_sv = 0.0
            _buyer_accs: set[str] = set()

            for f4 in form4s[:5]:
                cik_int = int(cik)
                xml_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{f4['acc']}/{f4['doc']}"
                try:
                    async with session.get(xml_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                        if r.status != 200:
                            continue
                        text = await r.text()
                    b, s, bv, sv = _parse_form4(text)
                    total_buys += b
                    total_bv += bv
                    total_sells += s
                    total_sv += sv
                    if b > 0:
                        _buyer_accs.add(f4["acc"])
                except Exception:
                    continue

            net = total_buys - total_sells
            denom = total_buys + total_sells
            # Insiders buying is a stronger signal than selling (sells can be diversification)
            score = round((net / denom * 15) if denom > 0 else 0.0, 1)

            result = {
                "buys": total_buys,
                "sells": total_sells,
                "buy_value": round(total_bv, 0),
                "sell_value": round(total_sv, 0),
                "net_shares": net,
                "score": score,
                "filings": len(form4s),
                "unique_buyers": len(_buyer_accs),
            }
            await cache_set(cache_key, result, ttl=3600)
            _activity_cache[ticker_upper] = (result, time.time())
            return result

    except Exception as e:
        log.warning(None)
        return None

mutants_x_get_insider_activity__mutmut['_mutmut_orig'] = x_get_insider_activity__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_1'] = x_get_insider_activity__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_2'] = x_get_insider_activity__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_3'] = x_get_insider_activity__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_4'] = x_get_insider_activity__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_5'] = x_get_insider_activity__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_6'] = x_get_insider_activity__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_7'] = x_get_insider_activity__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_8'] = x_get_insider_activity__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_9'] = x_get_insider_activity__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_10'] = x_get_insider_activity__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_11'] = x_get_insider_activity__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_12'] = x_get_insider_activity__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_13'] = x_get_insider_activity__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_14'] = x_get_insider_activity__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_15'] = x_get_insider_activity__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_16'] = x_get_insider_activity__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_17'] = x_get_insider_activity__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_18'] = x_get_insider_activity__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_19'] = x_get_insider_activity__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_20'] = x_get_insider_activity__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_21'] = x_get_insider_activity__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_22'] = x_get_insider_activity__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_23'] = x_get_insider_activity__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_24'] = x_get_insider_activity__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_25'] = x_get_insider_activity__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_26'] = x_get_insider_activity__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_27'] = x_get_insider_activity__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_28'] = x_get_insider_activity__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_29'] = x_get_insider_activity__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_30'] = x_get_insider_activity__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_31'] = x_get_insider_activity__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_32'] = x_get_insider_activity__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_33'] = x_get_insider_activity__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_34'] = x_get_insider_activity__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_35'] = x_get_insider_activity__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_36'] = x_get_insider_activity__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_37'] = x_get_insider_activity__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_38'] = x_get_insider_activity__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_39'] = x_get_insider_activity__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_40'] = x_get_insider_activity__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_41'] = x_get_insider_activity__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_42'] = x_get_insider_activity__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_43'] = x_get_insider_activity__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_44'] = x_get_insider_activity__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_45'] = x_get_insider_activity__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_46'] = x_get_insider_activity__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_47'] = x_get_insider_activity__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_48'] = x_get_insider_activity__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_49'] = x_get_insider_activity__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_50'] = x_get_insider_activity__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_51'] = x_get_insider_activity__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_52'] = x_get_insider_activity__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_53'] = x_get_insider_activity__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_54'] = x_get_insider_activity__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_55'] = x_get_insider_activity__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_56'] = x_get_insider_activity__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_57'] = x_get_insider_activity__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_58'] = x_get_insider_activity__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_59'] = x_get_insider_activity__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_60'] = x_get_insider_activity__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_61'] = x_get_insider_activity__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_62'] = x_get_insider_activity__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_63'] = x_get_insider_activity__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_64'] = x_get_insider_activity__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_65'] = x_get_insider_activity__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_66'] = x_get_insider_activity__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_67'] = x_get_insider_activity__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_68'] = x_get_insider_activity__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_69'] = x_get_insider_activity__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_70'] = x_get_insider_activity__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_71'] = x_get_insider_activity__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_72'] = x_get_insider_activity__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_73'] = x_get_insider_activity__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_74'] = x_get_insider_activity__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_75'] = x_get_insider_activity__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_76'] = x_get_insider_activity__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_77'] = x_get_insider_activity__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_78'] = x_get_insider_activity__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_79'] = x_get_insider_activity__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_80'] = x_get_insider_activity__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_81'] = x_get_insider_activity__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_82'] = x_get_insider_activity__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_83'] = x_get_insider_activity__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_84'] = x_get_insider_activity__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_85'] = x_get_insider_activity__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_86'] = x_get_insider_activity__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_87'] = x_get_insider_activity__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_88'] = x_get_insider_activity__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_89'] = x_get_insider_activity__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_90'] = x_get_insider_activity__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_91'] = x_get_insider_activity__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_92'] = x_get_insider_activity__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_93'] = x_get_insider_activity__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_94'] = x_get_insider_activity__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_95'] = x_get_insider_activity__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_96'] = x_get_insider_activity__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_97'] = x_get_insider_activity__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_98'] = x_get_insider_activity__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_99'] = x_get_insider_activity__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_100'] = x_get_insider_activity__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_101'] = x_get_insider_activity__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_102'] = x_get_insider_activity__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_103'] = x_get_insider_activity__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_104'] = x_get_insider_activity__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_105'] = x_get_insider_activity__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_106'] = x_get_insider_activity__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_107'] = x_get_insider_activity__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_108'] = x_get_insider_activity__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_109'] = x_get_insider_activity__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_110'] = x_get_insider_activity__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_111'] = x_get_insider_activity__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_112'] = x_get_insider_activity__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_113'] = x_get_insider_activity__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_114'] = x_get_insider_activity__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_115'] = x_get_insider_activity__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_116'] = x_get_insider_activity__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_117'] = x_get_insider_activity__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_118'] = x_get_insider_activity__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_119'] = x_get_insider_activity__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_120'] = x_get_insider_activity__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_121'] = x_get_insider_activity__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_122'] = x_get_insider_activity__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_123'] = x_get_insider_activity__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_124'] = x_get_insider_activity__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_125'] = x_get_insider_activity__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_126'] = x_get_insider_activity__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_127'] = x_get_insider_activity__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_128'] = x_get_insider_activity__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_129'] = x_get_insider_activity__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_130'] = x_get_insider_activity__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_131'] = x_get_insider_activity__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_132'] = x_get_insider_activity__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_133'] = x_get_insider_activity__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_134'] = x_get_insider_activity__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_135'] = x_get_insider_activity__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_136'] = x_get_insider_activity__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_137'] = x_get_insider_activity__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_138'] = x_get_insider_activity__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_139'] = x_get_insider_activity__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_140'] = x_get_insider_activity__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_141'] = x_get_insider_activity__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_142'] = x_get_insider_activity__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_143'] = x_get_insider_activity__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_144'] = x_get_insider_activity__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_145'] = x_get_insider_activity__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_146'] = x_get_insider_activity__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_147'] = x_get_insider_activity__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_148'] = x_get_insider_activity__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_149'] = x_get_insider_activity__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_150'] = x_get_insider_activity__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_151'] = x_get_insider_activity__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_152'] = x_get_insider_activity__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_153'] = x_get_insider_activity__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_154'] = x_get_insider_activity__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_155'] = x_get_insider_activity__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_156'] = x_get_insider_activity__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_157'] = x_get_insider_activity__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_158'] = x_get_insider_activity__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_159'] = x_get_insider_activity__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_160'] = x_get_insider_activity__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_161'] = x_get_insider_activity__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_162'] = x_get_insider_activity__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_163'] = x_get_insider_activity__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_164'] = x_get_insider_activity__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_165'] = x_get_insider_activity__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_166'] = x_get_insider_activity__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_167'] = x_get_insider_activity__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_168'] = x_get_insider_activity__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_169'] = x_get_insider_activity__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_170'] = x_get_insider_activity__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_171'] = x_get_insider_activity__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_172'] = x_get_insider_activity__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_173'] = x_get_insider_activity__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_174'] = x_get_insider_activity__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_175'] = x_get_insider_activity__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_176'] = x_get_insider_activity__mutmut_176 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_177'] = x_get_insider_activity__mutmut_177 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_178'] = x_get_insider_activity__mutmut_178 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_179'] = x_get_insider_activity__mutmut_179 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_180'] = x_get_insider_activity__mutmut_180 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_181'] = x_get_insider_activity__mutmut_181 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_182'] = x_get_insider_activity__mutmut_182 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_183'] = x_get_insider_activity__mutmut_183 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_184'] = x_get_insider_activity__mutmut_184 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_185'] = x_get_insider_activity__mutmut_185 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_186'] = x_get_insider_activity__mutmut_186 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_187'] = x_get_insider_activity__mutmut_187 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_188'] = x_get_insider_activity__mutmut_188 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_189'] = x_get_insider_activity__mutmut_189 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_190'] = x_get_insider_activity__mutmut_190 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_191'] = x_get_insider_activity__mutmut_191 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_192'] = x_get_insider_activity__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_193'] = x_get_insider_activity__mutmut_193 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_194'] = x_get_insider_activity__mutmut_194 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_195'] = x_get_insider_activity__mutmut_195 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_196'] = x_get_insider_activity__mutmut_196 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_197'] = x_get_insider_activity__mutmut_197 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_198'] = x_get_insider_activity__mutmut_198 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_199'] = x_get_insider_activity__mutmut_199 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_200'] = x_get_insider_activity__mutmut_200 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_201'] = x_get_insider_activity__mutmut_201 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_202'] = x_get_insider_activity__mutmut_202 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_203'] = x_get_insider_activity__mutmut_203 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_204'] = x_get_insider_activity__mutmut_204 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_205'] = x_get_insider_activity__mutmut_205 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_206'] = x_get_insider_activity__mutmut_206 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_207'] = x_get_insider_activity__mutmut_207 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_208'] = x_get_insider_activity__mutmut_208 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_209'] = x_get_insider_activity__mutmut_209 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_210'] = x_get_insider_activity__mutmut_210 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_211'] = x_get_insider_activity__mutmut_211 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_212'] = x_get_insider_activity__mutmut_212 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_213'] = x_get_insider_activity__mutmut_213 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_214'] = x_get_insider_activity__mutmut_214 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_215'] = x_get_insider_activity__mutmut_215 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_216'] = x_get_insider_activity__mutmut_216 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_217'] = x_get_insider_activity__mutmut_217 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_218'] = x_get_insider_activity__mutmut_218 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_219'] = x_get_insider_activity__mutmut_219 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_220'] = x_get_insider_activity__mutmut_220 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_221'] = x_get_insider_activity__mutmut_221 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_222'] = x_get_insider_activity__mutmut_222 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_223'] = x_get_insider_activity__mutmut_223 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_224'] = x_get_insider_activity__mutmut_224 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_225'] = x_get_insider_activity__mutmut_225 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_226'] = x_get_insider_activity__mutmut_226 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_227'] = x_get_insider_activity__mutmut_227 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_228'] = x_get_insider_activity__mutmut_228 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_229'] = x_get_insider_activity__mutmut_229 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_230'] = x_get_insider_activity__mutmut_230 # type: ignore # mutmut generated
mutants_x_get_insider_activity__mutmut['x_get_insider_activity__mutmut_231'] = x_get_insider_activity__mutmut_231 # type: ignore # mutmut generated


# ── 10-K / 10-Q MD&A Delta Analysis ─────────────────────────────────────────
# Tracks textual changes in SEC filings (MD&A and Risk Factors sections) QoQ.
# When companies silently add new risk warnings or change forward-looking language,
# it is a leading indicator of fundamental weakness.
#
# Strategy: fetch the two most recent 10-Q/10-K filings, extract MD&A section text,
# compute word-level diff. Flag: new negative risk language, removed bullish language.
#
# Cache: 24 hours (filings update quarterly).

_mda_cache: dict[str, tuple[dict, float]] = {}
_MDA_CACHE_TTL = 86400  # 24 hours

_RISK_KEYWORDS_NEG = {
    "uncertain",
    "uncertainty",
    "headwind",
    "challenging",
    "deteriorat",
    "decline",
    "decreased",
    "impairment",
    "write-down",
    "writedown",
    "litigation",
    "regulatory",
    "investigation",
    "significant risk",
    "material weakness",
    "going concern",
    "liquidity risk",
    "covenant",
    "adverse",
    "exposure",
    "inflationary",
    "supply constraint",
}
_RISK_KEYWORDS_POS = {
    "growth",
    "expand",
    "acceleration",
    "momentum",
    "record",
    "exceed",
    "outperform",
    "opportunit",
    "invest",
    "innovate",
    "strong demand",
    "favorable",
    "margin improvement",
    "pipeline",
}
mutants_x__fetch_filing_text__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_filing_text__mutmut)
async def _fetch_filing_text(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_orig(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_1(cik: str, form_type: str = "XX10-QXX") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_2(cik: str, form_type: str = "10-q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_3(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = None
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_4(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(None)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_5(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(11)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_6(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = None
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_7(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(None, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_8(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=None, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_9(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=None, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_10(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=None) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_11(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_12(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_13(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_14(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, ) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_15(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_16(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=9)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_17(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status == 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_18(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 201:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_19(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = None

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_20(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = None
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_21(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get(None, {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_22(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", None)
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_23(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get({})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_24(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", )
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_25(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get(None, {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_26(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", None).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_27(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get({}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_28(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", ).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_29(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("XXfilingsXX", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_30(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("FILINGS", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_31(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("XXrecentXX", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_32(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("RECENT", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_33(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = None
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_34(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get(None, [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_35(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", None)
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_36(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get([])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_37(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", )
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_38(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("XXformXX", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_39(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("FORM", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_40(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = None
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_41(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get(None, [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_42(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", None)
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_43(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get([])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_44(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", )
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_45(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("XXaccessionNumberXX", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_46(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionnumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_47(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("ACCESSIONNUMBER", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_48(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = None
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_49(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get(None, [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_50(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", None)
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_51(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get([])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_52(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", )
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_53(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("XXfilingDateXX", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_54(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingdate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_55(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("FILINGDATE", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_56(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = None

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_57(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get(None, [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_58(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", None)

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_59(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get([])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_60(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", )

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_61(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("XXprimaryDocumentXX", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_62(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primarydocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_63(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("PRIMARYDOCUMENT", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_64(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = None

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_65(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(None, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_66(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, None, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_67(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, None, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_68(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, None)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_69(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_70(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_71(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_72(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, )
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_73(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type or doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_74(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form != form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_75(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:3]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_76(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = None
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_77(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace(None, "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_78(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", None)
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_79(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_80(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", )
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_81(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("XX-XX", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_82(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "XXXX")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_83(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = None
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_84(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(None)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_85(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        None, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_86(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=None, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_87(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=None, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_88(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=None
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_89(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_90(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_91(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_92(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_93(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_94(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=11)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_95(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status == 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_96(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 201:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_97(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            break
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_98(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = None

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_99(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors=None)

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_100(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="XXreplaceXX")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_101(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="REPLACE")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_102(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = None
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_103(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(None, " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_104(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", None, html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_105(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", None)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_106(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(" ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_107(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_108(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", )
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_109(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"XX<[^>]+>XX", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_110(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", "XX XX", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_111(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = None
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_112(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(None, " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_113(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", None, plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_114(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", None)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_115(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(" ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_116(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_117(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", )
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_118(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"XX\s+XX", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_119(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", "XX XX", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_120(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(None)  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_121(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50001])  # cap at 50K chars per filing
            except Exception:
                continue
    except Exception:
        pass
    return texts


async def x__fetch_filing_text__mutmut_122(cik: str, form_type: str = "10-Q") -> list[str]:
    """
    Return text of the last 2 10-Q (or 10-K) filings for a given CIK.
    Uses EDGAR XBRL inline viewer for quick text extraction.
    Returns list of (at most 2) filing texts, newest first.
    """
    url = f"https://data.sec.gov/submissions/CIK{cik.zfill(10)}.json"
    texts = []
    try:
        async with shared_session() as sess:
            async with sess.get(url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=8)) as r:
                if r.status != 200:
                    return []
                data = await r.json(content_type=None)

        filings = data.get("filings", {}).get("recent", {})
        forms = filings.get("form", [])
        accs = filings.get("accessionNumber", [])
        dates = filings.get("filingDate", [])
        pri_docs = filings.get("primaryDocument", [])

        targets = [
            (acc, dt, doc)
            for form, acc, dt, doc in zip(forms, accs, dates, pri_docs)
            if form == form_type and doc
        ][:2]

        for acc, _dt, primary_doc in targets:
            acc_clean = acc.replace("-", "")
            doc_url = f"https://www.sec.gov/Archives/edgar/data/{int(cik)}/{acc_clean}/{primary_doc}"
            try:
                async with shared_session() as sess:
                    async with sess.get(
                        doc_url, headers=HEADERS, ssl=_ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)
                    ) as r:
                        if r.status != 200:
                            continue
                        html = await r.text(errors="replace")

                # Extract plain text (strip HTML tags)
                import re as _re

                plain = _re.sub(r"<[^>]+>", " ", html)
                plain = _re.sub(r"\s+", " ", plain)
                texts.append(plain[:50_000])  # cap at 50K chars per filing
            except Exception:
                break
    except Exception:
        pass
    return texts

mutants_x__fetch_filing_text__mutmut['_mutmut_orig'] = x__fetch_filing_text__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_1'] = x__fetch_filing_text__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_2'] = x__fetch_filing_text__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_3'] = x__fetch_filing_text__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_4'] = x__fetch_filing_text__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_5'] = x__fetch_filing_text__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_6'] = x__fetch_filing_text__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_7'] = x__fetch_filing_text__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_8'] = x__fetch_filing_text__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_9'] = x__fetch_filing_text__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_10'] = x__fetch_filing_text__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_11'] = x__fetch_filing_text__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_12'] = x__fetch_filing_text__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_13'] = x__fetch_filing_text__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_14'] = x__fetch_filing_text__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_15'] = x__fetch_filing_text__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_16'] = x__fetch_filing_text__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_17'] = x__fetch_filing_text__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_18'] = x__fetch_filing_text__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_19'] = x__fetch_filing_text__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_20'] = x__fetch_filing_text__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_21'] = x__fetch_filing_text__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_22'] = x__fetch_filing_text__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_23'] = x__fetch_filing_text__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_24'] = x__fetch_filing_text__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_25'] = x__fetch_filing_text__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_26'] = x__fetch_filing_text__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_27'] = x__fetch_filing_text__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_28'] = x__fetch_filing_text__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_29'] = x__fetch_filing_text__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_30'] = x__fetch_filing_text__mutmut_30 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_31'] = x__fetch_filing_text__mutmut_31 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_32'] = x__fetch_filing_text__mutmut_32 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_33'] = x__fetch_filing_text__mutmut_33 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_34'] = x__fetch_filing_text__mutmut_34 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_35'] = x__fetch_filing_text__mutmut_35 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_36'] = x__fetch_filing_text__mutmut_36 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_37'] = x__fetch_filing_text__mutmut_37 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_38'] = x__fetch_filing_text__mutmut_38 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_39'] = x__fetch_filing_text__mutmut_39 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_40'] = x__fetch_filing_text__mutmut_40 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_41'] = x__fetch_filing_text__mutmut_41 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_42'] = x__fetch_filing_text__mutmut_42 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_43'] = x__fetch_filing_text__mutmut_43 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_44'] = x__fetch_filing_text__mutmut_44 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_45'] = x__fetch_filing_text__mutmut_45 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_46'] = x__fetch_filing_text__mutmut_46 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_47'] = x__fetch_filing_text__mutmut_47 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_48'] = x__fetch_filing_text__mutmut_48 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_49'] = x__fetch_filing_text__mutmut_49 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_50'] = x__fetch_filing_text__mutmut_50 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_51'] = x__fetch_filing_text__mutmut_51 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_52'] = x__fetch_filing_text__mutmut_52 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_53'] = x__fetch_filing_text__mutmut_53 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_54'] = x__fetch_filing_text__mutmut_54 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_55'] = x__fetch_filing_text__mutmut_55 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_56'] = x__fetch_filing_text__mutmut_56 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_57'] = x__fetch_filing_text__mutmut_57 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_58'] = x__fetch_filing_text__mutmut_58 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_59'] = x__fetch_filing_text__mutmut_59 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_60'] = x__fetch_filing_text__mutmut_60 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_61'] = x__fetch_filing_text__mutmut_61 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_62'] = x__fetch_filing_text__mutmut_62 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_63'] = x__fetch_filing_text__mutmut_63 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_64'] = x__fetch_filing_text__mutmut_64 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_65'] = x__fetch_filing_text__mutmut_65 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_66'] = x__fetch_filing_text__mutmut_66 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_67'] = x__fetch_filing_text__mutmut_67 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_68'] = x__fetch_filing_text__mutmut_68 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_69'] = x__fetch_filing_text__mutmut_69 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_70'] = x__fetch_filing_text__mutmut_70 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_71'] = x__fetch_filing_text__mutmut_71 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_72'] = x__fetch_filing_text__mutmut_72 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_73'] = x__fetch_filing_text__mutmut_73 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_74'] = x__fetch_filing_text__mutmut_74 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_75'] = x__fetch_filing_text__mutmut_75 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_76'] = x__fetch_filing_text__mutmut_76 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_77'] = x__fetch_filing_text__mutmut_77 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_78'] = x__fetch_filing_text__mutmut_78 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_79'] = x__fetch_filing_text__mutmut_79 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_80'] = x__fetch_filing_text__mutmut_80 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_81'] = x__fetch_filing_text__mutmut_81 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_82'] = x__fetch_filing_text__mutmut_82 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_83'] = x__fetch_filing_text__mutmut_83 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_84'] = x__fetch_filing_text__mutmut_84 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_85'] = x__fetch_filing_text__mutmut_85 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_86'] = x__fetch_filing_text__mutmut_86 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_87'] = x__fetch_filing_text__mutmut_87 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_88'] = x__fetch_filing_text__mutmut_88 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_89'] = x__fetch_filing_text__mutmut_89 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_90'] = x__fetch_filing_text__mutmut_90 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_91'] = x__fetch_filing_text__mutmut_91 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_92'] = x__fetch_filing_text__mutmut_92 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_93'] = x__fetch_filing_text__mutmut_93 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_94'] = x__fetch_filing_text__mutmut_94 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_95'] = x__fetch_filing_text__mutmut_95 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_96'] = x__fetch_filing_text__mutmut_96 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_97'] = x__fetch_filing_text__mutmut_97 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_98'] = x__fetch_filing_text__mutmut_98 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_99'] = x__fetch_filing_text__mutmut_99 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_100'] = x__fetch_filing_text__mutmut_100 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_101'] = x__fetch_filing_text__mutmut_101 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_102'] = x__fetch_filing_text__mutmut_102 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_103'] = x__fetch_filing_text__mutmut_103 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_104'] = x__fetch_filing_text__mutmut_104 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_105'] = x__fetch_filing_text__mutmut_105 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_106'] = x__fetch_filing_text__mutmut_106 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_107'] = x__fetch_filing_text__mutmut_107 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_108'] = x__fetch_filing_text__mutmut_108 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_109'] = x__fetch_filing_text__mutmut_109 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_110'] = x__fetch_filing_text__mutmut_110 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_111'] = x__fetch_filing_text__mutmut_111 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_112'] = x__fetch_filing_text__mutmut_112 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_113'] = x__fetch_filing_text__mutmut_113 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_114'] = x__fetch_filing_text__mutmut_114 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_115'] = x__fetch_filing_text__mutmut_115 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_116'] = x__fetch_filing_text__mutmut_116 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_117'] = x__fetch_filing_text__mutmut_117 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_118'] = x__fetch_filing_text__mutmut_118 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_119'] = x__fetch_filing_text__mutmut_119 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_120'] = x__fetch_filing_text__mutmut_120 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_121'] = x__fetch_filing_text__mutmut_121 # type: ignore # mutmut generated
mutants_x__fetch_filing_text__mutmut['x__fetch_filing_text__mutmut_122'] = x__fetch_filing_text__mutmut_122 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__extract_mda_section__mutmut)
def _extract_mda_section(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_orig(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_1(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = None
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_2(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        None,
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_3(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        None,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_4(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_5(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_6(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"XX(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])XX"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_7(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:MANAGEMENT.{0,30}DISCUSSION.{0,30}ANALYSIS|ITEM\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_8(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"XX(.{500,15000})XX"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_9(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"XX(?:item\s+3|quantitative.{0,30}qualitative|market risk)XX",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_10(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:ITEM\s+3|QUANTITATIVE.{0,30}QUALITATIVE|MARKET RISK)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_11(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE & _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_12(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = None
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_13(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(None)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_14(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(None)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_15(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(2)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_16(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8001]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_17(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = None
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_18(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) / 3
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_19(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 4
    return text[mid : mid + 8000]


def x__extract_mda_section__mutmut_20(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid - 8000]


def x__extract_mda_section__mutmut_21(text: str) -> str:
    """Extract MD&A / Risk Factors section from raw SEC filing text."""
    import re as _re

    mda_pattern = _re.compile(
        r"(?:management.{0,30}discussion.{0,30}analysis|item\s+2[\.\s])"
        r"(.{500,15000})"
        r"(?:item\s+3|quantitative.{0,30}qualitative|market risk)",
        _re.IGNORECASE | _re.DOTALL,
    )
    m = mda_pattern.search(text)
    if m:
        return m.group(1)[:8000]
    # Fallback: return middle section of filing
    mid = len(text) // 3
    return text[mid : mid + 8001]

mutants_x__extract_mda_section__mutmut['_mutmut_orig'] = x__extract_mda_section__mutmut_orig # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_1'] = x__extract_mda_section__mutmut_1 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_2'] = x__extract_mda_section__mutmut_2 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_3'] = x__extract_mda_section__mutmut_3 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_4'] = x__extract_mda_section__mutmut_4 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_5'] = x__extract_mda_section__mutmut_5 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_6'] = x__extract_mda_section__mutmut_6 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_7'] = x__extract_mda_section__mutmut_7 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_8'] = x__extract_mda_section__mutmut_8 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_9'] = x__extract_mda_section__mutmut_9 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_10'] = x__extract_mda_section__mutmut_10 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_11'] = x__extract_mda_section__mutmut_11 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_12'] = x__extract_mda_section__mutmut_12 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_13'] = x__extract_mda_section__mutmut_13 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_14'] = x__extract_mda_section__mutmut_14 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_15'] = x__extract_mda_section__mutmut_15 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_16'] = x__extract_mda_section__mutmut_16 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_17'] = x__extract_mda_section__mutmut_17 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_18'] = x__extract_mda_section__mutmut_18 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_19'] = x__extract_mda_section__mutmut_19 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_20'] = x__extract_mda_section__mutmut_20 # type: ignore # mutmut generated
mutants_x__extract_mda_section__mutmut['x__extract_mda_section__mutmut_21'] = x__extract_mda_section__mutmut_21 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__score_mda_delta__mutmut)
def _score_mda_delta(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_orig(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_1(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(None)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_2(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.upper() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_3(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(None, t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_4(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", None) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_5(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_6(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", ) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_7(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.rsplit(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_8(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"XX\W+XX", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_9(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) >= 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_10(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 4)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_11(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = None
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_12(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(None)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_13(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = None
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_14(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(None)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_15(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = None  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_16(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w + prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_17(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = None  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_18(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w + cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_19(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = None
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_20(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(None)
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_21(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(2 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_22(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(None))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_23(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw not in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_24(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = None
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_25(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(None)
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_26(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(2 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_27(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(None))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_28(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw not in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_29(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = None

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_30(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(None)

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_31(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(2 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_32(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(None))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_33(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw not in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_34(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = None
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_35(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 + new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_36(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) / 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_37(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos + gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_38(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 2.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_39(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg / 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_40(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 3.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_41(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = None

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_42(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(None, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_43(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, None)

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_44(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_45(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, )

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_46(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(+8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_47(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-9.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_48(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(None, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_49(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, None))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_50(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_51(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, ))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_52(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(9.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_53(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(None, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_54(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, None)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_55(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_56(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, )))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_57(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 2)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_58(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = None
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_59(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg >= 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_60(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 3:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_61(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(None)
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_62(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos >= 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_63(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 3:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_64(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(None)
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_65(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos >= 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_66(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 3:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_67(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(None)
    reason = "; ".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_68(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = None
    return score_delta, reason


def x__score_mda_delta__mutmut_69(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(None) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_70(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "XX; XX".join(reason_parts) if reason_parts else "No significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_71(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "XXNo significant language changeXX"
    return score_delta, reason


def x__score_mda_delta__mutmut_72(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "no significant language change"
    return score_delta, reason


def x__score_mda_delta__mutmut_73(current: str, previous: str) -> tuple[float, str]:
    """
    Compare two MD&A texts and return (score_delta, human_reason).
    Positive delta = current filing more bullish / less risky than prior.
    Negative delta = new risk language added or bullish language removed.
    """
    import re as _re

    def _words(t: str) -> set[str]:
        return set(w.lower() for w in _re.split(r"\W+", t) if len(w) > 3)

    cur_w = _words(current)
    prev_w = _words(previous)
    new_w = cur_w - prev_w  # words added in current filing
    gone_w = prev_w - cur_w  # words removed from current filing

    # Count newly added risk vs bullish language
    new_neg = sum(1 for kw in _RISK_KEYWORDS_NEG if any(kw in w for w in new_w))
    new_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in new_w))
    gone_pos = sum(1 for kw in _RISK_KEYWORDS_POS if any(kw in w for w in gone_w))

    score_delta = (new_pos - gone_pos) * 1.5 - new_neg * 2.0
    score_delta = max(-8.0, min(8.0, round(score_delta, 1)))

    reason_parts = []
    if new_neg > 2:
        reason_parts.append(f"{new_neg} new risk terms added")
    if gone_pos > 2:
        reason_parts.append(f"{gone_pos} bullish terms removed")
    if new_pos > 2:
        reason_parts.append(f"{new_pos} new positive terms")
    reason = "; ".join(reason_parts) if reason_parts else "NO SIGNIFICANT LANGUAGE CHANGE"
    return score_delta, reason

mutants_x__score_mda_delta__mutmut['_mutmut_orig'] = x__score_mda_delta__mutmut_orig # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_1'] = x__score_mda_delta__mutmut_1 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_2'] = x__score_mda_delta__mutmut_2 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_3'] = x__score_mda_delta__mutmut_3 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_4'] = x__score_mda_delta__mutmut_4 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_5'] = x__score_mda_delta__mutmut_5 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_6'] = x__score_mda_delta__mutmut_6 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_7'] = x__score_mda_delta__mutmut_7 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_8'] = x__score_mda_delta__mutmut_8 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_9'] = x__score_mda_delta__mutmut_9 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_10'] = x__score_mda_delta__mutmut_10 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_11'] = x__score_mda_delta__mutmut_11 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_12'] = x__score_mda_delta__mutmut_12 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_13'] = x__score_mda_delta__mutmut_13 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_14'] = x__score_mda_delta__mutmut_14 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_15'] = x__score_mda_delta__mutmut_15 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_16'] = x__score_mda_delta__mutmut_16 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_17'] = x__score_mda_delta__mutmut_17 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_18'] = x__score_mda_delta__mutmut_18 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_19'] = x__score_mda_delta__mutmut_19 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_20'] = x__score_mda_delta__mutmut_20 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_21'] = x__score_mda_delta__mutmut_21 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_22'] = x__score_mda_delta__mutmut_22 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_23'] = x__score_mda_delta__mutmut_23 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_24'] = x__score_mda_delta__mutmut_24 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_25'] = x__score_mda_delta__mutmut_25 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_26'] = x__score_mda_delta__mutmut_26 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_27'] = x__score_mda_delta__mutmut_27 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_28'] = x__score_mda_delta__mutmut_28 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_29'] = x__score_mda_delta__mutmut_29 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_30'] = x__score_mda_delta__mutmut_30 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_31'] = x__score_mda_delta__mutmut_31 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_32'] = x__score_mda_delta__mutmut_32 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_33'] = x__score_mda_delta__mutmut_33 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_34'] = x__score_mda_delta__mutmut_34 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_35'] = x__score_mda_delta__mutmut_35 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_36'] = x__score_mda_delta__mutmut_36 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_37'] = x__score_mda_delta__mutmut_37 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_38'] = x__score_mda_delta__mutmut_38 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_39'] = x__score_mda_delta__mutmut_39 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_40'] = x__score_mda_delta__mutmut_40 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_41'] = x__score_mda_delta__mutmut_41 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_42'] = x__score_mda_delta__mutmut_42 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_43'] = x__score_mda_delta__mutmut_43 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_44'] = x__score_mda_delta__mutmut_44 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_45'] = x__score_mda_delta__mutmut_45 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_46'] = x__score_mda_delta__mutmut_46 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_47'] = x__score_mda_delta__mutmut_47 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_48'] = x__score_mda_delta__mutmut_48 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_49'] = x__score_mda_delta__mutmut_49 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_50'] = x__score_mda_delta__mutmut_50 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_51'] = x__score_mda_delta__mutmut_51 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_52'] = x__score_mda_delta__mutmut_52 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_53'] = x__score_mda_delta__mutmut_53 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_54'] = x__score_mda_delta__mutmut_54 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_55'] = x__score_mda_delta__mutmut_55 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_56'] = x__score_mda_delta__mutmut_56 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_57'] = x__score_mda_delta__mutmut_57 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_58'] = x__score_mda_delta__mutmut_58 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_59'] = x__score_mda_delta__mutmut_59 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_60'] = x__score_mda_delta__mutmut_60 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_61'] = x__score_mda_delta__mutmut_61 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_62'] = x__score_mda_delta__mutmut_62 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_63'] = x__score_mda_delta__mutmut_63 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_64'] = x__score_mda_delta__mutmut_64 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_65'] = x__score_mda_delta__mutmut_65 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_66'] = x__score_mda_delta__mutmut_66 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_67'] = x__score_mda_delta__mutmut_67 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_68'] = x__score_mda_delta__mutmut_68 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_69'] = x__score_mda_delta__mutmut_69 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_70'] = x__score_mda_delta__mutmut_70 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_71'] = x__score_mda_delta__mutmut_71 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_72'] = x__score_mda_delta__mutmut_72 # type: ignore # mutmut generated
mutants_x__score_mda_delta__mutmut['x__score_mda_delta__mutmut_73'] = x__score_mda_delta__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_mda_delta__mutmut)
async def get_mda_delta(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_orig(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_1(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = None
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_2(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(None)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_3(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached or time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_4(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() + cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_5(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[2] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_6(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] <= _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_7(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[1]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_8(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = None
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_9(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(None)
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_10(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.lower())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_11(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_12(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = None
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_13(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = None
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_14(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(None, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_15(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, None)
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_16(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text("10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_17(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, )
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_18(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "XX10-QXX")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_19(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_20(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = None
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_21(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "XX10-QXX"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_22(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_23(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) <= 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_24(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 3:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_25(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = None
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_26(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(None, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_27(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, None)
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_28(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text("10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_29(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, )
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_30(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "XX10-KXX")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_31(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-k")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_32(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = None
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_33(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "XX10-KXX"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_34(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-k"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_35(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) <= 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_36(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 3:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_37(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = None
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_38(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = None
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_39(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(None)
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_40(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[1])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_41(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = None
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_42(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(None)
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_43(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[2])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_44(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = None

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_45(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(None, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_46(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, None)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_47(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_48(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, )

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_49(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = None
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_50(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"XXscoreXX": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_51(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"SCORE": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_52(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "XXreasonXX": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_53(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "REASON": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_54(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "XXform_typeXX": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_55(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "FORM_TYPE": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_56(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = None
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_57(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(None)
        _mda_cache[ticker] = ({}, time.time())
        return {}


async def x_get_mda_delta__mutmut_58(ticker: str) -> dict:
    """
    Fetch the two most recent 10-Q filings for `ticker`, extract MD&A sections,
    and compute a score delta based on language shift.
    Returns {"score": float, "reason": str, "form_type": str} or {}.
    Cache: 24 hours.
    """
    cached = _mda_cache.get(ticker)
    if cached and time.time() - cached[1] < _MDA_CACHE_TTL:
        return cached[0]

    cik = _KNOWN_CIKS.get(ticker.upper())
    if not cik:
        _mda_cache[ticker] = ({}, time.time())
        return {}

    try:
        texts = await _fetch_filing_text(cik, "10-Q")
        form_type = "10-Q"
        if len(texts) < 2:
            texts = await _fetch_filing_text(cik, "10-K")
            form_type = "10-K"
        if len(texts) < 2:
            _mda_cache[ticker] = ({}, time.time())
            return {}

        mda_cur = _extract_mda_section(texts[0])
        mda_prev = _extract_mda_section(texts[1])
        delta, reason = _score_mda_delta(mda_cur, mda_prev)

        result = {"score": delta, "reason": reason, "form_type": form_type}
        _mda_cache[ticker] = (result, time.time())
        return result
    except Exception as e:
        log.warning(f"[edgar mda_delta] {ticker}: {e}")
        _mda_cache[ticker] = None
        return {}

mutants_x_get_mda_delta__mutmut['_mutmut_orig'] = x_get_mda_delta__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_1'] = x_get_mda_delta__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_2'] = x_get_mda_delta__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_3'] = x_get_mda_delta__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_4'] = x_get_mda_delta__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_5'] = x_get_mda_delta__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_6'] = x_get_mda_delta__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_7'] = x_get_mda_delta__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_8'] = x_get_mda_delta__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_9'] = x_get_mda_delta__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_10'] = x_get_mda_delta__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_11'] = x_get_mda_delta__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_12'] = x_get_mda_delta__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_13'] = x_get_mda_delta__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_14'] = x_get_mda_delta__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_15'] = x_get_mda_delta__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_16'] = x_get_mda_delta__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_17'] = x_get_mda_delta__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_18'] = x_get_mda_delta__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_19'] = x_get_mda_delta__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_20'] = x_get_mda_delta__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_21'] = x_get_mda_delta__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_22'] = x_get_mda_delta__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_23'] = x_get_mda_delta__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_24'] = x_get_mda_delta__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_25'] = x_get_mda_delta__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_26'] = x_get_mda_delta__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_27'] = x_get_mda_delta__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_28'] = x_get_mda_delta__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_29'] = x_get_mda_delta__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_30'] = x_get_mda_delta__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_31'] = x_get_mda_delta__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_32'] = x_get_mda_delta__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_33'] = x_get_mda_delta__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_34'] = x_get_mda_delta__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_35'] = x_get_mda_delta__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_36'] = x_get_mda_delta__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_37'] = x_get_mda_delta__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_38'] = x_get_mda_delta__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_39'] = x_get_mda_delta__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_40'] = x_get_mda_delta__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_41'] = x_get_mda_delta__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_42'] = x_get_mda_delta__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_43'] = x_get_mda_delta__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_44'] = x_get_mda_delta__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_45'] = x_get_mda_delta__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_46'] = x_get_mda_delta__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_47'] = x_get_mda_delta__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_48'] = x_get_mda_delta__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_49'] = x_get_mda_delta__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_50'] = x_get_mda_delta__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_51'] = x_get_mda_delta__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_52'] = x_get_mda_delta__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_53'] = x_get_mda_delta__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_54'] = x_get_mda_delta__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_55'] = x_get_mda_delta__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_56'] = x_get_mda_delta__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_57'] = x_get_mda_delta__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_mda_delta__mutmut['x_get_mda_delta__mutmut_58'] = x_get_mda_delta__mutmut_58 # type: ignore # mutmut generated


_buyback_cache: dict[str, tuple[bool, float]] = {}
_BUYBACK_CACHE_TTL = 3600  # 1 hour
mutants_x_has_active_buyback__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_has_active_buyback__mutmut)
async def has_active_buyback(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_orig(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_1(ticker: str, days: int = 91) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_2(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = None
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_3(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.lower()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_4(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = None
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_5(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(None)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_6(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached or time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_7(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() + cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_8(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[2] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_9(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] <= _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_10(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[1]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_11(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = None
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_12(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(None)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_13(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_14(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = None
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_15(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (True, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_16(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return True

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_17(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = None
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_18(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=None)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_19(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=None) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_20(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = None
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_21(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(None, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_22(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=None, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_23(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=None) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_24(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_25(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_26(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, ) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_27(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=None)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_28(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=11)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_29(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status == 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_30(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 201:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_31(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = None
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_32(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (True, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_33(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return True
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_34(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = None

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_35(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = None
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_36(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get(None, {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_37(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", None)
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_38(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get({})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_39(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", )
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_40(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get(None, {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_41(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", None).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_42(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get({}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_43(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", ).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_44(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("XXfilingsXX", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_45(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("FILINGS", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_46(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("XXrecentXX", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_47(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("RECENT", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_48(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = None
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_49(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get(None, [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_50(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", None)
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_51(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get([])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_52(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", )
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_53(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("XXformXX", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_54(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("FORM", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_55(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = None
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_56(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get(None, [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_57(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", None)
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_58(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get([])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_59(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", )
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_60(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("XXfilingDateXX", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_61(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingdate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_62(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("FILINGDATE", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_63(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = None
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_64(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get(None, [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_65(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", None)
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_66(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get([])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_67(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", )
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_68(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("XXaccessionNumberXX", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_69(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionnumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_70(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("ACCESSIONNUMBER", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_71(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = None
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_72(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get(None, [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_73(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", None)
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_74(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get([])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_75(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", )
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_76(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("XXprimaryDocumentXX", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_77(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primarydocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_78(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("PRIMARYDOCUMENT", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_79(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = None

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_80(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get(None, [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_81(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", None)

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_82(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get([])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_83(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", )

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_84(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("XXitemsXX", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_85(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("ITEMS", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_86(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = None
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_87(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() + timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_88(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=None)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_89(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = None
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_90(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(None):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_91(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form == "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_92(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "XX8-KXX":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_93(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-k":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_94(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    break
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_95(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = None
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_96(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(None, "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_97(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], None)
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_98(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime("%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_99(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], )
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_100(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "XX%Y-%m-%dXX")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_101(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_102(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%M-%D")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_103(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date > cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_104(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = None
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_105(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(None) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_106(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i <= len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_107(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else "XXXX"
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_108(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            None
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_109(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "XXaccXX": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_110(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "ACC": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_111(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace(None, ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_112(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", None),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_113(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace(""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_114(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_115(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("XX-XX", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_116(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", "XXXX"),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_117(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "XXdocXX": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_118(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "DOC": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_119(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "XXitemsXX": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_120(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "ITEMS": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_121(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    break

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_122(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_123(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = None
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_124(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (True, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_125(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return True

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_126(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = None

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_127(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "XXshare repurchaseXX",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_128(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "SHARE REPURCHASE",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_129(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "XXstock repurchaseXX",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_130(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "STOCK REPURCHASE",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_131(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "XXshare buybackXX",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_132(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "SHARE BUYBACK",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_133(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "XXstock buybackXX",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_134(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "STOCK BUYBACK",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_135(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "XXrepurchase programXX",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_136(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "REPURCHASE PROGRAM",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_137(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "XXbuyback programXX",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_138(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "BUYBACK PROGRAM",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_139(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "XXrepurchase of up toXX",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_140(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "REPURCHASE OF UP TO",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_141(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "XXbuyback of up toXX",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_142(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "BUYBACK OF UP TO",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_143(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:6]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_144(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] or "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_145(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] or "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_146(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "XX8.01XX" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_147(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_148(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["XXitemsXX"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_149(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["ITEMS"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_150(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "XX7.01XX" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_151(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_152(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["XXitemsXX"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_153(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["ITEMS"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_154(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "XX1.01XX" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_155(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_156(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["XXitemsXX"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_157(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["ITEMS"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_158(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    break

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_159(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = None
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_160(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(None)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_161(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = None
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_162(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['XXaccXX']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_163(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['ACC']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_164(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['XXdocXX']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_165(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['DOC']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_166(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(None, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_167(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=None, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_168(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=None) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_169(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_170(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_171(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, ) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_172(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=None)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_173(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=9)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_174(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status == 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_175(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 201:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_176(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            break
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_177(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = None
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_178(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).upper()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_179(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(None):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_180(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw not in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_181(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                None
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_182(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['XXaccXX']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_183(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['ACC']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_184(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = None
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_185(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (False, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_186(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return False
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_187(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(None)
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_188(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    break

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_189(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(None)

    _buyback_cache[ticker_upper] = (False, time.time())
    return False


async def x_has_active_buyback__mutmut_190(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = None
    return False


async def x_has_active_buyback__mutmut_191(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (True, time.time())
    return False


async def x_has_active_buyback__mutmut_192(ticker: str, days: int = 90) -> bool:
    """
    Check if the company has filed an 8-K disclosing a share buyback / repurchase
    program within the last `days` (default 90).
    """
    ticker_upper = ticker.upper()
    cached = _buyback_cache.get(ticker_upper)
    if cached and time.time() - cached[1] < _BUYBACK_CACHE_TTL:
        return cached[0]

    cik = await _get_cik(ticker_upper)
    if not cik:
        _buyback_cache[ticker_upper] = (False, time.time())
        return False

    try:
        connector = aiohttp.TCPConnector(ssl=_ssl_ctx)
        async with aiohttp.ClientSession(connector=connector) as session:
            url = f"https://data.sec.gov/submissions/CIK{cik}.json"
            async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as r:
                if r.status != 200:
                    _buyback_cache[ticker_upper] = (False, time.time())
                    return False
                subs = await r.json(content_type=None)

            recent = subs.get("filings", {}).get("recent", {})
            forms = recent.get("form", [])
            dates = recent.get("filingDate", [])
            accessions = recent.get("accessionNumber", [])
            pri_docs = recent.get("primaryDocument", [])
            items_list = recent.get("items", [])

            cutoff = datetime.now() - timedelta(days=days)
            recent_8ks = []
            for i, form in enumerate(forms):
                if form != "8-K":
                    continue
                try:
                    filing_date = datetime.strptime(dates[i], "%Y-%m-%d")
                    if filing_date >= cutoff:
                        items_str = str(items_list[i]) if i < len(items_list) else ""
                        recent_8ks.append(
                            {
                                "acc": accessions[i].replace("-", ""),
                                "doc": pri_docs[i],
                                "items": items_str,
                            }
                        )
                except (ValueError, IndexError):
                    continue

            if not recent_8ks:
                _buyback_cache[ticker_upper] = (False, time.time())
                return False

            buyback_keywords = [
                "share repurchase",
                "stock repurchase",
                "share buyback",
                "stock buyback",
                "repurchase program",
                "buyback program",
                "repurchase of up to",
                "buyback of up to",
            ]

            for filing in recent_8ks[:5]:
                if "8.01" not in filing["items"] and "7.01" not in filing["items"] and "1.01" not in filing["items"]:
                    continue

                cik_int = int(cik)
                doc_url = f"https://www.sec.gov/Archives/edgar/data/{cik_int}/{filing['acc']}/{filing['doc']}"
                try:
                    async with session.get(doc_url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=8)) as r:
                        if r.status != 200:
                            continue
                        html_text = (await r.text()).lower()
                        if any(kw in html_text for kw in buyback_keywords):
                            log.info(
                                f"[edgar] Found active buyback announcement in 8-K for {ticker_upper} filed on {filing['acc']}"
                            )
                            _buyback_cache[ticker_upper] = (True, time.time())
                            return True
                except Exception as e:
                    log.debug(f"[edgar] failed to parse 8-K doc for {ticker_upper}: {e}")
                    continue

    except Exception as e:
        log.warning(f"[edgar] buyback check failed for {ticker_upper}: {e}")

    _buyback_cache[ticker_upper] = (False, time.time())
    return True

mutants_x_has_active_buyback__mutmut['_mutmut_orig'] = x_has_active_buyback__mutmut_orig # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_1'] = x_has_active_buyback__mutmut_1 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_2'] = x_has_active_buyback__mutmut_2 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_3'] = x_has_active_buyback__mutmut_3 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_4'] = x_has_active_buyback__mutmut_4 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_5'] = x_has_active_buyback__mutmut_5 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_6'] = x_has_active_buyback__mutmut_6 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_7'] = x_has_active_buyback__mutmut_7 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_8'] = x_has_active_buyback__mutmut_8 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_9'] = x_has_active_buyback__mutmut_9 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_10'] = x_has_active_buyback__mutmut_10 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_11'] = x_has_active_buyback__mutmut_11 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_12'] = x_has_active_buyback__mutmut_12 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_13'] = x_has_active_buyback__mutmut_13 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_14'] = x_has_active_buyback__mutmut_14 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_15'] = x_has_active_buyback__mutmut_15 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_16'] = x_has_active_buyback__mutmut_16 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_17'] = x_has_active_buyback__mutmut_17 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_18'] = x_has_active_buyback__mutmut_18 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_19'] = x_has_active_buyback__mutmut_19 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_20'] = x_has_active_buyback__mutmut_20 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_21'] = x_has_active_buyback__mutmut_21 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_22'] = x_has_active_buyback__mutmut_22 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_23'] = x_has_active_buyback__mutmut_23 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_24'] = x_has_active_buyback__mutmut_24 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_25'] = x_has_active_buyback__mutmut_25 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_26'] = x_has_active_buyback__mutmut_26 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_27'] = x_has_active_buyback__mutmut_27 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_28'] = x_has_active_buyback__mutmut_28 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_29'] = x_has_active_buyback__mutmut_29 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_30'] = x_has_active_buyback__mutmut_30 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_31'] = x_has_active_buyback__mutmut_31 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_32'] = x_has_active_buyback__mutmut_32 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_33'] = x_has_active_buyback__mutmut_33 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_34'] = x_has_active_buyback__mutmut_34 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_35'] = x_has_active_buyback__mutmut_35 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_36'] = x_has_active_buyback__mutmut_36 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_37'] = x_has_active_buyback__mutmut_37 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_38'] = x_has_active_buyback__mutmut_38 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_39'] = x_has_active_buyback__mutmut_39 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_40'] = x_has_active_buyback__mutmut_40 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_41'] = x_has_active_buyback__mutmut_41 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_42'] = x_has_active_buyback__mutmut_42 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_43'] = x_has_active_buyback__mutmut_43 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_44'] = x_has_active_buyback__mutmut_44 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_45'] = x_has_active_buyback__mutmut_45 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_46'] = x_has_active_buyback__mutmut_46 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_47'] = x_has_active_buyback__mutmut_47 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_48'] = x_has_active_buyback__mutmut_48 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_49'] = x_has_active_buyback__mutmut_49 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_50'] = x_has_active_buyback__mutmut_50 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_51'] = x_has_active_buyback__mutmut_51 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_52'] = x_has_active_buyback__mutmut_52 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_53'] = x_has_active_buyback__mutmut_53 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_54'] = x_has_active_buyback__mutmut_54 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_55'] = x_has_active_buyback__mutmut_55 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_56'] = x_has_active_buyback__mutmut_56 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_57'] = x_has_active_buyback__mutmut_57 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_58'] = x_has_active_buyback__mutmut_58 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_59'] = x_has_active_buyback__mutmut_59 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_60'] = x_has_active_buyback__mutmut_60 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_61'] = x_has_active_buyback__mutmut_61 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_62'] = x_has_active_buyback__mutmut_62 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_63'] = x_has_active_buyback__mutmut_63 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_64'] = x_has_active_buyback__mutmut_64 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_65'] = x_has_active_buyback__mutmut_65 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_66'] = x_has_active_buyback__mutmut_66 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_67'] = x_has_active_buyback__mutmut_67 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_68'] = x_has_active_buyback__mutmut_68 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_69'] = x_has_active_buyback__mutmut_69 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_70'] = x_has_active_buyback__mutmut_70 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_71'] = x_has_active_buyback__mutmut_71 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_72'] = x_has_active_buyback__mutmut_72 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_73'] = x_has_active_buyback__mutmut_73 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_74'] = x_has_active_buyback__mutmut_74 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_75'] = x_has_active_buyback__mutmut_75 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_76'] = x_has_active_buyback__mutmut_76 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_77'] = x_has_active_buyback__mutmut_77 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_78'] = x_has_active_buyback__mutmut_78 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_79'] = x_has_active_buyback__mutmut_79 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_80'] = x_has_active_buyback__mutmut_80 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_81'] = x_has_active_buyback__mutmut_81 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_82'] = x_has_active_buyback__mutmut_82 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_83'] = x_has_active_buyback__mutmut_83 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_84'] = x_has_active_buyback__mutmut_84 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_85'] = x_has_active_buyback__mutmut_85 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_86'] = x_has_active_buyback__mutmut_86 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_87'] = x_has_active_buyback__mutmut_87 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_88'] = x_has_active_buyback__mutmut_88 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_89'] = x_has_active_buyback__mutmut_89 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_90'] = x_has_active_buyback__mutmut_90 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_91'] = x_has_active_buyback__mutmut_91 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_92'] = x_has_active_buyback__mutmut_92 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_93'] = x_has_active_buyback__mutmut_93 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_94'] = x_has_active_buyback__mutmut_94 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_95'] = x_has_active_buyback__mutmut_95 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_96'] = x_has_active_buyback__mutmut_96 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_97'] = x_has_active_buyback__mutmut_97 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_98'] = x_has_active_buyback__mutmut_98 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_99'] = x_has_active_buyback__mutmut_99 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_100'] = x_has_active_buyback__mutmut_100 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_101'] = x_has_active_buyback__mutmut_101 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_102'] = x_has_active_buyback__mutmut_102 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_103'] = x_has_active_buyback__mutmut_103 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_104'] = x_has_active_buyback__mutmut_104 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_105'] = x_has_active_buyback__mutmut_105 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_106'] = x_has_active_buyback__mutmut_106 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_107'] = x_has_active_buyback__mutmut_107 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_108'] = x_has_active_buyback__mutmut_108 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_109'] = x_has_active_buyback__mutmut_109 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_110'] = x_has_active_buyback__mutmut_110 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_111'] = x_has_active_buyback__mutmut_111 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_112'] = x_has_active_buyback__mutmut_112 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_113'] = x_has_active_buyback__mutmut_113 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_114'] = x_has_active_buyback__mutmut_114 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_115'] = x_has_active_buyback__mutmut_115 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_116'] = x_has_active_buyback__mutmut_116 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_117'] = x_has_active_buyback__mutmut_117 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_118'] = x_has_active_buyback__mutmut_118 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_119'] = x_has_active_buyback__mutmut_119 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_120'] = x_has_active_buyback__mutmut_120 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_121'] = x_has_active_buyback__mutmut_121 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_122'] = x_has_active_buyback__mutmut_122 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_123'] = x_has_active_buyback__mutmut_123 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_124'] = x_has_active_buyback__mutmut_124 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_125'] = x_has_active_buyback__mutmut_125 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_126'] = x_has_active_buyback__mutmut_126 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_127'] = x_has_active_buyback__mutmut_127 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_128'] = x_has_active_buyback__mutmut_128 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_129'] = x_has_active_buyback__mutmut_129 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_130'] = x_has_active_buyback__mutmut_130 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_131'] = x_has_active_buyback__mutmut_131 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_132'] = x_has_active_buyback__mutmut_132 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_133'] = x_has_active_buyback__mutmut_133 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_134'] = x_has_active_buyback__mutmut_134 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_135'] = x_has_active_buyback__mutmut_135 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_136'] = x_has_active_buyback__mutmut_136 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_137'] = x_has_active_buyback__mutmut_137 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_138'] = x_has_active_buyback__mutmut_138 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_139'] = x_has_active_buyback__mutmut_139 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_140'] = x_has_active_buyback__mutmut_140 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_141'] = x_has_active_buyback__mutmut_141 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_142'] = x_has_active_buyback__mutmut_142 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_143'] = x_has_active_buyback__mutmut_143 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_144'] = x_has_active_buyback__mutmut_144 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_145'] = x_has_active_buyback__mutmut_145 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_146'] = x_has_active_buyback__mutmut_146 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_147'] = x_has_active_buyback__mutmut_147 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_148'] = x_has_active_buyback__mutmut_148 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_149'] = x_has_active_buyback__mutmut_149 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_150'] = x_has_active_buyback__mutmut_150 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_151'] = x_has_active_buyback__mutmut_151 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_152'] = x_has_active_buyback__mutmut_152 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_153'] = x_has_active_buyback__mutmut_153 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_154'] = x_has_active_buyback__mutmut_154 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_155'] = x_has_active_buyback__mutmut_155 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_156'] = x_has_active_buyback__mutmut_156 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_157'] = x_has_active_buyback__mutmut_157 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_158'] = x_has_active_buyback__mutmut_158 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_159'] = x_has_active_buyback__mutmut_159 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_160'] = x_has_active_buyback__mutmut_160 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_161'] = x_has_active_buyback__mutmut_161 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_162'] = x_has_active_buyback__mutmut_162 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_163'] = x_has_active_buyback__mutmut_163 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_164'] = x_has_active_buyback__mutmut_164 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_165'] = x_has_active_buyback__mutmut_165 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_166'] = x_has_active_buyback__mutmut_166 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_167'] = x_has_active_buyback__mutmut_167 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_168'] = x_has_active_buyback__mutmut_168 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_169'] = x_has_active_buyback__mutmut_169 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_170'] = x_has_active_buyback__mutmut_170 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_171'] = x_has_active_buyback__mutmut_171 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_172'] = x_has_active_buyback__mutmut_172 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_173'] = x_has_active_buyback__mutmut_173 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_174'] = x_has_active_buyback__mutmut_174 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_175'] = x_has_active_buyback__mutmut_175 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_176'] = x_has_active_buyback__mutmut_176 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_177'] = x_has_active_buyback__mutmut_177 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_178'] = x_has_active_buyback__mutmut_178 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_179'] = x_has_active_buyback__mutmut_179 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_180'] = x_has_active_buyback__mutmut_180 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_181'] = x_has_active_buyback__mutmut_181 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_182'] = x_has_active_buyback__mutmut_182 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_183'] = x_has_active_buyback__mutmut_183 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_184'] = x_has_active_buyback__mutmut_184 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_185'] = x_has_active_buyback__mutmut_185 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_186'] = x_has_active_buyback__mutmut_186 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_187'] = x_has_active_buyback__mutmut_187 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_188'] = x_has_active_buyback__mutmut_188 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_189'] = x_has_active_buyback__mutmut_189 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_190'] = x_has_active_buyback__mutmut_190 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_191'] = x_has_active_buyback__mutmut_191 # type: ignore # mutmut generated
mutants_x_has_active_buyback__mutmut['x_has_active_buyback__mutmut_192'] = x_has_active_buyback__mutmut_192 # type: ignore # mutmut generated
