import asyncio
import logging
from datetime import datetime, timezone as _tz
import stripe
from sqlalchemy import select
from config import get_settings
from models import User

log = logging.getLogger("signal.trade.reconciliation")


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_reconcile_stripe_subscriptions__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_reconcile_stripe_subscriptions__mutmut)
async def reconcile_stripe_subscriptions(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_orig(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_1(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = None
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_2(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_3(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning(None)
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_4(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("XX[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)XX")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_5(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] stripe is not configured (missing stripe_secret_key)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_6(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[RECONCILE] STRIPE IS NOT CONFIGURED (MISSING STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_7(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = None

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_8(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = None
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_9(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where(None)
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_10(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(None).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_11(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) & (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_12(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = None
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_13(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(None)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_14(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(None)

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_15(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = None

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_16(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 1

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_17(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = None

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_18(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(None, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_19(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, None)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_20(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_21(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, )

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_22(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = None
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_23(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get(None, "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_24(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", None)
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_25(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_26(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", )
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_27(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("XXstatusXX", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_28(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("STATUS", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_29(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "XXinactiveXX")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_30(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "INACTIVE")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_31(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = None

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_32(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "XXactiveXX"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_33(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "ACTIVE"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_34(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status not in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_35(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("XXactiveXX", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_36(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("ACTIVE", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_37(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "XXtrialingXX")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_38(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "TRIALING")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_39(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("XXpast_dueXX" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_40(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("PAST_DUE" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_41(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status != "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_42(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "XXpast_dueXX" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_43(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "PAST_DUE" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_44(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "XXinactiveXX")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_45(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "INACTIVE")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_46(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = None
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_47(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "XXbasicXX"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_48(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "BASIC"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_49(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = None
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_50(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get(None, [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_51(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", None)
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_52(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get([])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_53(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", )
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_54(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get(None, {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_55(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", None).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_56(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get({}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_57(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", ).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_58(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("XXitemsXX", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_59(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("ITEMS", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_60(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("XXdataXX", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_61(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("DATA", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_62(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = None
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_63(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get(None, "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_64(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", None)
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_65(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_66(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", )
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_67(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get(None, {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_68(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", None).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_69(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get({}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_70(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", ).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_71(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[1].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_72(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("XXpriceXX", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_73(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("PRICE", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_74(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("XXidXX", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_75(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("ID", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_76(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "XXXX")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_77(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id != s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_78(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = None

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_79(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "XXproXX"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_80(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "PRO"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_81(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = None
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_82(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = True
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_83(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status == mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_84(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        None
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_85(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = None
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_86(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = None

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_87(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = False

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_88(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status != "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_89(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "XXactiveXX":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_90(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "ACTIVE":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_91(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = None
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_92(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status != "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_93(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "XXpast_dueXX":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_94(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "PAST_DUE":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_95(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = None  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_96(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = None

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_97(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "XXfreeXX"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_98(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "FREE"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_99(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier == expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_100(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        None
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_101(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = None
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_102(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = None

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_103(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = False

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_104(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = None
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_105(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get(None)
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_106(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("XXcurrent_period_endXX")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_107(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("CURRENT_PERIOD_END")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_108(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = None
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_109(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(None, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_110(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=None).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_111(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_112(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, ).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_113(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end and abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_114(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_115(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs(None) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_116(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end + expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_117(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) >= 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_118(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 61
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_119(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = None
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_120(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = None

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_121(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = False

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_122(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(None)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_123(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count = 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_124(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count -= 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_125(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 2
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_126(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(None)

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_127(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "XXNo such subscriptionXX" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_128(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "no such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_129(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "NO SUCH SUBSCRIPTION" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_130(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" not in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_131(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(None):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_132(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        None
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_133(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = None
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_134(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "XXfreeXX"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_135(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "FREE"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_136(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = None
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_137(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "XXcanceledXX"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_138(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "CANCELED"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_139(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = ""
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_140(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(None)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_141(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count = 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_142(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count -= 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_143(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 2
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_144(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(None)

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_145(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = None
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_146(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(None, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_147(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=None)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_148(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_149(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, )
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_150(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = None

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_151(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get(None, [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_152(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", None)

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_153(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get([])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_154(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", )

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_155(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("XXdataXX", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_156(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("DATA", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_157(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = ""
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_158(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get(None) in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_159(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("XXstatusXX") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_160(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("STATUS") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_161(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") not in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_162(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("XXactiveXX", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_163(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("ACTIVE", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_164(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "XXtrialingXX", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_165(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "TRIALING", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_166(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "XXpast_dueXX"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_167(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "PAST_DUE"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_168(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = None
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_169(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        return

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_170(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = None
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_171(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "XXactiveXX" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_172(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "ACTIVE" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_173(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get(None) in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_174(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("XXstatusXX") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_175(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("STATUS") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_176(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") not in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_177(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("XXactiveXX", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_178(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("ACTIVE", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_179(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "XXtrialingXX") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_180(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "TRIALING") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_181(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "XXpast_dueXX"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_182(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "PAST_DUE"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_183(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        None
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_184(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get(None)} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_185(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('XXidXX')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_186(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('ID')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_187(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = None
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_188(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get(None)
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_189(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("XXidXX")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_190(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("ID")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_191(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = None

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_192(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = None
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_193(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "XXbasicXX"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_194(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "BASIC"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_195(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = None
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_196(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get(None, "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_197(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", None)
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_198(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_199(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", )
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_200(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get(None, {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_201(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", None).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_202(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get({}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_203(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", ).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_204(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get(None, [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_205(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", None)[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_206(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get([{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_207(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", )[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_208(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get(None, {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_209(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", None).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_210(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get({}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_211(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", ).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_212(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("XXitemsXX", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_213(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("ITEMS", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_214(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("XXdataXX", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_215(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("DATA", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_216(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[1].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_217(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("XXpriceXX", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_218(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("PRICE", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_219(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("XXidXX", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_220(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("ID", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_221(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "XXXX")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_222(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id != s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_223(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = None

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_224(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "XXproXX"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_225(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "PRO"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_226(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status != "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_227(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "XXactiveXX":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_228(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "ACTIVE":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_229(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = None
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_230(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = None

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_231(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier == "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_232(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "XXfreeXX" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_233(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "FREE" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_234(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = None
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_235(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get(None)
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_236(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("XXcurrent_period_endXX")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_237(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("CURRENT_PERIOD_END")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_238(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = None

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_239(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(None, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_240(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=None).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_241(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_242(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, ).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_243(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(None)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_244(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count = 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_245(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count -= 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_246(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 2
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_247(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" or user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_248(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier == "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_249(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "XXfreeXX" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_250(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "FREE" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_251(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status not in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_252(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("XXactiveXX", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_253(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("ACTIVE", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_254(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "XXpast_dueXX"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_255(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "PAST_DUE"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_256(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        None
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_257(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = None
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_258(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "XXfreeXX"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_259(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "FREE"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_260(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_261(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "XXcanceledXX"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_262(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "CANCELED"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_263(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(None)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_264(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count = 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_265(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count -= 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_266(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 2
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_267(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(None)

    if reconciled_count > 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_268(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count >= 0:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_269(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 1:
        await db.commit()
    log.info(f"[reconcile] Completed. Reconciled {reconciled_count} users.")


async def x_reconcile_stripe_subscriptions__mutmut_270(db):
    """
    Nightly Stripe entitlement reconciliation job (TSYS-2a).
    Compares local database subscription state against Stripe.
    """
    s = get_settings()
    if not s.stripe_secret_key:
        log.warning("[reconcile] Stripe is not configured (missing STRIPE_SECRET_KEY)")
        return

    stripe.api_key = s.stripe_secret_key.get_secret_value()

    # Query all users connected to Stripe
    stmt = select(User).where((User.stripe_customer_id.isnot(None)) | (User.stripe_subscription_id.isnot(None)))
    users = (await db.execute(stmt)).scalars().all()
    log.info(f"[reconcile] Starting Stripe reconciliation for {len(users)} users")

    reconciled_count = 0

    for user in users:
        # Case A: User has a specific subscription ID recorded
        if user.stripe_subscription_id:
            try:
                sub = await asyncio.to_thread(stripe.Subscription.retrieve, user.stripe_subscription_id)

                stripe_status = sub.get("status", "inactive")
                mapped_status = (
                    "active"
                    if stripe_status in ("active", "trialing")
                    else ("past_due" if stripe_status == "past_due" else "inactive")
                )

                tier = "basic"
                items = sub.get("items", {}).get("data", [])
                if items:
                    price_id = items[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                mismatch = False
                if user.subscription_status != mapped_status:
                    log.info(
                        f"[reconcile] Status mismatch for user {user.id}: DB={user.subscription_status}, Stripe={mapped_status}"
                    )
                    user.subscription_status = mapped_status
                    mismatch = True

                if mapped_status == "active":
                    expected_tier = tier
                elif mapped_status == "past_due":
                    expected_tier = user.subscription_tier  # Keep current tier during grace period
                else:
                    expected_tier = "free"

                if user.subscription_tier != expected_tier:
                    log.info(
                        f"[reconcile] Tier mismatch for user {user.id}: DB={user.subscription_tier}, Stripe_expected={expected_tier}"
                    )
                    user.subscription_tier = expected_tier
                    mismatch = True

                period_end = sub.get("current_period_end")
                if period_end:
                    expected_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(tzinfo=None)
                    if (
                        not user.subscription_period_end
                        or abs((user.subscription_period_end - expected_period_end).total_seconds()) > 60
                    ):
                        user.subscription_period_end = expected_period_end
                        mismatch = True

                if mismatch:
                    db.add(user)
                    reconciled_count += 1
                    log.info(f"[reconcile] Corrected subscription details for user={user.id}")

            except stripe.error.InvalidRequestError as e:
                # If subscription does not exist on Stripe, downgrade to free
                if "No such subscription" in str(e):
                    log.warning(
                        f"[reconcile] Stripe subscription {user.stripe_subscription_id} not found for user {user.id}. Downgrading to free."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    user.stripe_subscription_id = None
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to reconcile user {user.id} subscription: {e}")

        # Case B: User has a customer ID but no subscription ID recorded in DB
        elif user.stripe_customer_id:
            try:
                subs = await asyncio.to_thread(stripe.Subscription.list, customer=user.stripe_customer_id)
                stripe_subs = subs.get("data", [])

                # Look for first active or past_due subscription
                valid_sub = None
                for sub in stripe_subs:
                    if sub.get("status") in ("active", "trialing", "past_due"):
                        valid_sub = sub
                        break

                if valid_sub:
                    sub_status = "active" if valid_sub.get("status") in ("active", "trialing") else "past_due"
                    log.info(
                        f"[reconcile] Found active/past_due Stripe subscription {valid_sub.get('id')} for user {user.id} missing in DB."
                    )
                    user.stripe_subscription_id = valid_sub.get("id")
                    user.subscription_status = sub_status

                    tier = "basic"
                    price_id = valid_sub.get("items", {}).get("data", [{}])[0].get("price", {}).get("id", "")
                    if price_id == s.stripe_price_pro:
                        tier = "pro"

                    if sub_status == "active":
                        user.subscription_tier = tier
                    else:
                        user.subscription_tier = user.subscription_tier if user.subscription_tier != "free" else tier

                    period_end = valid_sub.get("current_period_end")
                    if period_end:
                        user.subscription_period_end = datetime.fromtimestamp(period_end, tz=_tz.utc).replace(
                            tzinfo=None
                        )

                    db.add(user)
                    reconciled_count += 1
                elif user.subscription_tier != "free" and user.subscription_status in ("active", "past_due"):
                    log.info(
                        f"[reconcile] DB says active/past_due for user {user.id} but Stripe has no active subscriptions. Downgrading."
                    )
                    user.subscription_tier = "free"
                    user.subscription_status = "canceled"
                    db.add(user)
                    reconciled_count += 1
            except Exception as e:
                log.warning(f"[reconcile] Failed to check customer subscriptions for user {user.id}: {e}")

    if reconciled_count > 0:
        await db.commit()
    log.info(None)

mutants_x_reconcile_stripe_subscriptions__mutmut['_mutmut_orig'] = x_reconcile_stripe_subscriptions__mutmut_orig # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_1'] = x_reconcile_stripe_subscriptions__mutmut_1 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_2'] = x_reconcile_stripe_subscriptions__mutmut_2 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_3'] = x_reconcile_stripe_subscriptions__mutmut_3 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_4'] = x_reconcile_stripe_subscriptions__mutmut_4 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_5'] = x_reconcile_stripe_subscriptions__mutmut_5 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_6'] = x_reconcile_stripe_subscriptions__mutmut_6 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_7'] = x_reconcile_stripe_subscriptions__mutmut_7 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_8'] = x_reconcile_stripe_subscriptions__mutmut_8 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_9'] = x_reconcile_stripe_subscriptions__mutmut_9 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_10'] = x_reconcile_stripe_subscriptions__mutmut_10 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_11'] = x_reconcile_stripe_subscriptions__mutmut_11 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_12'] = x_reconcile_stripe_subscriptions__mutmut_12 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_13'] = x_reconcile_stripe_subscriptions__mutmut_13 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_14'] = x_reconcile_stripe_subscriptions__mutmut_14 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_15'] = x_reconcile_stripe_subscriptions__mutmut_15 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_16'] = x_reconcile_stripe_subscriptions__mutmut_16 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_17'] = x_reconcile_stripe_subscriptions__mutmut_17 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_18'] = x_reconcile_stripe_subscriptions__mutmut_18 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_19'] = x_reconcile_stripe_subscriptions__mutmut_19 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_20'] = x_reconcile_stripe_subscriptions__mutmut_20 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_21'] = x_reconcile_stripe_subscriptions__mutmut_21 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_22'] = x_reconcile_stripe_subscriptions__mutmut_22 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_23'] = x_reconcile_stripe_subscriptions__mutmut_23 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_24'] = x_reconcile_stripe_subscriptions__mutmut_24 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_25'] = x_reconcile_stripe_subscriptions__mutmut_25 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_26'] = x_reconcile_stripe_subscriptions__mutmut_26 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_27'] = x_reconcile_stripe_subscriptions__mutmut_27 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_28'] = x_reconcile_stripe_subscriptions__mutmut_28 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_29'] = x_reconcile_stripe_subscriptions__mutmut_29 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_30'] = x_reconcile_stripe_subscriptions__mutmut_30 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_31'] = x_reconcile_stripe_subscriptions__mutmut_31 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_32'] = x_reconcile_stripe_subscriptions__mutmut_32 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_33'] = x_reconcile_stripe_subscriptions__mutmut_33 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_34'] = x_reconcile_stripe_subscriptions__mutmut_34 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_35'] = x_reconcile_stripe_subscriptions__mutmut_35 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_36'] = x_reconcile_stripe_subscriptions__mutmut_36 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_37'] = x_reconcile_stripe_subscriptions__mutmut_37 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_38'] = x_reconcile_stripe_subscriptions__mutmut_38 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_39'] = x_reconcile_stripe_subscriptions__mutmut_39 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_40'] = x_reconcile_stripe_subscriptions__mutmut_40 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_41'] = x_reconcile_stripe_subscriptions__mutmut_41 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_42'] = x_reconcile_stripe_subscriptions__mutmut_42 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_43'] = x_reconcile_stripe_subscriptions__mutmut_43 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_44'] = x_reconcile_stripe_subscriptions__mutmut_44 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_45'] = x_reconcile_stripe_subscriptions__mutmut_45 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_46'] = x_reconcile_stripe_subscriptions__mutmut_46 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_47'] = x_reconcile_stripe_subscriptions__mutmut_47 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_48'] = x_reconcile_stripe_subscriptions__mutmut_48 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_49'] = x_reconcile_stripe_subscriptions__mutmut_49 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_50'] = x_reconcile_stripe_subscriptions__mutmut_50 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_51'] = x_reconcile_stripe_subscriptions__mutmut_51 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_52'] = x_reconcile_stripe_subscriptions__mutmut_52 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_53'] = x_reconcile_stripe_subscriptions__mutmut_53 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_54'] = x_reconcile_stripe_subscriptions__mutmut_54 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_55'] = x_reconcile_stripe_subscriptions__mutmut_55 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_56'] = x_reconcile_stripe_subscriptions__mutmut_56 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_57'] = x_reconcile_stripe_subscriptions__mutmut_57 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_58'] = x_reconcile_stripe_subscriptions__mutmut_58 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_59'] = x_reconcile_stripe_subscriptions__mutmut_59 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_60'] = x_reconcile_stripe_subscriptions__mutmut_60 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_61'] = x_reconcile_stripe_subscriptions__mutmut_61 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_62'] = x_reconcile_stripe_subscriptions__mutmut_62 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_63'] = x_reconcile_stripe_subscriptions__mutmut_63 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_64'] = x_reconcile_stripe_subscriptions__mutmut_64 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_65'] = x_reconcile_stripe_subscriptions__mutmut_65 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_66'] = x_reconcile_stripe_subscriptions__mutmut_66 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_67'] = x_reconcile_stripe_subscriptions__mutmut_67 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_68'] = x_reconcile_stripe_subscriptions__mutmut_68 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_69'] = x_reconcile_stripe_subscriptions__mutmut_69 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_70'] = x_reconcile_stripe_subscriptions__mutmut_70 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_71'] = x_reconcile_stripe_subscriptions__mutmut_71 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_72'] = x_reconcile_stripe_subscriptions__mutmut_72 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_73'] = x_reconcile_stripe_subscriptions__mutmut_73 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_74'] = x_reconcile_stripe_subscriptions__mutmut_74 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_75'] = x_reconcile_stripe_subscriptions__mutmut_75 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_76'] = x_reconcile_stripe_subscriptions__mutmut_76 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_77'] = x_reconcile_stripe_subscriptions__mutmut_77 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_78'] = x_reconcile_stripe_subscriptions__mutmut_78 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_79'] = x_reconcile_stripe_subscriptions__mutmut_79 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_80'] = x_reconcile_stripe_subscriptions__mutmut_80 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_81'] = x_reconcile_stripe_subscriptions__mutmut_81 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_82'] = x_reconcile_stripe_subscriptions__mutmut_82 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_83'] = x_reconcile_stripe_subscriptions__mutmut_83 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_84'] = x_reconcile_stripe_subscriptions__mutmut_84 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_85'] = x_reconcile_stripe_subscriptions__mutmut_85 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_86'] = x_reconcile_stripe_subscriptions__mutmut_86 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_87'] = x_reconcile_stripe_subscriptions__mutmut_87 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_88'] = x_reconcile_stripe_subscriptions__mutmut_88 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_89'] = x_reconcile_stripe_subscriptions__mutmut_89 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_90'] = x_reconcile_stripe_subscriptions__mutmut_90 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_91'] = x_reconcile_stripe_subscriptions__mutmut_91 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_92'] = x_reconcile_stripe_subscriptions__mutmut_92 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_93'] = x_reconcile_stripe_subscriptions__mutmut_93 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_94'] = x_reconcile_stripe_subscriptions__mutmut_94 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_95'] = x_reconcile_stripe_subscriptions__mutmut_95 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_96'] = x_reconcile_stripe_subscriptions__mutmut_96 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_97'] = x_reconcile_stripe_subscriptions__mutmut_97 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_98'] = x_reconcile_stripe_subscriptions__mutmut_98 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_99'] = x_reconcile_stripe_subscriptions__mutmut_99 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_100'] = x_reconcile_stripe_subscriptions__mutmut_100 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_101'] = x_reconcile_stripe_subscriptions__mutmut_101 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_102'] = x_reconcile_stripe_subscriptions__mutmut_102 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_103'] = x_reconcile_stripe_subscriptions__mutmut_103 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_104'] = x_reconcile_stripe_subscriptions__mutmut_104 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_105'] = x_reconcile_stripe_subscriptions__mutmut_105 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_106'] = x_reconcile_stripe_subscriptions__mutmut_106 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_107'] = x_reconcile_stripe_subscriptions__mutmut_107 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_108'] = x_reconcile_stripe_subscriptions__mutmut_108 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_109'] = x_reconcile_stripe_subscriptions__mutmut_109 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_110'] = x_reconcile_stripe_subscriptions__mutmut_110 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_111'] = x_reconcile_stripe_subscriptions__mutmut_111 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_112'] = x_reconcile_stripe_subscriptions__mutmut_112 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_113'] = x_reconcile_stripe_subscriptions__mutmut_113 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_114'] = x_reconcile_stripe_subscriptions__mutmut_114 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_115'] = x_reconcile_stripe_subscriptions__mutmut_115 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_116'] = x_reconcile_stripe_subscriptions__mutmut_116 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_117'] = x_reconcile_stripe_subscriptions__mutmut_117 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_118'] = x_reconcile_stripe_subscriptions__mutmut_118 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_119'] = x_reconcile_stripe_subscriptions__mutmut_119 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_120'] = x_reconcile_stripe_subscriptions__mutmut_120 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_121'] = x_reconcile_stripe_subscriptions__mutmut_121 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_122'] = x_reconcile_stripe_subscriptions__mutmut_122 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_123'] = x_reconcile_stripe_subscriptions__mutmut_123 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_124'] = x_reconcile_stripe_subscriptions__mutmut_124 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_125'] = x_reconcile_stripe_subscriptions__mutmut_125 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_126'] = x_reconcile_stripe_subscriptions__mutmut_126 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_127'] = x_reconcile_stripe_subscriptions__mutmut_127 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_128'] = x_reconcile_stripe_subscriptions__mutmut_128 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_129'] = x_reconcile_stripe_subscriptions__mutmut_129 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_130'] = x_reconcile_stripe_subscriptions__mutmut_130 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_131'] = x_reconcile_stripe_subscriptions__mutmut_131 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_132'] = x_reconcile_stripe_subscriptions__mutmut_132 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_133'] = x_reconcile_stripe_subscriptions__mutmut_133 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_134'] = x_reconcile_stripe_subscriptions__mutmut_134 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_135'] = x_reconcile_stripe_subscriptions__mutmut_135 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_136'] = x_reconcile_stripe_subscriptions__mutmut_136 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_137'] = x_reconcile_stripe_subscriptions__mutmut_137 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_138'] = x_reconcile_stripe_subscriptions__mutmut_138 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_139'] = x_reconcile_stripe_subscriptions__mutmut_139 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_140'] = x_reconcile_stripe_subscriptions__mutmut_140 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_141'] = x_reconcile_stripe_subscriptions__mutmut_141 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_142'] = x_reconcile_stripe_subscriptions__mutmut_142 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_143'] = x_reconcile_stripe_subscriptions__mutmut_143 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_144'] = x_reconcile_stripe_subscriptions__mutmut_144 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_145'] = x_reconcile_stripe_subscriptions__mutmut_145 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_146'] = x_reconcile_stripe_subscriptions__mutmut_146 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_147'] = x_reconcile_stripe_subscriptions__mutmut_147 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_148'] = x_reconcile_stripe_subscriptions__mutmut_148 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_149'] = x_reconcile_stripe_subscriptions__mutmut_149 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_150'] = x_reconcile_stripe_subscriptions__mutmut_150 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_151'] = x_reconcile_stripe_subscriptions__mutmut_151 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_152'] = x_reconcile_stripe_subscriptions__mutmut_152 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_153'] = x_reconcile_stripe_subscriptions__mutmut_153 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_154'] = x_reconcile_stripe_subscriptions__mutmut_154 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_155'] = x_reconcile_stripe_subscriptions__mutmut_155 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_156'] = x_reconcile_stripe_subscriptions__mutmut_156 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_157'] = x_reconcile_stripe_subscriptions__mutmut_157 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_158'] = x_reconcile_stripe_subscriptions__mutmut_158 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_159'] = x_reconcile_stripe_subscriptions__mutmut_159 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_160'] = x_reconcile_stripe_subscriptions__mutmut_160 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_161'] = x_reconcile_stripe_subscriptions__mutmut_161 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_162'] = x_reconcile_stripe_subscriptions__mutmut_162 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_163'] = x_reconcile_stripe_subscriptions__mutmut_163 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_164'] = x_reconcile_stripe_subscriptions__mutmut_164 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_165'] = x_reconcile_stripe_subscriptions__mutmut_165 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_166'] = x_reconcile_stripe_subscriptions__mutmut_166 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_167'] = x_reconcile_stripe_subscriptions__mutmut_167 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_168'] = x_reconcile_stripe_subscriptions__mutmut_168 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_169'] = x_reconcile_stripe_subscriptions__mutmut_169 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_170'] = x_reconcile_stripe_subscriptions__mutmut_170 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_171'] = x_reconcile_stripe_subscriptions__mutmut_171 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_172'] = x_reconcile_stripe_subscriptions__mutmut_172 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_173'] = x_reconcile_stripe_subscriptions__mutmut_173 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_174'] = x_reconcile_stripe_subscriptions__mutmut_174 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_175'] = x_reconcile_stripe_subscriptions__mutmut_175 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_176'] = x_reconcile_stripe_subscriptions__mutmut_176 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_177'] = x_reconcile_stripe_subscriptions__mutmut_177 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_178'] = x_reconcile_stripe_subscriptions__mutmut_178 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_179'] = x_reconcile_stripe_subscriptions__mutmut_179 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_180'] = x_reconcile_stripe_subscriptions__mutmut_180 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_181'] = x_reconcile_stripe_subscriptions__mutmut_181 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_182'] = x_reconcile_stripe_subscriptions__mutmut_182 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_183'] = x_reconcile_stripe_subscriptions__mutmut_183 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_184'] = x_reconcile_stripe_subscriptions__mutmut_184 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_185'] = x_reconcile_stripe_subscriptions__mutmut_185 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_186'] = x_reconcile_stripe_subscriptions__mutmut_186 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_187'] = x_reconcile_stripe_subscriptions__mutmut_187 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_188'] = x_reconcile_stripe_subscriptions__mutmut_188 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_189'] = x_reconcile_stripe_subscriptions__mutmut_189 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_190'] = x_reconcile_stripe_subscriptions__mutmut_190 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_191'] = x_reconcile_stripe_subscriptions__mutmut_191 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_192'] = x_reconcile_stripe_subscriptions__mutmut_192 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_193'] = x_reconcile_stripe_subscriptions__mutmut_193 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_194'] = x_reconcile_stripe_subscriptions__mutmut_194 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_195'] = x_reconcile_stripe_subscriptions__mutmut_195 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_196'] = x_reconcile_stripe_subscriptions__mutmut_196 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_197'] = x_reconcile_stripe_subscriptions__mutmut_197 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_198'] = x_reconcile_stripe_subscriptions__mutmut_198 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_199'] = x_reconcile_stripe_subscriptions__mutmut_199 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_200'] = x_reconcile_stripe_subscriptions__mutmut_200 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_201'] = x_reconcile_stripe_subscriptions__mutmut_201 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_202'] = x_reconcile_stripe_subscriptions__mutmut_202 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_203'] = x_reconcile_stripe_subscriptions__mutmut_203 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_204'] = x_reconcile_stripe_subscriptions__mutmut_204 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_205'] = x_reconcile_stripe_subscriptions__mutmut_205 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_206'] = x_reconcile_stripe_subscriptions__mutmut_206 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_207'] = x_reconcile_stripe_subscriptions__mutmut_207 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_208'] = x_reconcile_stripe_subscriptions__mutmut_208 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_209'] = x_reconcile_stripe_subscriptions__mutmut_209 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_210'] = x_reconcile_stripe_subscriptions__mutmut_210 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_211'] = x_reconcile_stripe_subscriptions__mutmut_211 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_212'] = x_reconcile_stripe_subscriptions__mutmut_212 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_213'] = x_reconcile_stripe_subscriptions__mutmut_213 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_214'] = x_reconcile_stripe_subscriptions__mutmut_214 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_215'] = x_reconcile_stripe_subscriptions__mutmut_215 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_216'] = x_reconcile_stripe_subscriptions__mutmut_216 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_217'] = x_reconcile_stripe_subscriptions__mutmut_217 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_218'] = x_reconcile_stripe_subscriptions__mutmut_218 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_219'] = x_reconcile_stripe_subscriptions__mutmut_219 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_220'] = x_reconcile_stripe_subscriptions__mutmut_220 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_221'] = x_reconcile_stripe_subscriptions__mutmut_221 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_222'] = x_reconcile_stripe_subscriptions__mutmut_222 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_223'] = x_reconcile_stripe_subscriptions__mutmut_223 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_224'] = x_reconcile_stripe_subscriptions__mutmut_224 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_225'] = x_reconcile_stripe_subscriptions__mutmut_225 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_226'] = x_reconcile_stripe_subscriptions__mutmut_226 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_227'] = x_reconcile_stripe_subscriptions__mutmut_227 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_228'] = x_reconcile_stripe_subscriptions__mutmut_228 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_229'] = x_reconcile_stripe_subscriptions__mutmut_229 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_230'] = x_reconcile_stripe_subscriptions__mutmut_230 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_231'] = x_reconcile_stripe_subscriptions__mutmut_231 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_232'] = x_reconcile_stripe_subscriptions__mutmut_232 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_233'] = x_reconcile_stripe_subscriptions__mutmut_233 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_234'] = x_reconcile_stripe_subscriptions__mutmut_234 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_235'] = x_reconcile_stripe_subscriptions__mutmut_235 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_236'] = x_reconcile_stripe_subscriptions__mutmut_236 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_237'] = x_reconcile_stripe_subscriptions__mutmut_237 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_238'] = x_reconcile_stripe_subscriptions__mutmut_238 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_239'] = x_reconcile_stripe_subscriptions__mutmut_239 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_240'] = x_reconcile_stripe_subscriptions__mutmut_240 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_241'] = x_reconcile_stripe_subscriptions__mutmut_241 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_242'] = x_reconcile_stripe_subscriptions__mutmut_242 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_243'] = x_reconcile_stripe_subscriptions__mutmut_243 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_244'] = x_reconcile_stripe_subscriptions__mutmut_244 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_245'] = x_reconcile_stripe_subscriptions__mutmut_245 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_246'] = x_reconcile_stripe_subscriptions__mutmut_246 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_247'] = x_reconcile_stripe_subscriptions__mutmut_247 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_248'] = x_reconcile_stripe_subscriptions__mutmut_248 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_249'] = x_reconcile_stripe_subscriptions__mutmut_249 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_250'] = x_reconcile_stripe_subscriptions__mutmut_250 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_251'] = x_reconcile_stripe_subscriptions__mutmut_251 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_252'] = x_reconcile_stripe_subscriptions__mutmut_252 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_253'] = x_reconcile_stripe_subscriptions__mutmut_253 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_254'] = x_reconcile_stripe_subscriptions__mutmut_254 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_255'] = x_reconcile_stripe_subscriptions__mutmut_255 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_256'] = x_reconcile_stripe_subscriptions__mutmut_256 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_257'] = x_reconcile_stripe_subscriptions__mutmut_257 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_258'] = x_reconcile_stripe_subscriptions__mutmut_258 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_259'] = x_reconcile_stripe_subscriptions__mutmut_259 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_260'] = x_reconcile_stripe_subscriptions__mutmut_260 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_261'] = x_reconcile_stripe_subscriptions__mutmut_261 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_262'] = x_reconcile_stripe_subscriptions__mutmut_262 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_263'] = x_reconcile_stripe_subscriptions__mutmut_263 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_264'] = x_reconcile_stripe_subscriptions__mutmut_264 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_265'] = x_reconcile_stripe_subscriptions__mutmut_265 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_266'] = x_reconcile_stripe_subscriptions__mutmut_266 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_267'] = x_reconcile_stripe_subscriptions__mutmut_267 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_268'] = x_reconcile_stripe_subscriptions__mutmut_268 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_269'] = x_reconcile_stripe_subscriptions__mutmut_269 # type: ignore # mutmut generated
mutants_x_reconcile_stripe_subscriptions__mutmut['x_reconcile_stripe_subscriptions__mutmut_270'] = x_reconcile_stripe_subscriptions__mutmut_270 # type: ignore # mutmut generated
