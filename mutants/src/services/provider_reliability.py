import logging
from datetime import date
from sqlalchemy import select
from sqlalchemy.exc import IntegrityError
from database import AsyncSessionLocal
from models import ProviderResponseSample, ProviderHealthScorecard, CorporateActionValidation, IncidentTimeline

log = logging.getLogger("signal.trade.reliability")


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_record_endpoint_call__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_record_endpoint_call__mutmut)
async def record_endpoint_call(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_orig(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_1(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "XXXX",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_2(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = True,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_3(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = True,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_4(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = None

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_5(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted and (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_6(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) and is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_7(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code == 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_8(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 201) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_9(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() <= 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_10(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 1.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_11(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample or response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_12(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = None
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_13(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5001] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_14(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) >= 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_15(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5001 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_16(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = None
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_17(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=None,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_18(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=None,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_19(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=None,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_20(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=None,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_21(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=None,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_22(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=None,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_23(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=None,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_24(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=None,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_25(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_26(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_27(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_28(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_29(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_30(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_31(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_32(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_33(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(None)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_34(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = None
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_35(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                None, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_36(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, None
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_37(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_38(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_39(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(None).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_40(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider != provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_41(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint != endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_42(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = None
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_43(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(None)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_44(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = None

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_45(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = None
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_46(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 2 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_47(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code == 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_48(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 201 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_49(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 1
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_50(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = None
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_51(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 2 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_52(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 1
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_53(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = None

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_54(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 2 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_55(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 1

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_56(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_57(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = None
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_58(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=None,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_59(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=None,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_60(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=None,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_61(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=None,
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_62(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=None,
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_63(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=None,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_64(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=None,
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_65(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=None,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_66(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_67(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_68(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_69(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_70(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_71(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_72(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_73(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_74(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(None),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_75(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(None),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_76(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 + (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_77(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=101.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_78(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 - drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_79(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 - stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_80(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val / 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_81(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 21 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_82(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val / 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_83(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 11 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_84(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val / 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_85(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 31),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_86(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=False,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_87(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(None)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_88(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = None
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_89(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 1.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_90(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = None
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_91(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms - alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_92(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) / scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_93(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 + alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_94(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (2 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_95(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha / latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_96(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = None
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_97(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate - alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_98(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) / scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_99(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 + alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_100(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (2 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_101(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha / error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_102(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = None
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_103(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate - alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_104(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) / scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_105(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 + alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_106(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (2 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_107(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha / stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_108(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count = 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_109(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count -= 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_110(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 2

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_111(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = None  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_112(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(None, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_113(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, None)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_114(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max((scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_115(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, )  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_116(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(1.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_117(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) * 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_118(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms + 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_119(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 501.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_120(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 51.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_121(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = None
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_122(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate / 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_123(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 101.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_124(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = None
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_125(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate / 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_126(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 51.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_127(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = None

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_128(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(None, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_129(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, None)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_130(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_131(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, )

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_132(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(51.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_133(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count / 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_134(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 11.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_135(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = None
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_136(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    None, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_137(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, None
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_138(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_139(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_140(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    1.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_141(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 + (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_142(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 101.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_143(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty - latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_144(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty - drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_145(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty - stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_146(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 or scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_147(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score <= 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_148(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 31.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_149(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = None
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_150(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = True
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_151(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = None
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_152(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type=None,
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_153(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity=None,
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_154(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=None,
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_155(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details=None,
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_156(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_157(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_158(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_159(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_160(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="XXprovider_degradationXX",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_161(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="PROVIDER_DEGRADATION",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_162(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="XXcriticalXX",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_163(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="CRITICAL",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_164(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"XXproviderXX": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_165(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"PROVIDER": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_166(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "XXendpointXX": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_167(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "ENDPOINT": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_168(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "XXhealth_scoreXX": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_169(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "HEALTH_SCORE": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_170(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(None)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(f"Failed to record endpoint call: {e}")


async def x_record_endpoint_call__mutmut_171(
    provider: str,
    endpoint: str,
    latency_ms: float,
    status_code: int,
    response_body: str = "",
    ticker: str = None,
    is_stale: bool = False,
    is_drifted: bool = False,
    drift_details: str = None,
):
    """TSYS-5a & TSYS-5b: Record API call and update health scorecard, sampling response if needed."""
    try:
        # Sample raw response under certain conditions:
        # 1. Status code is error (not 200)
        # 2. Schema is drifted
        # 3. Random sampling (5% of successful requests)
        import random

        should_sample = (status_code != 200) or is_drifted or (random.random() < 0.05)

        async with AsyncSessionLocal() as db:
            if should_sample and response_body:
                # Truncate response body if it's too long
                body_to_store = response_body[:5000] if len(response_body) > 5000 else response_body
                sample = ProviderResponseSample(
                    provider=provider,
                    endpoint=endpoint,
                    ticker=ticker,
                    status_code=status_code,
                    latency_ms=latency_ms,
                    response_body=body_to_store,
                    is_drifted=is_drifted,
                    drift_details=drift_details,
                )
                db.add(sample)

            # Update Health Scorecard
            stmt = select(ProviderHealthScorecard).where(
                ProviderHealthScorecard.provider == provider, ProviderHealthScorecard.endpoint == endpoint
            )
            res = await db.execute(stmt)
            # .first() (not scalar_one_or_none) so any pre-existing duplicate
            # (provider, endpoint) rows can't crash this hot path. A unique
            # constraint (uq_provider_endpoint) now prevents new duplicates.
            scorecard = res.scalars().first()

            error_val = 1 if status_code != 200 else 0
            stale_val = 1 if is_stale else 0
            drift_val = 1 if is_drifted else 0

            if not scorecard:
                scorecard = ProviderHealthScorecard(
                    provider=provider,
                    endpoint=endpoint,
                    latency_avg_ms=latency_ms,
                    error_rate=float(error_val),
                    stale_data_rate=float(stale_val),
                    schema_drift_count=drift_val,
                    health_score=100.0 - (error_val * 20 + stale_val * 10 + drift_val * 30),
                    is_active=True,
                )
                db.add(scorecard)
            else:
                # Exponential moving average for metrics
                alpha = 0.1
                scorecard.latency_avg_ms = (1 - alpha) * scorecard.latency_avg_ms + alpha * latency_ms
                scorecard.error_rate = (1 - alpha) * scorecard.error_rate + alpha * error_val
                scorecard.stale_data_rate = (1 - alpha) * scorecard.stale_data_rate + alpha * stale_val
                if is_drifted:
                    scorecard.schema_drift_count += 1

                # Calculate health score (0-100)
                # Penalize errors, latency (if > 1000ms), stale data, and drift incidents
                latency_penalty = max(0.0, (scorecard.latency_avg_ms - 500.0) / 50.0)  # penalize above 500ms
                error_penalty = scorecard.error_rate * 100.0
                stale_penalty = scorecard.stale_data_rate * 50.0
                drift_penalty = min(50.0, scorecard.schema_drift_count * 10.0)

                scorecard.health_score = max(
                    0.0, 100.0 - (error_penalty + stale_penalty + drift_penalty + latency_penalty)
                )
                # Auto degradation/deactivation if score drops too low
                if scorecard.health_score < 30.0 and scorecard.is_active:
                    scorecard.is_active = False
                    # Log incident timeline (TSYS-10a)
                    incident = IncidentTimeline(
                        event_type="provider_degradation",
                        severity="critical",
                        message=f"Provider {provider} endpoint {endpoint} health score dropped to {scorecard.health_score:.1f}. Endpoint marked inactive.",
                        details={"provider": provider, "endpoint": endpoint, "health_score": scorecard.health_score},
                    )
                    db.add(incident)

            try:
                await db.commit()
            except IntegrityError:
                # Concurrent first-write for this (provider, endpoint) won the
                # uq_provider_endpoint race. The other writer's row stands; the
                # next call updates it via EMA. Nothing to recover here.
                await db.rollback()
    except Exception as e:
        log.error(None)

mutants_x_record_endpoint_call__mutmut['_mutmut_orig'] = x_record_endpoint_call__mutmut_orig # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_1'] = x_record_endpoint_call__mutmut_1 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_2'] = x_record_endpoint_call__mutmut_2 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_3'] = x_record_endpoint_call__mutmut_3 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_4'] = x_record_endpoint_call__mutmut_4 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_5'] = x_record_endpoint_call__mutmut_5 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_6'] = x_record_endpoint_call__mutmut_6 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_7'] = x_record_endpoint_call__mutmut_7 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_8'] = x_record_endpoint_call__mutmut_8 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_9'] = x_record_endpoint_call__mutmut_9 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_10'] = x_record_endpoint_call__mutmut_10 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_11'] = x_record_endpoint_call__mutmut_11 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_12'] = x_record_endpoint_call__mutmut_12 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_13'] = x_record_endpoint_call__mutmut_13 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_14'] = x_record_endpoint_call__mutmut_14 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_15'] = x_record_endpoint_call__mutmut_15 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_16'] = x_record_endpoint_call__mutmut_16 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_17'] = x_record_endpoint_call__mutmut_17 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_18'] = x_record_endpoint_call__mutmut_18 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_19'] = x_record_endpoint_call__mutmut_19 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_20'] = x_record_endpoint_call__mutmut_20 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_21'] = x_record_endpoint_call__mutmut_21 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_22'] = x_record_endpoint_call__mutmut_22 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_23'] = x_record_endpoint_call__mutmut_23 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_24'] = x_record_endpoint_call__mutmut_24 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_25'] = x_record_endpoint_call__mutmut_25 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_26'] = x_record_endpoint_call__mutmut_26 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_27'] = x_record_endpoint_call__mutmut_27 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_28'] = x_record_endpoint_call__mutmut_28 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_29'] = x_record_endpoint_call__mutmut_29 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_30'] = x_record_endpoint_call__mutmut_30 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_31'] = x_record_endpoint_call__mutmut_31 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_32'] = x_record_endpoint_call__mutmut_32 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_33'] = x_record_endpoint_call__mutmut_33 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_34'] = x_record_endpoint_call__mutmut_34 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_35'] = x_record_endpoint_call__mutmut_35 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_36'] = x_record_endpoint_call__mutmut_36 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_37'] = x_record_endpoint_call__mutmut_37 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_38'] = x_record_endpoint_call__mutmut_38 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_39'] = x_record_endpoint_call__mutmut_39 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_40'] = x_record_endpoint_call__mutmut_40 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_41'] = x_record_endpoint_call__mutmut_41 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_42'] = x_record_endpoint_call__mutmut_42 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_43'] = x_record_endpoint_call__mutmut_43 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_44'] = x_record_endpoint_call__mutmut_44 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_45'] = x_record_endpoint_call__mutmut_45 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_46'] = x_record_endpoint_call__mutmut_46 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_47'] = x_record_endpoint_call__mutmut_47 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_48'] = x_record_endpoint_call__mutmut_48 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_49'] = x_record_endpoint_call__mutmut_49 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_50'] = x_record_endpoint_call__mutmut_50 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_51'] = x_record_endpoint_call__mutmut_51 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_52'] = x_record_endpoint_call__mutmut_52 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_53'] = x_record_endpoint_call__mutmut_53 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_54'] = x_record_endpoint_call__mutmut_54 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_55'] = x_record_endpoint_call__mutmut_55 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_56'] = x_record_endpoint_call__mutmut_56 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_57'] = x_record_endpoint_call__mutmut_57 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_58'] = x_record_endpoint_call__mutmut_58 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_59'] = x_record_endpoint_call__mutmut_59 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_60'] = x_record_endpoint_call__mutmut_60 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_61'] = x_record_endpoint_call__mutmut_61 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_62'] = x_record_endpoint_call__mutmut_62 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_63'] = x_record_endpoint_call__mutmut_63 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_64'] = x_record_endpoint_call__mutmut_64 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_65'] = x_record_endpoint_call__mutmut_65 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_66'] = x_record_endpoint_call__mutmut_66 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_67'] = x_record_endpoint_call__mutmut_67 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_68'] = x_record_endpoint_call__mutmut_68 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_69'] = x_record_endpoint_call__mutmut_69 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_70'] = x_record_endpoint_call__mutmut_70 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_71'] = x_record_endpoint_call__mutmut_71 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_72'] = x_record_endpoint_call__mutmut_72 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_73'] = x_record_endpoint_call__mutmut_73 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_74'] = x_record_endpoint_call__mutmut_74 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_75'] = x_record_endpoint_call__mutmut_75 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_76'] = x_record_endpoint_call__mutmut_76 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_77'] = x_record_endpoint_call__mutmut_77 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_78'] = x_record_endpoint_call__mutmut_78 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_79'] = x_record_endpoint_call__mutmut_79 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_80'] = x_record_endpoint_call__mutmut_80 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_81'] = x_record_endpoint_call__mutmut_81 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_82'] = x_record_endpoint_call__mutmut_82 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_83'] = x_record_endpoint_call__mutmut_83 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_84'] = x_record_endpoint_call__mutmut_84 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_85'] = x_record_endpoint_call__mutmut_85 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_86'] = x_record_endpoint_call__mutmut_86 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_87'] = x_record_endpoint_call__mutmut_87 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_88'] = x_record_endpoint_call__mutmut_88 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_89'] = x_record_endpoint_call__mutmut_89 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_90'] = x_record_endpoint_call__mutmut_90 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_91'] = x_record_endpoint_call__mutmut_91 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_92'] = x_record_endpoint_call__mutmut_92 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_93'] = x_record_endpoint_call__mutmut_93 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_94'] = x_record_endpoint_call__mutmut_94 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_95'] = x_record_endpoint_call__mutmut_95 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_96'] = x_record_endpoint_call__mutmut_96 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_97'] = x_record_endpoint_call__mutmut_97 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_98'] = x_record_endpoint_call__mutmut_98 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_99'] = x_record_endpoint_call__mutmut_99 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_100'] = x_record_endpoint_call__mutmut_100 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_101'] = x_record_endpoint_call__mutmut_101 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_102'] = x_record_endpoint_call__mutmut_102 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_103'] = x_record_endpoint_call__mutmut_103 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_104'] = x_record_endpoint_call__mutmut_104 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_105'] = x_record_endpoint_call__mutmut_105 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_106'] = x_record_endpoint_call__mutmut_106 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_107'] = x_record_endpoint_call__mutmut_107 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_108'] = x_record_endpoint_call__mutmut_108 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_109'] = x_record_endpoint_call__mutmut_109 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_110'] = x_record_endpoint_call__mutmut_110 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_111'] = x_record_endpoint_call__mutmut_111 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_112'] = x_record_endpoint_call__mutmut_112 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_113'] = x_record_endpoint_call__mutmut_113 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_114'] = x_record_endpoint_call__mutmut_114 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_115'] = x_record_endpoint_call__mutmut_115 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_116'] = x_record_endpoint_call__mutmut_116 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_117'] = x_record_endpoint_call__mutmut_117 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_118'] = x_record_endpoint_call__mutmut_118 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_119'] = x_record_endpoint_call__mutmut_119 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_120'] = x_record_endpoint_call__mutmut_120 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_121'] = x_record_endpoint_call__mutmut_121 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_122'] = x_record_endpoint_call__mutmut_122 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_123'] = x_record_endpoint_call__mutmut_123 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_124'] = x_record_endpoint_call__mutmut_124 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_125'] = x_record_endpoint_call__mutmut_125 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_126'] = x_record_endpoint_call__mutmut_126 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_127'] = x_record_endpoint_call__mutmut_127 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_128'] = x_record_endpoint_call__mutmut_128 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_129'] = x_record_endpoint_call__mutmut_129 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_130'] = x_record_endpoint_call__mutmut_130 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_131'] = x_record_endpoint_call__mutmut_131 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_132'] = x_record_endpoint_call__mutmut_132 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_133'] = x_record_endpoint_call__mutmut_133 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_134'] = x_record_endpoint_call__mutmut_134 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_135'] = x_record_endpoint_call__mutmut_135 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_136'] = x_record_endpoint_call__mutmut_136 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_137'] = x_record_endpoint_call__mutmut_137 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_138'] = x_record_endpoint_call__mutmut_138 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_139'] = x_record_endpoint_call__mutmut_139 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_140'] = x_record_endpoint_call__mutmut_140 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_141'] = x_record_endpoint_call__mutmut_141 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_142'] = x_record_endpoint_call__mutmut_142 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_143'] = x_record_endpoint_call__mutmut_143 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_144'] = x_record_endpoint_call__mutmut_144 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_145'] = x_record_endpoint_call__mutmut_145 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_146'] = x_record_endpoint_call__mutmut_146 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_147'] = x_record_endpoint_call__mutmut_147 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_148'] = x_record_endpoint_call__mutmut_148 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_149'] = x_record_endpoint_call__mutmut_149 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_150'] = x_record_endpoint_call__mutmut_150 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_151'] = x_record_endpoint_call__mutmut_151 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_152'] = x_record_endpoint_call__mutmut_152 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_153'] = x_record_endpoint_call__mutmut_153 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_154'] = x_record_endpoint_call__mutmut_154 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_155'] = x_record_endpoint_call__mutmut_155 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_156'] = x_record_endpoint_call__mutmut_156 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_157'] = x_record_endpoint_call__mutmut_157 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_158'] = x_record_endpoint_call__mutmut_158 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_159'] = x_record_endpoint_call__mutmut_159 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_160'] = x_record_endpoint_call__mutmut_160 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_161'] = x_record_endpoint_call__mutmut_161 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_162'] = x_record_endpoint_call__mutmut_162 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_163'] = x_record_endpoint_call__mutmut_163 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_164'] = x_record_endpoint_call__mutmut_164 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_165'] = x_record_endpoint_call__mutmut_165 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_166'] = x_record_endpoint_call__mutmut_166 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_167'] = x_record_endpoint_call__mutmut_167 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_168'] = x_record_endpoint_call__mutmut_168 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_169'] = x_record_endpoint_call__mutmut_169 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_170'] = x_record_endpoint_call__mutmut_170 # type: ignore # mutmut generated
mutants_x_record_endpoint_call__mutmut['x_record_endpoint_call__mutmut_171'] = x_record_endpoint_call__mutmut_171 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_select_best_provider__mutmut)
async def select_best_provider(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_orig(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_1(endpoint: str, fallback_provider: str = "XXyfinanceXX") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_2(endpoint: str, fallback_provider: str = "YFINANCE") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_3(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = None
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_4(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(None)
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_5(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(None, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_6(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, None)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_7(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_8(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, )
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_9(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(None)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_10(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint != endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_11(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active != True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_12(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == False)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_13(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = None
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_14(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(None)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_15(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = None
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_16(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best or best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_17(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score > 50.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_18(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 51.0:
                return best.provider
    except Exception as e:
        log.error(f"Error selecting best provider: {e}")
    return fallback_provider


async def x_select_best_provider__mutmut_19(endpoint: str, fallback_provider: str = "yfinance") -> str:
    """TSYS-5a: Automatic priority selection based on health scorecards."""
    try:
        async with AsyncSessionLocal() as db:
            stmt = (
                select(ProviderHealthScorecard)
                .where(ProviderHealthScorecard.endpoint == endpoint, ProviderHealthScorecard.is_active == True)
                .order_by(ProviderHealthScorecard.health_score.desc())
            )
            res = await db.execute(stmt)
            best = res.scalars().first()
            if best and best.health_score >= 50.0:
                return best.provider
    except Exception as e:
        log.error(None)
    return fallback_provider

mutants_x_select_best_provider__mutmut['_mutmut_orig'] = x_select_best_provider__mutmut_orig # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_1'] = x_select_best_provider__mutmut_1 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_2'] = x_select_best_provider__mutmut_2 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_3'] = x_select_best_provider__mutmut_3 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_4'] = x_select_best_provider__mutmut_4 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_5'] = x_select_best_provider__mutmut_5 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_6'] = x_select_best_provider__mutmut_6 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_7'] = x_select_best_provider__mutmut_7 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_8'] = x_select_best_provider__mutmut_8 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_9'] = x_select_best_provider__mutmut_9 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_10'] = x_select_best_provider__mutmut_10 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_11'] = x_select_best_provider__mutmut_11 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_12'] = x_select_best_provider__mutmut_12 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_13'] = x_select_best_provider__mutmut_13 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_14'] = x_select_best_provider__mutmut_14 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_15'] = x_select_best_provider__mutmut_15 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_16'] = x_select_best_provider__mutmut_16 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_17'] = x_select_best_provider__mutmut_17 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_18'] = x_select_best_provider__mutmut_18 # type: ignore # mutmut generated
mutants_x_select_best_provider__mutmut['x_select_best_provider__mutmut_19'] = x_select_best_provider__mutmut_19 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_validate_corporate_actions__mutmut)
async def validate_corporate_actions(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_orig(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_1(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = None
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_2(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = False
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_3(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = ""

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_4(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = None
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_5(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(None)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_6(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value + yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_7(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value == 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_8(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 1:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_9(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = None
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_10(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff * polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_11(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = None

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_12(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff >= 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_13(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 1.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_14(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = None
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_15(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = True
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_16(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = None

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_17(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = None
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_18(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=None,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_19(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=None,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_20(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=None,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_21(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=None,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_22(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=None,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_23(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=None,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_24(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=None,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_25(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_26(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_27(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_28(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_29(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_30(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_31(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_32(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(None)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_33(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_34(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = None
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_35(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type=None,
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_36(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity=None,
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_37(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=None,
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_38(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details=None,
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_39(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_40(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_41(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_42(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_43(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="XXcorporate_action_discrepancyXX",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_44(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="CORPORATE_ACTION_DISCREPANCY",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_45(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="XXwarningXX",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_46(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="WARNING",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_47(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "XXtickerXX": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_48(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "TICKER": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_49(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "XXaction_typeXX": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_50(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "ACTION_TYPE": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_51(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "XXdateXX": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_52(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "DATE": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_53(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(None),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_54(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "XXpolygonXX": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_55(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "POLYGON": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_56(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "XXyfinanceXX": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_57(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "YFINANCE": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_58(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(None)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return True


async def x_validate_corporate_actions__mutmut_59(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(None)
        return True


async def x_validate_corporate_actions__mutmut_60(
    ticker: str, execution_date: date, polygon_value: float, yfinance_value: float, action_type: str
) -> bool:
    """TSYS-5c: Compare splits/dividends across providers and record discrepancy details."""
    try:
        is_valid = True
        discrepancy_details = None

        # Check if values differ significantly (more than 1% relative difference)
        diff = abs(polygon_value - yfinance_value)
        if polygon_value != 0:
            rel_diff = diff / polygon_value
        else:
            rel_diff = diff

        if rel_diff > 0.01:
            is_valid = False
            discrepancy_details = (
                f"Discrepancy: Polygon={polygon_value}, yfinance={yfinance_value} (diff={diff:.4f}, rel={rel_diff:.2%})"
            )

        async with AsyncSessionLocal() as db:
            val = CorporateActionValidation(
                ticker=ticker,
                action_type=action_type,
                execution_date=execution_date,
                polygon_value=polygon_value,
                yfinance_value=yfinance_value,
                is_valid=is_valid,
                discrepancy_details=discrepancy_details,
            )
            db.add(val)

            if not is_valid:
                # Log incident
                incident = IncidentTimeline(
                    event_type="corporate_action_discrepancy",
                    severity="warning",
                    message=f"Corporate action validation failed for {ticker} on {execution_date}: {discrepancy_details}",
                    details={
                        "ticker": ticker,
                        "action_type": action_type,
                        "date": str(execution_date),
                        "polygon": polygon_value,
                        "yfinance": yfinance_value,
                    },
                )
                db.add(incident)

            await db.commit()
        return is_valid
    except Exception as e:
        log.error(f"Failed to validate corporate action: {e}")
        return False

mutants_x_validate_corporate_actions__mutmut['_mutmut_orig'] = x_validate_corporate_actions__mutmut_orig # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_1'] = x_validate_corporate_actions__mutmut_1 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_2'] = x_validate_corporate_actions__mutmut_2 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_3'] = x_validate_corporate_actions__mutmut_3 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_4'] = x_validate_corporate_actions__mutmut_4 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_5'] = x_validate_corporate_actions__mutmut_5 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_6'] = x_validate_corporate_actions__mutmut_6 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_7'] = x_validate_corporate_actions__mutmut_7 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_8'] = x_validate_corporate_actions__mutmut_8 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_9'] = x_validate_corporate_actions__mutmut_9 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_10'] = x_validate_corporate_actions__mutmut_10 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_11'] = x_validate_corporate_actions__mutmut_11 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_12'] = x_validate_corporate_actions__mutmut_12 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_13'] = x_validate_corporate_actions__mutmut_13 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_14'] = x_validate_corporate_actions__mutmut_14 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_15'] = x_validate_corporate_actions__mutmut_15 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_16'] = x_validate_corporate_actions__mutmut_16 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_17'] = x_validate_corporate_actions__mutmut_17 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_18'] = x_validate_corporate_actions__mutmut_18 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_19'] = x_validate_corporate_actions__mutmut_19 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_20'] = x_validate_corporate_actions__mutmut_20 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_21'] = x_validate_corporate_actions__mutmut_21 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_22'] = x_validate_corporate_actions__mutmut_22 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_23'] = x_validate_corporate_actions__mutmut_23 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_24'] = x_validate_corporate_actions__mutmut_24 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_25'] = x_validate_corporate_actions__mutmut_25 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_26'] = x_validate_corporate_actions__mutmut_26 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_27'] = x_validate_corporate_actions__mutmut_27 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_28'] = x_validate_corporate_actions__mutmut_28 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_29'] = x_validate_corporate_actions__mutmut_29 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_30'] = x_validate_corporate_actions__mutmut_30 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_31'] = x_validate_corporate_actions__mutmut_31 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_32'] = x_validate_corporate_actions__mutmut_32 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_33'] = x_validate_corporate_actions__mutmut_33 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_34'] = x_validate_corporate_actions__mutmut_34 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_35'] = x_validate_corporate_actions__mutmut_35 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_36'] = x_validate_corporate_actions__mutmut_36 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_37'] = x_validate_corporate_actions__mutmut_37 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_38'] = x_validate_corporate_actions__mutmut_38 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_39'] = x_validate_corporate_actions__mutmut_39 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_40'] = x_validate_corporate_actions__mutmut_40 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_41'] = x_validate_corporate_actions__mutmut_41 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_42'] = x_validate_corporate_actions__mutmut_42 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_43'] = x_validate_corporate_actions__mutmut_43 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_44'] = x_validate_corporate_actions__mutmut_44 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_45'] = x_validate_corporate_actions__mutmut_45 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_46'] = x_validate_corporate_actions__mutmut_46 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_47'] = x_validate_corporate_actions__mutmut_47 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_48'] = x_validate_corporate_actions__mutmut_48 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_49'] = x_validate_corporate_actions__mutmut_49 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_50'] = x_validate_corporate_actions__mutmut_50 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_51'] = x_validate_corporate_actions__mutmut_51 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_52'] = x_validate_corporate_actions__mutmut_52 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_53'] = x_validate_corporate_actions__mutmut_53 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_54'] = x_validate_corporate_actions__mutmut_54 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_55'] = x_validate_corporate_actions__mutmut_55 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_56'] = x_validate_corporate_actions__mutmut_56 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_57'] = x_validate_corporate_actions__mutmut_57 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_58'] = x_validate_corporate_actions__mutmut_58 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_59'] = x_validate_corporate_actions__mutmut_59 # type: ignore # mutmut generated
mutants_x_validate_corporate_actions__mutmut['x_validate_corporate_actions__mutmut_60'] = x_validate_corporate_actions__mutmut_60 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_detect_schema_drift__mutmut)
def detect_schema_drift(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_orig(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_1(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider != "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_2(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "XXpolygonXX":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_3(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "POLYGON":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_4(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "XXaggsXX" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_5(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "AGGS" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_6(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" not in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_7(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = None
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_8(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["XXresultsXX", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_9(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["RESULTS", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_10(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "XXtickerXX", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_11(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "TICKER", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_12(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "XXstatusXX"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_13(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "STATUS"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_14(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = None
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_15(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_16(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return False, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_17(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_18(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return False, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_19(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(None)}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_20(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get(None))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_21(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('XXresultsXX'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_22(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('RESULTS'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_23(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "XXsnapshotXX" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_24(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "SNAPSHOT" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_25(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" not in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_26(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = None
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_27(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["XXstatusXX", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_28(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["STATUS", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_29(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "XXtickersXX"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_30(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "TICKERS"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_31(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = None
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_32(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_33(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return False, f"Missing fields: {missing}"
    return False, None


def x_detect_schema_drift__mutmut_34(provider: str, endpoint: str, data: dict) -> tuple[bool, str]:
    """Simple check on key expected fields in responses to detect schema drift."""
    if provider == "polygon":
        if "aggs" in endpoint:
            expected = ["results", "ticker", "status"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
            if not isinstance(data.get("results"), list):
                return True, f"results field is not a list, got {type(data.get('results'))}"
        elif "snapshot" in endpoint:
            expected = ["status", "tickers"]
            missing = [k for k in expected if k not in data]
            if missing:
                return True, f"Missing fields: {missing}"
    return True, None

mutants_x_detect_schema_drift__mutmut['_mutmut_orig'] = x_detect_schema_drift__mutmut_orig # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_1'] = x_detect_schema_drift__mutmut_1 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_2'] = x_detect_schema_drift__mutmut_2 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_3'] = x_detect_schema_drift__mutmut_3 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_4'] = x_detect_schema_drift__mutmut_4 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_5'] = x_detect_schema_drift__mutmut_5 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_6'] = x_detect_schema_drift__mutmut_6 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_7'] = x_detect_schema_drift__mutmut_7 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_8'] = x_detect_schema_drift__mutmut_8 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_9'] = x_detect_schema_drift__mutmut_9 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_10'] = x_detect_schema_drift__mutmut_10 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_11'] = x_detect_schema_drift__mutmut_11 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_12'] = x_detect_schema_drift__mutmut_12 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_13'] = x_detect_schema_drift__mutmut_13 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_14'] = x_detect_schema_drift__mutmut_14 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_15'] = x_detect_schema_drift__mutmut_15 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_16'] = x_detect_schema_drift__mutmut_16 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_17'] = x_detect_schema_drift__mutmut_17 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_18'] = x_detect_schema_drift__mutmut_18 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_19'] = x_detect_schema_drift__mutmut_19 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_20'] = x_detect_schema_drift__mutmut_20 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_21'] = x_detect_schema_drift__mutmut_21 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_22'] = x_detect_schema_drift__mutmut_22 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_23'] = x_detect_schema_drift__mutmut_23 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_24'] = x_detect_schema_drift__mutmut_24 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_25'] = x_detect_schema_drift__mutmut_25 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_26'] = x_detect_schema_drift__mutmut_26 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_27'] = x_detect_schema_drift__mutmut_27 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_28'] = x_detect_schema_drift__mutmut_28 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_29'] = x_detect_schema_drift__mutmut_29 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_30'] = x_detect_schema_drift__mutmut_30 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_31'] = x_detect_schema_drift__mutmut_31 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_32'] = x_detect_schema_drift__mutmut_32 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_33'] = x_detect_schema_drift__mutmut_33 # type: ignore # mutmut generated
mutants_x_detect_schema_drift__mutmut['x_detect_schema_drift__mutmut_34'] = x_detect_schema_drift__mutmut_34 # type: ignore # mutmut generated
