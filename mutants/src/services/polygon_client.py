import asyncio
import logging
import os
import time
from datetime import datetime, timedelta, timezone

import pandas as pd

from services.http_client import shared_session

log = logging.getLogger("signal.trade.polygon")

_BASE = "https://api.polygon.io"

# In-process snapshot cache: keyed by ticker, value is (snap_dict, monotonic_ts).
# Populated by get_polygon_snapshot_batch(); reused by get_polygon_extended_hours()
# so a scan cycle's batch call eliminates per-ticker individual snapshot requests.
_snapshot_cache: dict[str, tuple[dict, float]] = {}
_SNAPSHOT_TTL = 300  # 5 min — one full scan cycle

# Dividend reference cache: stable data, 24 h TTL
_div_cache: dict[str, tuple[list, float]] = {}
_DIV_TTL = 86_400


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__get_api_key__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__get_api_key__mutmut)
def _get_api_key() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_orig() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_1() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = None
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_2() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") and os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_3() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv(None) or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_4() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("XXPOLYGON_API_KEYXX") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_5() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("polygon_api_key") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_6() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv(None)
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_7() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("XXMASSIVE_API_KEYXX")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_8() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("massive_api_key")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_9() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_10() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = None
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_11() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = None
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_12() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key and s.massive_api_key
        except Exception:
            key = ""
    return key or ""


def x__get_api_key__mutmut_13() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = None
    return key or ""


def x__get_api_key__mutmut_14() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = "XXXX"
    return key or ""


def x__get_api_key__mutmut_15() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key and ""


def x__get_api_key__mutmut_16() -> str:
    """
    Resolve Polygon.io API key. Checks the process env (POLYGON_API_KEY, then
    MASSIVE_API_KEY — same provider) first, then falls back to pydantic settings
    (which loads .env). The fallback matters: the live server's launchd plist sets
    only PATH, so os.getenv() is empty there and Polygon was silently falling back
    to yfinance engine-wide. Reading settings makes the configured .env key work.
    """
    key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not key:
        try:
            from config import get_settings

            s = get_settings()
            key = s.polygon_api_key or s.massive_api_key
        except Exception:
            key = ""
    return key or "XXXX"

mutants_x__get_api_key__mutmut['_mutmut_orig'] = x__get_api_key__mutmut_orig # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_1'] = x__get_api_key__mutmut_1 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_2'] = x__get_api_key__mutmut_2 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_3'] = x__get_api_key__mutmut_3 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_4'] = x__get_api_key__mutmut_4 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_5'] = x__get_api_key__mutmut_5 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_6'] = x__get_api_key__mutmut_6 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_7'] = x__get_api_key__mutmut_7 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_8'] = x__get_api_key__mutmut_8 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_9'] = x__get_api_key__mutmut_9 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_10'] = x__get_api_key__mutmut_10 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_11'] = x__get_api_key__mutmut_11 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_12'] = x__get_api_key__mutmut_12 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_13'] = x__get_api_key__mutmut_13 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_14'] = x__get_api_key__mutmut_14 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_15'] = x__get_api_key__mutmut_15 # type: ignore # mutmut generated
mutants_x__get_api_key__mutmut['x__get_api_key__mutmut_16'] = x__get_api_key__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_history__mutmut)
async def get_polygon_history(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_orig(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_1(ticker: str, period: str = "XX3moXX", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_2(ticker: str, period: str = "3MO", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_3(ticker: str, period: str = "3mo", interval: str = "XX1dXX") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_4(ticker: str, period: str = "3mo", interval: str = "1D") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_5(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = None
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_6(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_7(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval != "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_8(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "XX1dXX":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_9(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1D":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_10(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = None
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_11(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 2, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_12(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "XXdayXX"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_13(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "DAY"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_14(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval != "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_15(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "XX1hXX":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_16(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1H":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_17(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = None
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_18(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 2, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_19(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "XXhourXX"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_20(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "HOUR"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_21(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval != "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_22(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "XX5mXX":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_23(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5M":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_24(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = None
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_25(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 6, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_26(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "XXminuteXX"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_27(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "MINUTE"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_28(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = None

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_29(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 2, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_30(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "XXdayXX"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_31(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "DAY"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_32(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = None
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_33(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(None).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_34(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period != "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_35(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "XX1dXX":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_36(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1D":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_37(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = None  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_38(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt + timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_39(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=None)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_40(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=3)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_41(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period != "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_42(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "XX5dXX":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_43(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5D":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_44(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = None
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_45(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt + timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_46(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=None)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_47(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=8)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_48(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period != "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_49(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "XX1moXX":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_50(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1MO":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_51(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = None
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_52(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt + timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_53(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=None)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_54(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=31)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_55(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period != "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_56(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "XX3moXX":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_57(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3MO":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_58(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = None
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_59(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt + timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_60(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=None)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_61(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=91)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_62(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period != "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_63(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "XX6moXX":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_64(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6MO":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_65(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = None
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_66(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt + timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_67(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=None)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_68(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=181)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_69(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period != "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_70(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "XX1yXX":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_71(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1Y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_72(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = None
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_73(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt + timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_74(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=None)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_75(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=366)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_76(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period != "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_77(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "XX2yXX":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_78(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2Y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_79(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = None
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_80(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt + timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_81(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=None)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_82(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=731)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_83(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = None

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_84(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt + timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_85(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=None)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_86(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=91)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_87(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = None
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_88(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime(None)
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_89(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("XX%Y-%m-%dXX")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_90(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_91(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%M-%D")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_92(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = None

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_93(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime(None)

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_94(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("XX%Y-%m-%dXX")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_95(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_96(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%M-%D")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_97(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = None
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_98(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.lower()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_99(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = None

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_100(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"XXadjustedXX": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_101(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"ADJUSTED": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_102(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "XXtrueXX", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_103(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "TRUE", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_104(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "XXsortXX": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_105(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "SORT": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_106(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "XXascXX", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_107(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "ASC", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_108(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "XXlimitXX": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_109(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "LIMIT": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_110(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50001, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_111(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "XXapiKeyXX": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_112(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apikey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_113(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "APIKEY": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_114(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_115(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_116(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_117(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_118(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_119(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_120(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_121(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=11) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_122(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = None
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_123(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = None
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_124(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) / 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_125(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() + t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_126(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1001.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_127(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status == 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_128(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 201:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_129(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(None)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_130(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call(None, "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_131(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", None, latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_132(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", None, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_133(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, None, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_134(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=None)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_135(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_136(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_137(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_138(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_139(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, )
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_140(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("XXpolygonXX", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_141(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("POLYGON", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_142(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "XX/v2/aggsXX", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_143(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/V2/AGGS", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_144(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = None
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_145(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = None
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_146(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift(None, "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_147(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", None, data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_148(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", None)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_149(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_150(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_151(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", )
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_152(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("XXpolygonXX", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_153(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("POLYGON", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_154(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "XX/v2/aggsXX", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_155(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/V2/AGGS", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_156(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        None,
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_157(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        None,
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_158(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        None,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_159(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        None,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_160(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        None,
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_161(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        None,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_162(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=None,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_163(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=None,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_164(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_165(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_166(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_167(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_168(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_169(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_170(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_171(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_172(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "XXpolygonXX",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_173(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "POLYGON",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_174(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "XX/v2/aggsXX",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_175(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/V2/AGGS",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_176(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(None),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_177(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = None
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_178(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get(None, [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_179(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", None)
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_180(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get([])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_181(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", )
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_182(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("XXresultsXX", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_183(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("RESULTS", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_184(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_185(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = None
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_186(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(None)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_187(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = None
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_188(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "XXDatetimeXX"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_189(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_190(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "DATETIME"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_191(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert(None)
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_192(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(None, unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_193(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit=None, utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_194(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=None).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_195(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_196(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_197(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", ).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_198(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["XXtXX"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_199(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["T"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_200(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="XXmsXX", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_201(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="MS", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_202(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=False).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_203(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("XXAmerica/New_YorkXX")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_204(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("america/new_york")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_205(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("AMERICA/NEW_YORK")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_206(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index(None, inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_207(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=None)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_208(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index(inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_209(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", )

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_210(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("XXDatetimeXX", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_211(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_212(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("DATETIME", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_213(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=False)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_214(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns=None, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_215(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=None)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_216(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_217(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, )

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_218(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"XXoXX": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_219(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"O": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_220(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "XXOpenXX", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_221(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_222(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "OPEN", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_223(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "XXhXX": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_224(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "H": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_225(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "XXHighXX", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_226(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "high", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_227(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "HIGH", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_228(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "XXlXX": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_229(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "L": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_230(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "XXLowXX", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_231(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_232(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "LOW", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_233(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "XXcXX": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_234(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "C": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_235(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "XXCloseXX", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_236(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_237(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "CLOSE", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_238(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "XXvXX": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_239(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "V": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_240(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "XXVolumeXX"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_241(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_242(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "VOLUME"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_243(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=False)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_244(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["XXOpenXX", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_245(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_246(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["OPEN", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_247(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "XXHighXX", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_248(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "high", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_249(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "HIGH", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_250(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "XXLowXX", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_251(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_252(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "LOW", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_253(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "XXCloseXX", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_254(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_255(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "CLOSE", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_256(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "XXVolumeXX"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_257(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_258(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "VOLUME"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(e).__name__}: {e}")
        return None


async def x_get_polygon_history__mutmut_259(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(None)
        return None


async def x_get_polygon_history__mutmut_260(ticker: str, period: str = "3mo", interval: str = "1d") -> pd.DataFrame | None:
    """
    Fetch historical OHLCV data from Polygon.io.
    Returns a DataFrame identical in shape to yfinance (Open, High, Low, Close, Volume).
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    # Map interval to Polygon multiplier and timespan
    if interval == "1d":
        multiplier, timespan = 1, "day"
    elif interval == "1h":
        multiplier, timespan = 1, "hour"
    elif interval == "5m":
        multiplier, timespan = 5, "minute"
    else:
        multiplier, timespan = 1, "day"

    # Map yfinance period strings to date ranges
    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    if period == "1d":
        start_dt = end_dt - timedelta(days=2)  # Extra days to ensure we get data over weekends
    elif period == "5d":
        start_dt = end_dt - timedelta(days=7)
    elif period == "1mo":
        start_dt = end_dt - timedelta(days=30)
    elif period == "3mo":
        start_dt = end_dt - timedelta(days=90)
    elif period == "6mo":
        start_dt = end_dt - timedelta(days=180)
    elif period == "1y":
        start_dt = end_dt - timedelta(days=365)
    elif period == "2y":
        start_dt = end_dt - timedelta(days=730)
    else:
        start_dt = end_dt - timedelta(days=90)

    start_str = start_dt.strftime("%Y-%m-%d")
    end_str = end_dt.strftime("%Y-%m-%d")

    url = f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}/range/{multiplier}/{timespan}/{start_str}/{end_str}"
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(f"[polygon] Error fetching {ticker}: HTTP {resp.status}")
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/aggs", latency, status_code, ticker=ticker)
                    except Exception:
                        pass
                    return None
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/aggs", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/aggs",
                        latency,
                        status_code,
                        json.dumps(data),
                        ticker,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass

                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()

                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True).dt.tz_convert("America/New_York")
                df.set_index("Datetime", inplace=True)

                # Rename to match yfinance output exactly
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)

                # Return only the necessary columns in correct case
                return df[["Open", "High", "Low", "Close", "Volume"]]
    except Exception as e:
        log.warning(f"[polygon] Exception fetching {ticker}: {type(None).__name__}: {e}")
        return None

mutants_x_get_polygon_history__mutmut['_mutmut_orig'] = x_get_polygon_history__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_1'] = x_get_polygon_history__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_2'] = x_get_polygon_history__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_3'] = x_get_polygon_history__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_4'] = x_get_polygon_history__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_5'] = x_get_polygon_history__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_6'] = x_get_polygon_history__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_7'] = x_get_polygon_history__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_8'] = x_get_polygon_history__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_9'] = x_get_polygon_history__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_10'] = x_get_polygon_history__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_11'] = x_get_polygon_history__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_12'] = x_get_polygon_history__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_13'] = x_get_polygon_history__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_14'] = x_get_polygon_history__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_15'] = x_get_polygon_history__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_16'] = x_get_polygon_history__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_17'] = x_get_polygon_history__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_18'] = x_get_polygon_history__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_19'] = x_get_polygon_history__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_20'] = x_get_polygon_history__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_21'] = x_get_polygon_history__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_22'] = x_get_polygon_history__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_23'] = x_get_polygon_history__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_24'] = x_get_polygon_history__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_25'] = x_get_polygon_history__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_26'] = x_get_polygon_history__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_27'] = x_get_polygon_history__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_28'] = x_get_polygon_history__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_29'] = x_get_polygon_history__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_30'] = x_get_polygon_history__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_31'] = x_get_polygon_history__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_32'] = x_get_polygon_history__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_33'] = x_get_polygon_history__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_34'] = x_get_polygon_history__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_35'] = x_get_polygon_history__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_36'] = x_get_polygon_history__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_37'] = x_get_polygon_history__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_38'] = x_get_polygon_history__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_39'] = x_get_polygon_history__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_40'] = x_get_polygon_history__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_41'] = x_get_polygon_history__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_42'] = x_get_polygon_history__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_43'] = x_get_polygon_history__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_44'] = x_get_polygon_history__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_45'] = x_get_polygon_history__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_46'] = x_get_polygon_history__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_47'] = x_get_polygon_history__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_48'] = x_get_polygon_history__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_49'] = x_get_polygon_history__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_50'] = x_get_polygon_history__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_51'] = x_get_polygon_history__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_52'] = x_get_polygon_history__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_53'] = x_get_polygon_history__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_54'] = x_get_polygon_history__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_55'] = x_get_polygon_history__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_56'] = x_get_polygon_history__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_57'] = x_get_polygon_history__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_58'] = x_get_polygon_history__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_59'] = x_get_polygon_history__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_60'] = x_get_polygon_history__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_61'] = x_get_polygon_history__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_62'] = x_get_polygon_history__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_63'] = x_get_polygon_history__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_64'] = x_get_polygon_history__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_65'] = x_get_polygon_history__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_66'] = x_get_polygon_history__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_67'] = x_get_polygon_history__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_68'] = x_get_polygon_history__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_69'] = x_get_polygon_history__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_70'] = x_get_polygon_history__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_71'] = x_get_polygon_history__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_72'] = x_get_polygon_history__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_73'] = x_get_polygon_history__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_74'] = x_get_polygon_history__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_75'] = x_get_polygon_history__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_76'] = x_get_polygon_history__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_77'] = x_get_polygon_history__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_78'] = x_get_polygon_history__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_79'] = x_get_polygon_history__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_80'] = x_get_polygon_history__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_81'] = x_get_polygon_history__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_82'] = x_get_polygon_history__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_83'] = x_get_polygon_history__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_84'] = x_get_polygon_history__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_85'] = x_get_polygon_history__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_86'] = x_get_polygon_history__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_87'] = x_get_polygon_history__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_88'] = x_get_polygon_history__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_89'] = x_get_polygon_history__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_90'] = x_get_polygon_history__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_91'] = x_get_polygon_history__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_92'] = x_get_polygon_history__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_93'] = x_get_polygon_history__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_94'] = x_get_polygon_history__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_95'] = x_get_polygon_history__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_96'] = x_get_polygon_history__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_97'] = x_get_polygon_history__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_98'] = x_get_polygon_history__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_99'] = x_get_polygon_history__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_100'] = x_get_polygon_history__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_101'] = x_get_polygon_history__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_102'] = x_get_polygon_history__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_103'] = x_get_polygon_history__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_104'] = x_get_polygon_history__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_105'] = x_get_polygon_history__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_106'] = x_get_polygon_history__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_107'] = x_get_polygon_history__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_108'] = x_get_polygon_history__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_109'] = x_get_polygon_history__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_110'] = x_get_polygon_history__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_111'] = x_get_polygon_history__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_112'] = x_get_polygon_history__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_113'] = x_get_polygon_history__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_114'] = x_get_polygon_history__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_115'] = x_get_polygon_history__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_116'] = x_get_polygon_history__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_117'] = x_get_polygon_history__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_118'] = x_get_polygon_history__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_119'] = x_get_polygon_history__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_120'] = x_get_polygon_history__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_121'] = x_get_polygon_history__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_122'] = x_get_polygon_history__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_123'] = x_get_polygon_history__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_124'] = x_get_polygon_history__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_125'] = x_get_polygon_history__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_126'] = x_get_polygon_history__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_127'] = x_get_polygon_history__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_128'] = x_get_polygon_history__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_129'] = x_get_polygon_history__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_130'] = x_get_polygon_history__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_131'] = x_get_polygon_history__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_132'] = x_get_polygon_history__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_133'] = x_get_polygon_history__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_134'] = x_get_polygon_history__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_135'] = x_get_polygon_history__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_136'] = x_get_polygon_history__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_137'] = x_get_polygon_history__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_138'] = x_get_polygon_history__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_139'] = x_get_polygon_history__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_140'] = x_get_polygon_history__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_141'] = x_get_polygon_history__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_142'] = x_get_polygon_history__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_143'] = x_get_polygon_history__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_144'] = x_get_polygon_history__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_145'] = x_get_polygon_history__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_146'] = x_get_polygon_history__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_147'] = x_get_polygon_history__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_148'] = x_get_polygon_history__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_149'] = x_get_polygon_history__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_150'] = x_get_polygon_history__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_151'] = x_get_polygon_history__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_152'] = x_get_polygon_history__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_153'] = x_get_polygon_history__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_154'] = x_get_polygon_history__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_155'] = x_get_polygon_history__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_156'] = x_get_polygon_history__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_157'] = x_get_polygon_history__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_158'] = x_get_polygon_history__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_159'] = x_get_polygon_history__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_160'] = x_get_polygon_history__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_161'] = x_get_polygon_history__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_162'] = x_get_polygon_history__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_163'] = x_get_polygon_history__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_164'] = x_get_polygon_history__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_165'] = x_get_polygon_history__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_166'] = x_get_polygon_history__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_167'] = x_get_polygon_history__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_168'] = x_get_polygon_history__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_169'] = x_get_polygon_history__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_170'] = x_get_polygon_history__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_171'] = x_get_polygon_history__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_172'] = x_get_polygon_history__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_173'] = x_get_polygon_history__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_174'] = x_get_polygon_history__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_175'] = x_get_polygon_history__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_176'] = x_get_polygon_history__mutmut_176 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_177'] = x_get_polygon_history__mutmut_177 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_178'] = x_get_polygon_history__mutmut_178 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_179'] = x_get_polygon_history__mutmut_179 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_180'] = x_get_polygon_history__mutmut_180 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_181'] = x_get_polygon_history__mutmut_181 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_182'] = x_get_polygon_history__mutmut_182 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_183'] = x_get_polygon_history__mutmut_183 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_184'] = x_get_polygon_history__mutmut_184 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_185'] = x_get_polygon_history__mutmut_185 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_186'] = x_get_polygon_history__mutmut_186 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_187'] = x_get_polygon_history__mutmut_187 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_188'] = x_get_polygon_history__mutmut_188 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_189'] = x_get_polygon_history__mutmut_189 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_190'] = x_get_polygon_history__mutmut_190 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_191'] = x_get_polygon_history__mutmut_191 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_192'] = x_get_polygon_history__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_193'] = x_get_polygon_history__mutmut_193 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_194'] = x_get_polygon_history__mutmut_194 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_195'] = x_get_polygon_history__mutmut_195 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_196'] = x_get_polygon_history__mutmut_196 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_197'] = x_get_polygon_history__mutmut_197 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_198'] = x_get_polygon_history__mutmut_198 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_199'] = x_get_polygon_history__mutmut_199 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_200'] = x_get_polygon_history__mutmut_200 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_201'] = x_get_polygon_history__mutmut_201 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_202'] = x_get_polygon_history__mutmut_202 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_203'] = x_get_polygon_history__mutmut_203 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_204'] = x_get_polygon_history__mutmut_204 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_205'] = x_get_polygon_history__mutmut_205 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_206'] = x_get_polygon_history__mutmut_206 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_207'] = x_get_polygon_history__mutmut_207 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_208'] = x_get_polygon_history__mutmut_208 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_209'] = x_get_polygon_history__mutmut_209 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_210'] = x_get_polygon_history__mutmut_210 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_211'] = x_get_polygon_history__mutmut_211 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_212'] = x_get_polygon_history__mutmut_212 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_213'] = x_get_polygon_history__mutmut_213 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_214'] = x_get_polygon_history__mutmut_214 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_215'] = x_get_polygon_history__mutmut_215 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_216'] = x_get_polygon_history__mutmut_216 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_217'] = x_get_polygon_history__mutmut_217 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_218'] = x_get_polygon_history__mutmut_218 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_219'] = x_get_polygon_history__mutmut_219 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_220'] = x_get_polygon_history__mutmut_220 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_221'] = x_get_polygon_history__mutmut_221 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_222'] = x_get_polygon_history__mutmut_222 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_223'] = x_get_polygon_history__mutmut_223 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_224'] = x_get_polygon_history__mutmut_224 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_225'] = x_get_polygon_history__mutmut_225 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_226'] = x_get_polygon_history__mutmut_226 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_227'] = x_get_polygon_history__mutmut_227 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_228'] = x_get_polygon_history__mutmut_228 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_229'] = x_get_polygon_history__mutmut_229 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_230'] = x_get_polygon_history__mutmut_230 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_231'] = x_get_polygon_history__mutmut_231 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_232'] = x_get_polygon_history__mutmut_232 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_233'] = x_get_polygon_history__mutmut_233 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_234'] = x_get_polygon_history__mutmut_234 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_235'] = x_get_polygon_history__mutmut_235 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_236'] = x_get_polygon_history__mutmut_236 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_237'] = x_get_polygon_history__mutmut_237 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_238'] = x_get_polygon_history__mutmut_238 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_239'] = x_get_polygon_history__mutmut_239 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_240'] = x_get_polygon_history__mutmut_240 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_241'] = x_get_polygon_history__mutmut_241 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_242'] = x_get_polygon_history__mutmut_242 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_243'] = x_get_polygon_history__mutmut_243 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_244'] = x_get_polygon_history__mutmut_244 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_245'] = x_get_polygon_history__mutmut_245 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_246'] = x_get_polygon_history__mutmut_246 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_247'] = x_get_polygon_history__mutmut_247 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_248'] = x_get_polygon_history__mutmut_248 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_249'] = x_get_polygon_history__mutmut_249 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_250'] = x_get_polygon_history__mutmut_250 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_251'] = x_get_polygon_history__mutmut_251 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_252'] = x_get_polygon_history__mutmut_252 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_253'] = x_get_polygon_history__mutmut_253 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_254'] = x_get_polygon_history__mutmut_254 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_255'] = x_get_polygon_history__mutmut_255 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_256'] = x_get_polygon_history__mutmut_256 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_257'] = x_get_polygon_history__mutmut_257 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_258'] = x_get_polygon_history__mutmut_258 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_259'] = x_get_polygon_history__mutmut_259 # type: ignore # mutmut generated
mutants_x_get_polygon_history__mutmut['x_get_polygon_history__mutmut_260'] = x_get_polygon_history__mutmut_260 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_histories_batch__mutmut)
async def get_polygon_histories_batch(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_orig(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_1(
    tickers: list[str], period: str = "XX1yXX", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_2(
    tickers: list[str], period: str = "1Y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_3(
    tickers: list[str], period: str = "1y", interval: str = "XX1dXX", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_4(
    tickers: list[str], period: str = "1y", interval: str = "1D", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_5(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 21
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_6(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() and not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_7(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_8(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_9(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = None

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_10(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(None)

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_11(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(None, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_12(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, None))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_13(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_14(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, ))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_15(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(2, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_16(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = None
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_17(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(None, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_18(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=None, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_19(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=None)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_20(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_21(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_22(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, )
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_23(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty or len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_24(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None or not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_25(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_26(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_27(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) > 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_28(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 3:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_29(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = None
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_30(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=None)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_31(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_32(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], )
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_33(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(None) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_34(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.lower()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_35(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=False)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_36(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = None
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_37(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            break
        ticker, df = item
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_38(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = None
        if df is not None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_39(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is None:
            out[ticker] = df
    return out


async def x_get_polygon_histories_batch__mutmut_40(
    tickers: list[str], period: str = "1y", interval: str = "1d", concurrency: int = 20
) -> dict[str, pd.DataFrame]:
    """
    Fetch OHLCV for many tickers from Polygon/Massive first.
    Callers can fall back missing tickers to yfinance.
    """
    if not _get_api_key() or not tickers:
        return {}

    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            df = await get_polygon_history(ticker, period=period, interval=interval)
            if df is not None and not df.empty and len(df) >= 2:
                return ticker, df
            return ticker, None

    results = await asyncio.gather(*[_one(t.upper()) for t in tickers], return_exceptions=True)
    out: dict[str, pd.DataFrame] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, df = item
        if df is not None:
            out[ticker] = None
    return out

mutants_x_get_polygon_histories_batch__mutmut['_mutmut_orig'] = x_get_polygon_histories_batch__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_1'] = x_get_polygon_histories_batch__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_2'] = x_get_polygon_histories_batch__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_3'] = x_get_polygon_histories_batch__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_4'] = x_get_polygon_histories_batch__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_5'] = x_get_polygon_histories_batch__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_6'] = x_get_polygon_histories_batch__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_7'] = x_get_polygon_histories_batch__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_8'] = x_get_polygon_histories_batch__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_9'] = x_get_polygon_histories_batch__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_10'] = x_get_polygon_histories_batch__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_11'] = x_get_polygon_histories_batch__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_12'] = x_get_polygon_histories_batch__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_13'] = x_get_polygon_histories_batch__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_14'] = x_get_polygon_histories_batch__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_15'] = x_get_polygon_histories_batch__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_16'] = x_get_polygon_histories_batch__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_17'] = x_get_polygon_histories_batch__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_18'] = x_get_polygon_histories_batch__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_19'] = x_get_polygon_histories_batch__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_20'] = x_get_polygon_histories_batch__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_21'] = x_get_polygon_histories_batch__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_22'] = x_get_polygon_histories_batch__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_23'] = x_get_polygon_histories_batch__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_24'] = x_get_polygon_histories_batch__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_25'] = x_get_polygon_histories_batch__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_26'] = x_get_polygon_histories_batch__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_27'] = x_get_polygon_histories_batch__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_28'] = x_get_polygon_histories_batch__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_29'] = x_get_polygon_histories_batch__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_30'] = x_get_polygon_histories_batch__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_31'] = x_get_polygon_histories_batch__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_32'] = x_get_polygon_histories_batch__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_33'] = x_get_polygon_histories_batch__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_34'] = x_get_polygon_histories_batch__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_35'] = x_get_polygon_histories_batch__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_36'] = x_get_polygon_histories_batch__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_37'] = x_get_polygon_histories_batch__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_38'] = x_get_polygon_histories_batch__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_39'] = x_get_polygon_histories_batch__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_histories_batch__mutmut['x_get_polygon_histories_batch__mutmut_40'] = x_get_polygon_histories_batch__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_snapshot_batch__mutmut)
async def get_polygon_snapshot_batch(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_orig(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_1(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = None
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_2(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key and not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_3(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_4(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_5(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = None
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_6(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = None
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_7(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"XXtickersXX": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_8(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"TICKERS": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_9(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(None), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_10(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": "XX,XX".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_11(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.lower() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_12(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "XXapiKeyXX": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_13(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apikey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_14(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "APIKEY": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_15(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_16(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_17(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_18(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_19(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_20(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_21(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_22(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=16) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_23(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = None
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_24(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = None
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_25(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) / 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_26(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() + t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_27(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1001.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_28(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status == 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_29(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 201:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_30(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(None, resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_31(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", None)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_32(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning(resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_33(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", )
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_34(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("XX[polygon] snapshot batch: HTTP %sXX", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_35(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: http %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_36(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[POLYGON] SNAPSHOT BATCH: HTTP %S", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_37(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call(None, "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_38(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", None, latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_39(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", None, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_40(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, None)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_41(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_42(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_43(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_44(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, )
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_45(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("XXpolygonXX", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_46(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("POLYGON", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_47(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "XX/v2/snapshotXX", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_48(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/V2/SNAPSHOT", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_49(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = None
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_50(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = None
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_51(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift(None, "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_52(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", None, data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_53(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", None)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_54(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_55(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_56(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", )
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_57(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("XXpolygonXX", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_58(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("POLYGON", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_59(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "XX/v2/snapshotXX", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_60(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/V2/SNAPSHOT", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_61(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        None,
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_62(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        None,
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_63(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        None,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_64(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        None,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_65(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        None,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_66(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=None,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_67(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=None,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_68(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_69(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_70(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_71(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_72(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_73(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_74(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_75(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "XXpolygonXX",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_76(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "POLYGON",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_77(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "XX/v2/snapshotXX",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_78(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/V2/SNAPSHOT",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_79(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(None),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_80(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = None
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_81(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = None
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_82(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") and []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_83(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get(None) or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_84(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("XXtickersXX") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_85(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("TICKERS") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_86(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = None
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_87(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").lower()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_88(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") and "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_89(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get(None) or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_90(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("XXtickerXX") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_91(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("TICKER") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_92(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "XXXX").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_93(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_94(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                break
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_95(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = None
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_96(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = None
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_97(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning(None, e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_98(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", None)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_99(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning(e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_100(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[polygon] snapshot batch error: %s", )
        return {}


async def x_get_polygon_snapshot_batch__mutmut_101(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("XX[polygon] snapshot batch error: %sXX", e)
        return {}


async def x_get_polygon_snapshot_batch__mutmut_102(tickers: list[str]) -> dict[str, dict]:
    """Single batch call → real-time price, change, VWAP, volume for all tickers.

    Response is under the "tickers" key (NOT "results"). Each snap is cached in
    _snapshot_cache so get_polygon_extended_hours() can reuse it without a second call.
    """
    api_key = _get_api_key()
    if not api_key or not tickers:
        return {}
    url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers"
    params = {"tickers": ",".join(t.upper() for t in tickers), "apiKey": api_key}
    t0 = time.perf_counter()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                status_code = resp.status
                latency = (time.perf_counter() - t0) * 1000.0
                if resp.status != 200:
                    log.warning("[polygon] snapshot batch: HTTP %s", resp.status)
                    try:
                        from services.provider_reliability import record_endpoint_call

                        await record_endpoint_call("polygon", "/v2/snapshot", latency, status_code)
                    except Exception:
                        pass
                    return {}
                data = await resp.json()
                try:
                    import json
                    from services.provider_reliability import record_endpoint_call, detect_schema_drift

                    is_drifted, drift_details = detect_schema_drift("polygon", "/v2/snapshot", data)
                    await record_endpoint_call(
                        "polygon",
                        "/v2/snapshot",
                        latency,
                        status_code,
                        json.dumps(data),
                        is_drifted=is_drifted,
                        drift_details=drift_details,
                    )
                except Exception:
                    pass
        now = time.monotonic()
        out: dict[str, dict] = {}
        for snap in data.get("tickers") or []:  # NOTE: "tickers" key, NOT "results"
            t = (snap.get("ticker") or "").upper()
            if not t:
                continue
            _snapshot_cache[t] = (snap, now)
            out[t] = snap
        return out
    except Exception as e:
        log.warning("[POLYGON] SNAPSHOT BATCH ERROR: %S", e)
        return {}

mutants_x_get_polygon_snapshot_batch__mutmut['_mutmut_orig'] = x_get_polygon_snapshot_batch__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_1'] = x_get_polygon_snapshot_batch__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_2'] = x_get_polygon_snapshot_batch__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_3'] = x_get_polygon_snapshot_batch__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_4'] = x_get_polygon_snapshot_batch__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_5'] = x_get_polygon_snapshot_batch__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_6'] = x_get_polygon_snapshot_batch__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_7'] = x_get_polygon_snapshot_batch__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_8'] = x_get_polygon_snapshot_batch__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_9'] = x_get_polygon_snapshot_batch__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_10'] = x_get_polygon_snapshot_batch__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_11'] = x_get_polygon_snapshot_batch__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_12'] = x_get_polygon_snapshot_batch__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_13'] = x_get_polygon_snapshot_batch__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_14'] = x_get_polygon_snapshot_batch__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_15'] = x_get_polygon_snapshot_batch__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_16'] = x_get_polygon_snapshot_batch__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_17'] = x_get_polygon_snapshot_batch__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_18'] = x_get_polygon_snapshot_batch__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_19'] = x_get_polygon_snapshot_batch__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_20'] = x_get_polygon_snapshot_batch__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_21'] = x_get_polygon_snapshot_batch__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_22'] = x_get_polygon_snapshot_batch__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_23'] = x_get_polygon_snapshot_batch__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_24'] = x_get_polygon_snapshot_batch__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_25'] = x_get_polygon_snapshot_batch__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_26'] = x_get_polygon_snapshot_batch__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_27'] = x_get_polygon_snapshot_batch__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_28'] = x_get_polygon_snapshot_batch__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_29'] = x_get_polygon_snapshot_batch__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_30'] = x_get_polygon_snapshot_batch__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_31'] = x_get_polygon_snapshot_batch__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_32'] = x_get_polygon_snapshot_batch__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_33'] = x_get_polygon_snapshot_batch__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_34'] = x_get_polygon_snapshot_batch__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_35'] = x_get_polygon_snapshot_batch__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_36'] = x_get_polygon_snapshot_batch__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_37'] = x_get_polygon_snapshot_batch__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_38'] = x_get_polygon_snapshot_batch__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_39'] = x_get_polygon_snapshot_batch__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_40'] = x_get_polygon_snapshot_batch__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_41'] = x_get_polygon_snapshot_batch__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_42'] = x_get_polygon_snapshot_batch__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_43'] = x_get_polygon_snapshot_batch__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_44'] = x_get_polygon_snapshot_batch__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_45'] = x_get_polygon_snapshot_batch__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_46'] = x_get_polygon_snapshot_batch__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_47'] = x_get_polygon_snapshot_batch__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_48'] = x_get_polygon_snapshot_batch__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_49'] = x_get_polygon_snapshot_batch__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_50'] = x_get_polygon_snapshot_batch__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_51'] = x_get_polygon_snapshot_batch__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_52'] = x_get_polygon_snapshot_batch__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_53'] = x_get_polygon_snapshot_batch__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_54'] = x_get_polygon_snapshot_batch__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_55'] = x_get_polygon_snapshot_batch__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_56'] = x_get_polygon_snapshot_batch__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_57'] = x_get_polygon_snapshot_batch__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_58'] = x_get_polygon_snapshot_batch__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_59'] = x_get_polygon_snapshot_batch__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_60'] = x_get_polygon_snapshot_batch__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_61'] = x_get_polygon_snapshot_batch__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_62'] = x_get_polygon_snapshot_batch__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_63'] = x_get_polygon_snapshot_batch__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_64'] = x_get_polygon_snapshot_batch__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_65'] = x_get_polygon_snapshot_batch__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_66'] = x_get_polygon_snapshot_batch__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_67'] = x_get_polygon_snapshot_batch__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_68'] = x_get_polygon_snapshot_batch__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_69'] = x_get_polygon_snapshot_batch__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_70'] = x_get_polygon_snapshot_batch__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_71'] = x_get_polygon_snapshot_batch__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_72'] = x_get_polygon_snapshot_batch__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_73'] = x_get_polygon_snapshot_batch__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_74'] = x_get_polygon_snapshot_batch__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_75'] = x_get_polygon_snapshot_batch__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_76'] = x_get_polygon_snapshot_batch__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_77'] = x_get_polygon_snapshot_batch__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_78'] = x_get_polygon_snapshot_batch__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_79'] = x_get_polygon_snapshot_batch__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_80'] = x_get_polygon_snapshot_batch__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_81'] = x_get_polygon_snapshot_batch__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_82'] = x_get_polygon_snapshot_batch__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_83'] = x_get_polygon_snapshot_batch__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_84'] = x_get_polygon_snapshot_batch__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_85'] = x_get_polygon_snapshot_batch__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_86'] = x_get_polygon_snapshot_batch__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_87'] = x_get_polygon_snapshot_batch__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_88'] = x_get_polygon_snapshot_batch__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_89'] = x_get_polygon_snapshot_batch__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_90'] = x_get_polygon_snapshot_batch__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_91'] = x_get_polygon_snapshot_batch__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_92'] = x_get_polygon_snapshot_batch__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_93'] = x_get_polygon_snapshot_batch__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_94'] = x_get_polygon_snapshot_batch__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_95'] = x_get_polygon_snapshot_batch__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_96'] = x_get_polygon_snapshot_batch__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_97'] = x_get_polygon_snapshot_batch__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_98'] = x_get_polygon_snapshot_batch__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_99'] = x_get_polygon_snapshot_batch__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_100'] = x_get_polygon_snapshot_batch__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_101'] = x_get_polygon_snapshot_batch__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_polygon_snapshot_batch__mutmut['x_get_polygon_snapshot_batch__mutmut_102'] = x_get_polygon_snapshot_batch__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_quotes_batch__mutmut)
async def get_polygon_quotes_batch(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_orig(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_1(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = None
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_2(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(None)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_3(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = None
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_4(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = None
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_5(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(None)
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_6(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.lower())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_7(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_8(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            break
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_9(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = None
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_10(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") and {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_11(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get(None) or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_12(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("XXlastTradeXX") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_13(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lasttrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_14(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("LASTTRADE") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_15(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = None
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_16(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") and {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_17(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get(None) or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_18(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("XXdayXX") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_19(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("DAY") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_20(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = None
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_21(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") and {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_22(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get(None) or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_23(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("XXminXX") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_24(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("MIN") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_25(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = None
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_26(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") and {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_27(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get(None) or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_28(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("XXprevDayXX") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_29(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevday") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_30(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("PREVDAY") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_31(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = None
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_32(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                None
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_33(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") and 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_34(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") and day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_35(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") and minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_36(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get(None) or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_37(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("XXpXX") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_38(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("P") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_39(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get(None) or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_40(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("XXcXX") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_41(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("C") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_42(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get(None) or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_43(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("XXcXX") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_44(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("C") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_45(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 1
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_46(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = None
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_47(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(None)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_48(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") and 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_49(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get(None) or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_50(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("XXcXX") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_51(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("C") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_52(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 1)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_53(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 and prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_54(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price < 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_55(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 1 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_56(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close < 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_57(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 1:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_58(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                break
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_59(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                None
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_60(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "XXtXX": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_61(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "T": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_62(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.lower(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_63(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "XXpXX": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_64(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "P": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_65(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(None, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_66(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, None),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_67(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_68(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, ),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_69(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 3),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_70(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "XXcXX": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_71(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "C": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_72(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round(None, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_73(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, None),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_74(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round(2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_75(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, ),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_76(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close / 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_77(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) * prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_78(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price + prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_79(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 101, 2),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_80(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 3),
                }
            )
        except Exception:
            continue
    return quotes


async def x_get_polygon_quotes_batch__mutmut_81(tickers: list[str]) -> list[dict]:
    """Return real-time quote {t, p, c} rows via a single batch snapshot call.

    Replaces the old approach of N individual OHLCV calls. Falls back to empty
    list if Polygon is unavailable; market_data.py caller will use yfinance.
    """
    snaps = await get_polygon_snapshot_batch(tickers)
    quotes: list[dict] = []
    for ticker in tickers:
        snap = snaps.get(ticker.upper())
        if not snap:
            continue
        try:
            last_trade = snap.get("lastTrade") or {}
            day = snap.get("day") or {}
            minute = snap.get("min") or {}
            prev_day = snap.get("prevDay") or {}
            # Polygon snapshot structure varies by market state. Prefer lastTrade,
            # then minute close, then day close (all can lag slightly after hours).
            price = float(
                last_trade.get("p") or minute.get("c") or day.get("c") or 0
            )
            prev_close = float(prev_day.get("c") or 0)
            if price <= 0 or prev_close <= 0:
                continue
            quotes.append(
                {
                    "t": ticker.upper(),
                    "p": round(price, 2),
                    "c": round((price - prev_close) / prev_close * 100, 2),
                }
            )
        except Exception:
            break
    return quotes

mutants_x_get_polygon_quotes_batch__mutmut['_mutmut_orig'] = x_get_polygon_quotes_batch__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_1'] = x_get_polygon_quotes_batch__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_2'] = x_get_polygon_quotes_batch__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_3'] = x_get_polygon_quotes_batch__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_4'] = x_get_polygon_quotes_batch__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_5'] = x_get_polygon_quotes_batch__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_6'] = x_get_polygon_quotes_batch__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_7'] = x_get_polygon_quotes_batch__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_8'] = x_get_polygon_quotes_batch__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_9'] = x_get_polygon_quotes_batch__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_10'] = x_get_polygon_quotes_batch__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_11'] = x_get_polygon_quotes_batch__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_12'] = x_get_polygon_quotes_batch__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_13'] = x_get_polygon_quotes_batch__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_14'] = x_get_polygon_quotes_batch__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_15'] = x_get_polygon_quotes_batch__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_16'] = x_get_polygon_quotes_batch__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_17'] = x_get_polygon_quotes_batch__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_18'] = x_get_polygon_quotes_batch__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_19'] = x_get_polygon_quotes_batch__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_20'] = x_get_polygon_quotes_batch__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_21'] = x_get_polygon_quotes_batch__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_22'] = x_get_polygon_quotes_batch__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_23'] = x_get_polygon_quotes_batch__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_24'] = x_get_polygon_quotes_batch__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_25'] = x_get_polygon_quotes_batch__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_26'] = x_get_polygon_quotes_batch__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_27'] = x_get_polygon_quotes_batch__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_28'] = x_get_polygon_quotes_batch__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_29'] = x_get_polygon_quotes_batch__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_30'] = x_get_polygon_quotes_batch__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_31'] = x_get_polygon_quotes_batch__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_32'] = x_get_polygon_quotes_batch__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_33'] = x_get_polygon_quotes_batch__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_34'] = x_get_polygon_quotes_batch__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_35'] = x_get_polygon_quotes_batch__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_36'] = x_get_polygon_quotes_batch__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_37'] = x_get_polygon_quotes_batch__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_38'] = x_get_polygon_quotes_batch__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_39'] = x_get_polygon_quotes_batch__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_40'] = x_get_polygon_quotes_batch__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_41'] = x_get_polygon_quotes_batch__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_42'] = x_get_polygon_quotes_batch__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_43'] = x_get_polygon_quotes_batch__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_44'] = x_get_polygon_quotes_batch__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_45'] = x_get_polygon_quotes_batch__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_46'] = x_get_polygon_quotes_batch__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_47'] = x_get_polygon_quotes_batch__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_48'] = x_get_polygon_quotes_batch__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_49'] = x_get_polygon_quotes_batch__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_50'] = x_get_polygon_quotes_batch__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_51'] = x_get_polygon_quotes_batch__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_52'] = x_get_polygon_quotes_batch__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_53'] = x_get_polygon_quotes_batch__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_54'] = x_get_polygon_quotes_batch__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_55'] = x_get_polygon_quotes_batch__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_56'] = x_get_polygon_quotes_batch__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_57'] = x_get_polygon_quotes_batch__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_58'] = x_get_polygon_quotes_batch__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_59'] = x_get_polygon_quotes_batch__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_60'] = x_get_polygon_quotes_batch__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_61'] = x_get_polygon_quotes_batch__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_62'] = x_get_polygon_quotes_batch__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_63'] = x_get_polygon_quotes_batch__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_64'] = x_get_polygon_quotes_batch__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_65'] = x_get_polygon_quotes_batch__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_66'] = x_get_polygon_quotes_batch__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_67'] = x_get_polygon_quotes_batch__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_68'] = x_get_polygon_quotes_batch__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_69'] = x_get_polygon_quotes_batch__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_70'] = x_get_polygon_quotes_batch__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_71'] = x_get_polygon_quotes_batch__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_72'] = x_get_polygon_quotes_batch__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_73'] = x_get_polygon_quotes_batch__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_74'] = x_get_polygon_quotes_batch__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_75'] = x_get_polygon_quotes_batch__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_76'] = x_get_polygon_quotes_batch__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_77'] = x_get_polygon_quotes_batch__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_78'] = x_get_polygon_quotes_batch__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_79'] = x_get_polygon_quotes_batch__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_80'] = x_get_polygon_quotes_batch__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_quotes_batch__mutmut['x_get_polygon_quotes_batch__mutmut_81'] = x_get_polygon_quotes_batch__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_info__mutmut)
async def get_polygon_info(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_orig(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_1(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = None
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_2(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_3(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = None
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_4(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.lower()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_5(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_6(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"XXapiKeyXX": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_7(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apikey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_8(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"APIKEY": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_9(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_10(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_11(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_12(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_13(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_14(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_15(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=11) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_16(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status == 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_17(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 201:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_18(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = None
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_19(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = None
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_20(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") and {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_21(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get(None) or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_22(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("XXresultsXX") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_23(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("RESULTS") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_24(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_25(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "XXcompanyXX": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_26(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "COMPANY": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_27(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") and ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_28(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get(None) or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_29(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("XXnameXX") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_30(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("NAME") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_31(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.lower(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_32(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "XXmarket_capXX": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_33(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "MARKET_CAP": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_34(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get(None),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_35(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("XXmarket_capXX"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_36(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("MARKET_CAP"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_37(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "XXweighted_shares_outstandingXX": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_38(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "WEIGHTED_SHARES_OUTSTANDING": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_39(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get(None),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_40(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("XXweighted_shares_outstandingXX"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_41(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("WEIGHTED_SHARES_OUTSTANDING"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_42(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "XXhomepage_urlXX": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_43(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "HOMEPAGE_URL": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_44(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get(None),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_45(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("XXhomepage_urlXX"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_46(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("HOMEPAGE_URL"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_47(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "XXsic_descriptionXX": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_48(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "SIC_DESCRIPTION": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_49(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get(None),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_50(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("XXsic_descriptionXX"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_51(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("SIC_DESCRIPTION"),
        }
    except Exception as e:
        log.debug(f"[polygon] info {ticker}: {e}")
        return None


async def x_get_polygon_info__mutmut_52(ticker: str) -> dict | None:
    """Fetch reference metadata available from Polygon's ticker details endpoint."""
    api_key = _get_api_key()
    if not api_key:
        return None
    url = f"{_BASE}/v3/reference/tickers/{ticker.upper()}"
    params = {"apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
        result = data.get("results") or {}
        if not result:
            return None
        return {
            "company": result.get("name") or ticker.upper(),
            "market_cap": result.get("market_cap"),
            "weighted_shares_outstanding": result.get("weighted_shares_outstanding"),
            "homepage_url": result.get("homepage_url"),
            "sic_description": result.get("sic_description"),
        }
    except Exception as e:
        log.debug(None)
        return None

mutants_x_get_polygon_info__mutmut['_mutmut_orig'] = x_get_polygon_info__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_1'] = x_get_polygon_info__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_2'] = x_get_polygon_info__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_3'] = x_get_polygon_info__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_4'] = x_get_polygon_info__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_5'] = x_get_polygon_info__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_6'] = x_get_polygon_info__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_7'] = x_get_polygon_info__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_8'] = x_get_polygon_info__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_9'] = x_get_polygon_info__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_10'] = x_get_polygon_info__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_11'] = x_get_polygon_info__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_12'] = x_get_polygon_info__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_13'] = x_get_polygon_info__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_14'] = x_get_polygon_info__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_15'] = x_get_polygon_info__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_16'] = x_get_polygon_info__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_17'] = x_get_polygon_info__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_18'] = x_get_polygon_info__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_19'] = x_get_polygon_info__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_20'] = x_get_polygon_info__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_21'] = x_get_polygon_info__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_22'] = x_get_polygon_info__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_23'] = x_get_polygon_info__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_24'] = x_get_polygon_info__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_25'] = x_get_polygon_info__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_26'] = x_get_polygon_info__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_27'] = x_get_polygon_info__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_28'] = x_get_polygon_info__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_29'] = x_get_polygon_info__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_30'] = x_get_polygon_info__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_31'] = x_get_polygon_info__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_32'] = x_get_polygon_info__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_33'] = x_get_polygon_info__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_34'] = x_get_polygon_info__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_35'] = x_get_polygon_info__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_36'] = x_get_polygon_info__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_37'] = x_get_polygon_info__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_38'] = x_get_polygon_info__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_39'] = x_get_polygon_info__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_40'] = x_get_polygon_info__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_41'] = x_get_polygon_info__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_42'] = x_get_polygon_info__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_43'] = x_get_polygon_info__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_44'] = x_get_polygon_info__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_45'] = x_get_polygon_info__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_46'] = x_get_polygon_info__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_47'] = x_get_polygon_info__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_48'] = x_get_polygon_info__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_49'] = x_get_polygon_info__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_50'] = x_get_polygon_info__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_51'] = x_get_polygon_info__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_info__mutmut['x_get_polygon_info__mutmut_52'] = x_get_polygon_info__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_infos_batch__mutmut)
async def get_polygon_infos_batch(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_orig(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_1(tickers: list[str], concurrency: int = 21) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_2(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() and not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_3(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_4(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_5(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = None

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_6(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(None)

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_7(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(None, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_8(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, None))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_9(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_10(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, ))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_11(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(2, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_12(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.lower(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_13(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(None)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_14(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = None
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_15(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=None)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_16(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_17(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], )
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_18(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(None) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_19(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=False)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_20(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = None
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_21(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            break
        ticker, info = item
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_22(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = None
        if info:
            out[ticker] = info
    return out


async def x_get_polygon_infos_batch__mutmut_23(tickers: list[str], concurrency: int = 20) -> dict[str, dict]:
    if not _get_api_key() or not tickers:
        return {}
    sem = asyncio.Semaphore(max(1, concurrency))

    async def _one(ticker: str):
        async with sem:
            return ticker.upper(), await get_polygon_info(ticker)

    results = await asyncio.gather(*[_one(t) for t in tickers], return_exceptions=True)
    out: dict[str, dict] = {}
    for item in results:
        if isinstance(item, Exception):
            continue
        ticker, info = item
        if info:
            out[ticker] = None
    return out

mutants_x_get_polygon_infos_batch__mutmut['_mutmut_orig'] = x_get_polygon_infos_batch__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_1'] = x_get_polygon_infos_batch__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_2'] = x_get_polygon_infos_batch__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_3'] = x_get_polygon_infos_batch__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_4'] = x_get_polygon_infos_batch__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_5'] = x_get_polygon_infos_batch__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_6'] = x_get_polygon_infos_batch__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_7'] = x_get_polygon_infos_batch__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_8'] = x_get_polygon_infos_batch__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_9'] = x_get_polygon_infos_batch__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_10'] = x_get_polygon_infos_batch__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_11'] = x_get_polygon_infos_batch__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_12'] = x_get_polygon_infos_batch__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_13'] = x_get_polygon_infos_batch__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_14'] = x_get_polygon_infos_batch__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_15'] = x_get_polygon_infos_batch__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_16'] = x_get_polygon_infos_batch__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_17'] = x_get_polygon_infos_batch__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_18'] = x_get_polygon_infos_batch__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_19'] = x_get_polygon_infos_batch__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_20'] = x_get_polygon_infos_batch__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_21'] = x_get_polygon_infos_batch__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_22'] = x_get_polygon_infos_batch__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_infos_batch__mutmut['x_get_polygon_infos_batch__mutmut_23'] = x_get_polygon_infos_batch__mutmut_23 # type: ignore # mutmut generated


_weekly_cache: dict[str, tuple[pd.DataFrame, float]] = {}
_WEEKLY_TTL = 43200  # 12 hours
mutants_x_get_polygon_weekly_bars__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_weekly_bars__mutmut)
async def get_polygon_weekly_bars(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_orig(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_1(ticker: str, weeks: int = 27) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_2(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = None
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_3(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.lower()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_4(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = None
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_5(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = None
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_6(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(None, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_7(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, None)
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_8(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get((None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_9(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, )
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_10(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 1.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_11(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None or now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_12(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_13(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now + ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_14(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts <= _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_15(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = None
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_16(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_17(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = None
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_18(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(None).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_19(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = None  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_20(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt + timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_21(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=None)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_22(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks - 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_23(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 5)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_24(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = None
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_25(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.lower()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_26(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime(None)}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_27(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('XX%Y-%m-%dXX')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_28(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_29(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%M-%D')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_30(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime(None)}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_31(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('XX%Y-%m-%dXX')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_32(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_33(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%M-%D')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_34(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = None

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_35(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"XXadjustedXX": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_36(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"ADJUSTED": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_37(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "XXtrueXX", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_38(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "TRUE", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_39(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "XXsortXX": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_40(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "SORT": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_41(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "XXascXX", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_42(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "ASC", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_43(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "XXlimitXX": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_44(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "LIMIT": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_45(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50001, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_46(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "XXapiKeyXX": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_47(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apikey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_48(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "APIKEY": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_49(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_50(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_51(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_52(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_53(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_54(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_55(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=11) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_56(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status == 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_57(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 201:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_58(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = None
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_59(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = None
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_60(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get(None, [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_61(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", None)
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_62(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get([])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_63(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", )
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_64(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("XXresultsXX", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_65(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("RESULTS", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_66(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_67(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = None
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_68(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(None)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_69(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = None
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_70(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "XXDatetimeXX"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_71(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_72(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "DATETIME"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_73(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(None, unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_74(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit=None, utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_75(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=None)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_76(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_77(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_78(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", )
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_79(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["XXtXX"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_80(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["T"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_81(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="XXmsXX", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_82(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="MS", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_83(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=False)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_84(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index(None, inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_85(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=None)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_86(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index(inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_87(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", )
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_88(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("XXDatetimeXX", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_89(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_90(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("DATETIME", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_91(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=False)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_92(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns=None, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_93(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=None)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_94(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_95(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, )
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_96(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"XXoXX": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_97(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"O": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_98(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "XXOpenXX", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_99(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_100(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "OPEN", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_101(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "XXhXX": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_102(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "H": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_103(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "XXHighXX", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_104(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "high", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_105(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "HIGH", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_106(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "XXlXX": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_107(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "L": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_108(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "XXLowXX", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_109(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_110(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "LOW", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_111(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "XXcXX": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_112(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "C": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_113(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "XXCloseXX", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_114(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_115(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "CLOSE", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_116(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "XXvXX": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_117(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "V": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_118(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "XXVolumeXX"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_119(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_120(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "VOLUME"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_121(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=False)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_122(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = None
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_123(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(None)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_124(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["XXOpenXX", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_125(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_126(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["OPEN", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_127(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "XXHighXX", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_128(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "high", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_129(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "HIGH", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_130(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "XXLowXX", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_131(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_132(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "LOW", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_133(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "XXCloseXX", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_134(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_135(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "CLOSE", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_136(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "XXVolumeXX"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_137(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_138(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "VOLUME"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_139(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = None
                return df_out.copy()
    except Exception as e:
        log.warning(f"[polygon] weekly bars {ticker}: {e}")
        return None


async def x_get_polygon_weekly_bars__mutmut_140(ticker: str, weeks: int = 26) -> pd.DataFrame | None:
    """
    Fetch the last `weeks` weekly OHLCV bars from Polygon.io.
    Used for weekly trend strength confirmation — reduces false signals in
    choppy markets that look bullish on daily but not on weekly timeframe.
    Returns DataFrame with columns Open/High/Low/Close/Volume, or None on error.
    """
    t = ticker.upper()
    now = time.monotonic()
    cached, ts = _weekly_cache.get(t, (None, 0.0))
    if cached is not None and now - ts < _WEEKLY_TTL:
        return cached.copy()

    api_key = _get_api_key()
    if not api_key:
        return None

    end_dt = datetime.now(timezone.utc).replace(tzinfo=None)
    start_dt = end_dt - timedelta(weeks=weeks + 4)  # extra buffer for weekends/holidays
    url = (
        f"https://api.polygon.io/v2/aggs/ticker/{ticker.upper()}"
        f"/range/1/week/{start_dt.strftime('%Y-%m-%d')}/{end_dt.strftime('%Y-%m-%d')}"
    )
    params = {"adjusted": "true", "sort": "asc", "limit": 50000, "apiKey": api_key}

    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return None
                data = await resp.json()
                results = data.get("results", [])
                if not results:
                    return pd.DataFrame()
                df = pd.DataFrame(results)
                df.loc[:, "Datetime"] = pd.to_datetime(df["t"], unit="ms", utc=True)
                df.set_index("Datetime", inplace=True)
                df.rename(columns={"o": "Open", "h": "High", "l": "Low", "c": "Close", "v": "Volume"}, inplace=True)
                df_out = df[["Open", "High", "Low", "Close", "Volume"]].tail(weeks)
                _weekly_cache[t] = (df_out, time.monotonic())
                return df_out.copy()
    except Exception as e:
        log.warning(None)
        return None

mutants_x_get_polygon_weekly_bars__mutmut['_mutmut_orig'] = x_get_polygon_weekly_bars__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_1'] = x_get_polygon_weekly_bars__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_2'] = x_get_polygon_weekly_bars__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_3'] = x_get_polygon_weekly_bars__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_4'] = x_get_polygon_weekly_bars__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_5'] = x_get_polygon_weekly_bars__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_6'] = x_get_polygon_weekly_bars__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_7'] = x_get_polygon_weekly_bars__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_8'] = x_get_polygon_weekly_bars__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_9'] = x_get_polygon_weekly_bars__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_10'] = x_get_polygon_weekly_bars__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_11'] = x_get_polygon_weekly_bars__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_12'] = x_get_polygon_weekly_bars__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_13'] = x_get_polygon_weekly_bars__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_14'] = x_get_polygon_weekly_bars__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_15'] = x_get_polygon_weekly_bars__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_16'] = x_get_polygon_weekly_bars__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_17'] = x_get_polygon_weekly_bars__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_18'] = x_get_polygon_weekly_bars__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_19'] = x_get_polygon_weekly_bars__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_20'] = x_get_polygon_weekly_bars__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_21'] = x_get_polygon_weekly_bars__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_22'] = x_get_polygon_weekly_bars__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_23'] = x_get_polygon_weekly_bars__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_24'] = x_get_polygon_weekly_bars__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_25'] = x_get_polygon_weekly_bars__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_26'] = x_get_polygon_weekly_bars__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_27'] = x_get_polygon_weekly_bars__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_28'] = x_get_polygon_weekly_bars__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_29'] = x_get_polygon_weekly_bars__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_30'] = x_get_polygon_weekly_bars__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_31'] = x_get_polygon_weekly_bars__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_32'] = x_get_polygon_weekly_bars__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_33'] = x_get_polygon_weekly_bars__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_34'] = x_get_polygon_weekly_bars__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_35'] = x_get_polygon_weekly_bars__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_36'] = x_get_polygon_weekly_bars__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_37'] = x_get_polygon_weekly_bars__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_38'] = x_get_polygon_weekly_bars__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_39'] = x_get_polygon_weekly_bars__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_40'] = x_get_polygon_weekly_bars__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_41'] = x_get_polygon_weekly_bars__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_42'] = x_get_polygon_weekly_bars__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_43'] = x_get_polygon_weekly_bars__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_44'] = x_get_polygon_weekly_bars__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_45'] = x_get_polygon_weekly_bars__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_46'] = x_get_polygon_weekly_bars__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_47'] = x_get_polygon_weekly_bars__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_48'] = x_get_polygon_weekly_bars__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_49'] = x_get_polygon_weekly_bars__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_50'] = x_get_polygon_weekly_bars__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_51'] = x_get_polygon_weekly_bars__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_52'] = x_get_polygon_weekly_bars__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_53'] = x_get_polygon_weekly_bars__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_54'] = x_get_polygon_weekly_bars__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_55'] = x_get_polygon_weekly_bars__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_56'] = x_get_polygon_weekly_bars__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_57'] = x_get_polygon_weekly_bars__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_58'] = x_get_polygon_weekly_bars__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_59'] = x_get_polygon_weekly_bars__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_60'] = x_get_polygon_weekly_bars__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_61'] = x_get_polygon_weekly_bars__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_62'] = x_get_polygon_weekly_bars__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_63'] = x_get_polygon_weekly_bars__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_64'] = x_get_polygon_weekly_bars__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_65'] = x_get_polygon_weekly_bars__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_66'] = x_get_polygon_weekly_bars__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_67'] = x_get_polygon_weekly_bars__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_68'] = x_get_polygon_weekly_bars__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_69'] = x_get_polygon_weekly_bars__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_70'] = x_get_polygon_weekly_bars__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_71'] = x_get_polygon_weekly_bars__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_72'] = x_get_polygon_weekly_bars__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_73'] = x_get_polygon_weekly_bars__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_74'] = x_get_polygon_weekly_bars__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_75'] = x_get_polygon_weekly_bars__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_76'] = x_get_polygon_weekly_bars__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_77'] = x_get_polygon_weekly_bars__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_78'] = x_get_polygon_weekly_bars__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_79'] = x_get_polygon_weekly_bars__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_80'] = x_get_polygon_weekly_bars__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_81'] = x_get_polygon_weekly_bars__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_82'] = x_get_polygon_weekly_bars__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_83'] = x_get_polygon_weekly_bars__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_84'] = x_get_polygon_weekly_bars__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_85'] = x_get_polygon_weekly_bars__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_86'] = x_get_polygon_weekly_bars__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_87'] = x_get_polygon_weekly_bars__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_88'] = x_get_polygon_weekly_bars__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_89'] = x_get_polygon_weekly_bars__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_90'] = x_get_polygon_weekly_bars__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_91'] = x_get_polygon_weekly_bars__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_92'] = x_get_polygon_weekly_bars__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_93'] = x_get_polygon_weekly_bars__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_94'] = x_get_polygon_weekly_bars__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_95'] = x_get_polygon_weekly_bars__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_96'] = x_get_polygon_weekly_bars__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_97'] = x_get_polygon_weekly_bars__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_98'] = x_get_polygon_weekly_bars__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_99'] = x_get_polygon_weekly_bars__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_100'] = x_get_polygon_weekly_bars__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_101'] = x_get_polygon_weekly_bars__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_102'] = x_get_polygon_weekly_bars__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_103'] = x_get_polygon_weekly_bars__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_104'] = x_get_polygon_weekly_bars__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_105'] = x_get_polygon_weekly_bars__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_106'] = x_get_polygon_weekly_bars__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_107'] = x_get_polygon_weekly_bars__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_108'] = x_get_polygon_weekly_bars__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_109'] = x_get_polygon_weekly_bars__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_110'] = x_get_polygon_weekly_bars__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_111'] = x_get_polygon_weekly_bars__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_112'] = x_get_polygon_weekly_bars__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_113'] = x_get_polygon_weekly_bars__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_114'] = x_get_polygon_weekly_bars__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_115'] = x_get_polygon_weekly_bars__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_116'] = x_get_polygon_weekly_bars__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_117'] = x_get_polygon_weekly_bars__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_118'] = x_get_polygon_weekly_bars__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_119'] = x_get_polygon_weekly_bars__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_120'] = x_get_polygon_weekly_bars__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_121'] = x_get_polygon_weekly_bars__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_122'] = x_get_polygon_weekly_bars__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_123'] = x_get_polygon_weekly_bars__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_124'] = x_get_polygon_weekly_bars__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_125'] = x_get_polygon_weekly_bars__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_126'] = x_get_polygon_weekly_bars__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_127'] = x_get_polygon_weekly_bars__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_128'] = x_get_polygon_weekly_bars__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_129'] = x_get_polygon_weekly_bars__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_130'] = x_get_polygon_weekly_bars__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_131'] = x_get_polygon_weekly_bars__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_132'] = x_get_polygon_weekly_bars__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_133'] = x_get_polygon_weekly_bars__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_134'] = x_get_polygon_weekly_bars__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_135'] = x_get_polygon_weekly_bars__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_136'] = x_get_polygon_weekly_bars__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_137'] = x_get_polygon_weekly_bars__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_138'] = x_get_polygon_weekly_bars__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_139'] = x_get_polygon_weekly_bars__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_polygon_weekly_bars__mutmut['x_get_polygon_weekly_bars__mutmut_140'] = x_get_polygon_weekly_bars__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_extended_hours__mutmut)
async def get_polygon_extended_hours(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_orig(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_1(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = None
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_2(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_3(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = None
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_4(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.lower()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_5(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = None
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_6(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(None, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_7(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, None)
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_8(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get((None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_9(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, )
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_10(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 1.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_11(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None or time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_12(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_13(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() + ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_14(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts <= _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_15(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = None
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_16(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = None
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_17(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = None
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_18(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"XXapiKeyXX": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_19(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apikey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_20(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"APIKEY": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_21(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(None, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_22(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=None, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_23(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=None) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_24(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_25(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_26(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, ) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_27(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=11) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_28(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status == 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_29(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 201:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_30(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug(None, t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_31(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", None, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_32(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, None)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_33(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug(t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_34(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_35(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, )
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_36(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("XX[polygon] snapshot %s: HTTP %sXX", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_37(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: http %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_38(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[POLYGON] SNAPSHOT %S: HTTP %S", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_39(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = None
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_40(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = None
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_41(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") and {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_42(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get(None) or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_43(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("XXtickerXX") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_44(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("TICKER") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_45(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = None
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_46(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug(None, t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_47(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", None, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_48(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, None)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_49(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug(t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_50(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_51(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, )
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_52(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("XX[polygon] extended_hours %s: %sXX", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_53(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[POLYGON] EXTENDED_HOURS %S: %S", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_54(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = None
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_55(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") and {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_56(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get(None) or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_57(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("XXlastTradeXX") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_58(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lasttrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_59(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("LASTTRADE") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_60(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = None
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_61(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") and {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_62(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get(None) or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_63(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("XXprevDayXX") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_64(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevday") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_65(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("PREVDAY") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_66(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = None

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_67(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") and {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_68(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get(None) or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_69(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("XXminXX") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_70(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("MIN") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_71(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = None
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_72(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get(None)
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_73(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("XXpXX")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_74(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("P")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_75(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = None
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_76(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get(None)
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_77(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("XXcXX")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_78(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("C")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_79(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price and not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_80(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_81(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_82(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = None
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_83(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(None)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_84(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = None
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_85(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(None)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_86(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = None
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_87(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round(None, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_88(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, None)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_89(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round(3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_90(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, )
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_91(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close / 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_92(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) * prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_93(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price + prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_94(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 101, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_95(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 4)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_96(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = None
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_97(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "XXupXX" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_98(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "UP" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_99(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct >= 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_100(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 1.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_101(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "XXdownXX" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_102(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "DOWN" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_103(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct <= -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_104(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < +0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_105(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -1.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_106(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "XXflatXX"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_107(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "FLAT"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_108(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = None

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_109(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(None)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_110(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") and 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_111(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get(None) or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_112(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("XXvXX") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_113(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("V") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_114(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 1)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_115(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "XXpriceXX": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_116(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "PRICE": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_117(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "XXprev_closeXX": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_118(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "PREV_CLOSE": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_119(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "XXgap_pctXX": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_120(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "GAP_PCT": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_121(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "XXvol_ratioXX": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_122(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "VOL_RATIO": 1.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_123(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 2.0,
        "direction": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_124(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "XXdirectionXX": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_125(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "DIRECTION": direction,
        "ext_volume": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_126(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "XXext_volumeXX": ext_volume,
    }


async def x_get_polygon_extended_hours__mutmut_127(ticker: str) -> dict | None:
    """Return extended-hours stats. Checks _snapshot_cache first (populated by
    get_polygon_snapshot_batch during scan), falling back to an individual call.
    """
    api_key = _get_api_key()
    if not api_key:
        return None

    t = ticker.upper()
    snap_cached, ts = _snapshot_cache.get(t, (None, 0.0))
    if snap_cached is not None and time.monotonic() - ts < _SNAPSHOT_TTL:
        t_data = snap_cached
    else:
        url = f"{_BASE}/v2/snapshot/locale/us/markets/stocks/tickers/{t}"
        params = {"apiKey": api_key}
        try:
            async with shared_session() as session:
                async with session.get(url, params=params, timeout=10) as resp:
                    if resp.status != 200:
                        log.debug("[polygon] snapshot %s: HTTP %s", t, resp.status)
                        return None
                    data = await resp.json()
            t_data = data.get("ticker") or {}
            _snapshot_cache[t] = (t_data, time.monotonic())
        except Exception as e:
            log.debug("[polygon] extended_hours %s: %s", t, e)
            return None

    last_trade = t_data.get("lastTrade") or {}
    prev_day = t_data.get("prevDay") or {}
    min_data = t_data.get("min") or {}

    ext_price = last_trade.get("p")
    prev_close = prev_day.get("c")
    if not ext_price or not prev_close:
        return None

    ext_price = float(ext_price)
    prev_close = float(prev_close)
    gap_pct = round((ext_price - prev_close) / prev_close * 100, 3)
    direction = "up" if gap_pct > 0.1 else "down" if gap_pct < -0.1 else "flat"
    ext_volume = int(min_data.get("v") or 0)

    return {
        "price": ext_price,
        "prev_close": prev_close,
        "gap_pct": gap_pct,
        "vol_ratio": 1.0,
        "direction": direction,
        "EXT_VOLUME": ext_volume,
    }

mutants_x_get_polygon_extended_hours__mutmut['_mutmut_orig'] = x_get_polygon_extended_hours__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_1'] = x_get_polygon_extended_hours__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_2'] = x_get_polygon_extended_hours__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_3'] = x_get_polygon_extended_hours__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_4'] = x_get_polygon_extended_hours__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_5'] = x_get_polygon_extended_hours__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_6'] = x_get_polygon_extended_hours__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_7'] = x_get_polygon_extended_hours__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_8'] = x_get_polygon_extended_hours__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_9'] = x_get_polygon_extended_hours__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_10'] = x_get_polygon_extended_hours__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_11'] = x_get_polygon_extended_hours__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_12'] = x_get_polygon_extended_hours__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_13'] = x_get_polygon_extended_hours__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_14'] = x_get_polygon_extended_hours__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_15'] = x_get_polygon_extended_hours__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_16'] = x_get_polygon_extended_hours__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_17'] = x_get_polygon_extended_hours__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_18'] = x_get_polygon_extended_hours__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_19'] = x_get_polygon_extended_hours__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_20'] = x_get_polygon_extended_hours__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_21'] = x_get_polygon_extended_hours__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_22'] = x_get_polygon_extended_hours__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_23'] = x_get_polygon_extended_hours__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_24'] = x_get_polygon_extended_hours__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_25'] = x_get_polygon_extended_hours__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_26'] = x_get_polygon_extended_hours__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_27'] = x_get_polygon_extended_hours__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_28'] = x_get_polygon_extended_hours__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_29'] = x_get_polygon_extended_hours__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_30'] = x_get_polygon_extended_hours__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_31'] = x_get_polygon_extended_hours__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_32'] = x_get_polygon_extended_hours__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_33'] = x_get_polygon_extended_hours__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_34'] = x_get_polygon_extended_hours__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_35'] = x_get_polygon_extended_hours__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_36'] = x_get_polygon_extended_hours__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_37'] = x_get_polygon_extended_hours__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_38'] = x_get_polygon_extended_hours__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_39'] = x_get_polygon_extended_hours__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_40'] = x_get_polygon_extended_hours__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_41'] = x_get_polygon_extended_hours__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_42'] = x_get_polygon_extended_hours__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_43'] = x_get_polygon_extended_hours__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_44'] = x_get_polygon_extended_hours__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_45'] = x_get_polygon_extended_hours__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_46'] = x_get_polygon_extended_hours__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_47'] = x_get_polygon_extended_hours__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_48'] = x_get_polygon_extended_hours__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_49'] = x_get_polygon_extended_hours__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_50'] = x_get_polygon_extended_hours__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_51'] = x_get_polygon_extended_hours__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_52'] = x_get_polygon_extended_hours__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_53'] = x_get_polygon_extended_hours__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_54'] = x_get_polygon_extended_hours__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_55'] = x_get_polygon_extended_hours__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_56'] = x_get_polygon_extended_hours__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_57'] = x_get_polygon_extended_hours__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_58'] = x_get_polygon_extended_hours__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_59'] = x_get_polygon_extended_hours__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_60'] = x_get_polygon_extended_hours__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_61'] = x_get_polygon_extended_hours__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_62'] = x_get_polygon_extended_hours__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_63'] = x_get_polygon_extended_hours__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_64'] = x_get_polygon_extended_hours__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_65'] = x_get_polygon_extended_hours__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_66'] = x_get_polygon_extended_hours__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_67'] = x_get_polygon_extended_hours__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_68'] = x_get_polygon_extended_hours__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_69'] = x_get_polygon_extended_hours__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_70'] = x_get_polygon_extended_hours__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_71'] = x_get_polygon_extended_hours__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_72'] = x_get_polygon_extended_hours__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_73'] = x_get_polygon_extended_hours__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_74'] = x_get_polygon_extended_hours__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_75'] = x_get_polygon_extended_hours__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_76'] = x_get_polygon_extended_hours__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_77'] = x_get_polygon_extended_hours__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_78'] = x_get_polygon_extended_hours__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_79'] = x_get_polygon_extended_hours__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_80'] = x_get_polygon_extended_hours__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_81'] = x_get_polygon_extended_hours__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_82'] = x_get_polygon_extended_hours__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_83'] = x_get_polygon_extended_hours__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_84'] = x_get_polygon_extended_hours__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_85'] = x_get_polygon_extended_hours__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_86'] = x_get_polygon_extended_hours__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_87'] = x_get_polygon_extended_hours__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_88'] = x_get_polygon_extended_hours__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_89'] = x_get_polygon_extended_hours__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_90'] = x_get_polygon_extended_hours__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_91'] = x_get_polygon_extended_hours__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_92'] = x_get_polygon_extended_hours__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_93'] = x_get_polygon_extended_hours__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_94'] = x_get_polygon_extended_hours__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_95'] = x_get_polygon_extended_hours__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_96'] = x_get_polygon_extended_hours__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_97'] = x_get_polygon_extended_hours__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_98'] = x_get_polygon_extended_hours__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_99'] = x_get_polygon_extended_hours__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_100'] = x_get_polygon_extended_hours__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_101'] = x_get_polygon_extended_hours__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_102'] = x_get_polygon_extended_hours__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_103'] = x_get_polygon_extended_hours__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_104'] = x_get_polygon_extended_hours__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_105'] = x_get_polygon_extended_hours__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_106'] = x_get_polygon_extended_hours__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_107'] = x_get_polygon_extended_hours__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_108'] = x_get_polygon_extended_hours__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_109'] = x_get_polygon_extended_hours__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_110'] = x_get_polygon_extended_hours__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_111'] = x_get_polygon_extended_hours__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_112'] = x_get_polygon_extended_hours__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_113'] = x_get_polygon_extended_hours__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_114'] = x_get_polygon_extended_hours__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_115'] = x_get_polygon_extended_hours__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_116'] = x_get_polygon_extended_hours__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_117'] = x_get_polygon_extended_hours__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_118'] = x_get_polygon_extended_hours__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_119'] = x_get_polygon_extended_hours__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_120'] = x_get_polygon_extended_hours__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_121'] = x_get_polygon_extended_hours__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_122'] = x_get_polygon_extended_hours__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_123'] = x_get_polygon_extended_hours__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_124'] = x_get_polygon_extended_hours__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_125'] = x_get_polygon_extended_hours__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_126'] = x_get_polygon_extended_hours__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_polygon_extended_hours__mutmut['x_get_polygon_extended_hours__mutmut_127'] = x_get_polygon_extended_hours__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_dividends__mutmut)
async def get_polygon_dividends(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_orig(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_1(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = None
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_2(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_3(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = None
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_4(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.lower()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_5(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = None
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_6(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(None, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_7(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, None)
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_8(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get((None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_9(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, )
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_10(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 1.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_11(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None or time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_12(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_13(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() + ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_14(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts <= _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_15(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = None
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_16(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_17(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"XXtickerXX": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_18(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"TICKER": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_19(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "XXlimitXX": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_20(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "LIMIT": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_21(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 6, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_22(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "XXorderXX": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_23(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "ORDER": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_24(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "XXdescXX", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_25(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "DESC", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_26(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "XXsortXX": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_27(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "SORT": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_28(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "XXex_dividend_dateXX", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_29(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "EX_DIVIDEND_DATE", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_30(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "XXapiKeyXX": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_31(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apikey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_32(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "APIKEY": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_33(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_34(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_35(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_36(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_37(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_38(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_39(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=11) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_40(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status == 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_41(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 201:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_42(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = None
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_43(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = None
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_44(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "XXex_dividend_dateXX": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_45(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "EX_DIVIDEND_DATE": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_46(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get(None),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_47(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("XXex_dividend_dateXX"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_48(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("EX_DIVIDEND_DATE"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_49(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "XXcash_amountXX": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_50(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "CASH_AMOUNT": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_51(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get(None),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_52(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("XXcash_amountXX"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_53(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("CASH_AMOUNT"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_54(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "XXpay_dateXX": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_55(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "PAY_DATE": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_56(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get(None),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_57(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("XXpay_dateXX"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_58(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("PAY_DATE"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_59(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "XXfrequencyXX": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_60(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "FREQUENCY": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_61(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get(None),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_62(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("XXfrequencyXX"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_63(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("FREQUENCY"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_64(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") and [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_65(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get(None) or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_66(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("XXresultsXX") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_67(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("RESULTS") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_68(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get(None)
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_69(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("XXex_dividend_dateXX")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_70(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("EX_DIVIDEND_DATE")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_71(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=None)
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_72(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: None)
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_73(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["XXex_dividend_dateXX"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_74(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["EX_DIVIDEND_DATE"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_75(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = None
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, e)
        return []


async def x_get_polygon_dividends__mutmut_76(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug(None, t, e)
        return []


async def x_get_polygon_dividends__mutmut_77(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", None, e)
        return []


async def x_get_polygon_dividends__mutmut_78(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, None)
        return []


async def x_get_polygon_dividends__mutmut_79(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug(t, e)
        return []


async def x_get_polygon_dividends__mutmut_80(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", e)
        return []


async def x_get_polygon_dividends__mutmut_81(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[polygon] dividends %s: %s", t, )
        return []


async def x_get_polygon_dividends__mutmut_82(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("XX[polygon] dividends %s: %sXX", t, e)
        return []


async def x_get_polygon_dividends__mutmut_83(ticker: str) -> list[dict]:
    """Fetch upcoming ex-dividend dates from Polygon reference API.

    Returns list of {ex_dividend_date, cash_amount, pay_date, frequency},
    sorted ascending by ex_dividend_date (nearest first).
    Cached 24 h per ticker — dividend schedules are stable intraday.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _div_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _DIV_TTL:
        return cached
    url = f"{_BASE}/v3/reference/dividends"
    params = {"ticker": t, "limit": 5, "order": "desc", "sort": "ex_dividend_date", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        results = [
            {
                "ex_dividend_date": r.get("ex_dividend_date"),
                "cash_amount": r.get("cash_amount"),
                "pay_date": r.get("pay_date"),
                "frequency": r.get("frequency"),
            }
            for r in (data.get("results") or [])
            if r.get("ex_dividend_date")
        ]
        results.sort(key=lambda x: x["ex_dividend_date"])
        _div_cache[t] = (results, time.monotonic())
        return results
    except Exception as e:
        log.debug("[POLYGON] DIVIDENDS %S: %S", t, e)
        return []

mutants_x_get_polygon_dividends__mutmut['_mutmut_orig'] = x_get_polygon_dividends__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_1'] = x_get_polygon_dividends__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_2'] = x_get_polygon_dividends__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_3'] = x_get_polygon_dividends__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_4'] = x_get_polygon_dividends__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_5'] = x_get_polygon_dividends__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_6'] = x_get_polygon_dividends__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_7'] = x_get_polygon_dividends__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_8'] = x_get_polygon_dividends__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_9'] = x_get_polygon_dividends__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_10'] = x_get_polygon_dividends__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_11'] = x_get_polygon_dividends__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_12'] = x_get_polygon_dividends__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_13'] = x_get_polygon_dividends__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_14'] = x_get_polygon_dividends__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_15'] = x_get_polygon_dividends__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_16'] = x_get_polygon_dividends__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_17'] = x_get_polygon_dividends__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_18'] = x_get_polygon_dividends__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_19'] = x_get_polygon_dividends__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_20'] = x_get_polygon_dividends__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_21'] = x_get_polygon_dividends__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_22'] = x_get_polygon_dividends__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_23'] = x_get_polygon_dividends__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_24'] = x_get_polygon_dividends__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_25'] = x_get_polygon_dividends__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_26'] = x_get_polygon_dividends__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_27'] = x_get_polygon_dividends__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_28'] = x_get_polygon_dividends__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_29'] = x_get_polygon_dividends__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_30'] = x_get_polygon_dividends__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_31'] = x_get_polygon_dividends__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_32'] = x_get_polygon_dividends__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_33'] = x_get_polygon_dividends__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_34'] = x_get_polygon_dividends__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_35'] = x_get_polygon_dividends__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_36'] = x_get_polygon_dividends__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_37'] = x_get_polygon_dividends__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_38'] = x_get_polygon_dividends__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_39'] = x_get_polygon_dividends__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_40'] = x_get_polygon_dividends__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_41'] = x_get_polygon_dividends__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_42'] = x_get_polygon_dividends__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_43'] = x_get_polygon_dividends__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_44'] = x_get_polygon_dividends__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_45'] = x_get_polygon_dividends__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_46'] = x_get_polygon_dividends__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_47'] = x_get_polygon_dividends__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_48'] = x_get_polygon_dividends__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_49'] = x_get_polygon_dividends__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_50'] = x_get_polygon_dividends__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_51'] = x_get_polygon_dividends__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_52'] = x_get_polygon_dividends__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_53'] = x_get_polygon_dividends__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_54'] = x_get_polygon_dividends__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_55'] = x_get_polygon_dividends__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_56'] = x_get_polygon_dividends__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_57'] = x_get_polygon_dividends__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_58'] = x_get_polygon_dividends__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_59'] = x_get_polygon_dividends__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_60'] = x_get_polygon_dividends__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_61'] = x_get_polygon_dividends__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_62'] = x_get_polygon_dividends__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_63'] = x_get_polygon_dividends__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_64'] = x_get_polygon_dividends__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_65'] = x_get_polygon_dividends__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_66'] = x_get_polygon_dividends__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_67'] = x_get_polygon_dividends__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_68'] = x_get_polygon_dividends__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_69'] = x_get_polygon_dividends__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_70'] = x_get_polygon_dividends__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_71'] = x_get_polygon_dividends__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_72'] = x_get_polygon_dividends__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_73'] = x_get_polygon_dividends__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_74'] = x_get_polygon_dividends__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_75'] = x_get_polygon_dividends__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_76'] = x_get_polygon_dividends__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_77'] = x_get_polygon_dividends__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_78'] = x_get_polygon_dividends__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_79'] = x_get_polygon_dividends__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_80'] = x_get_polygon_dividends__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_81'] = x_get_polygon_dividends__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_82'] = x_get_polygon_dividends__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_dividends__mutmut['x_get_polygon_dividends__mutmut_83'] = x_get_polygon_dividends__mutmut_83 # type: ignore # mutmut generated


_short_vol_cache: dict[str, tuple[list, float]] = {}
_SHORT_VOL_TTL = 3600  # 1 h — FINRA short volume updates once per day
mutants_x_get_polygon_short_volume__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_short_volume__mutmut)
async def get_polygon_short_volume(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_orig(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_1(ticker: str, limit: int = 31) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_2(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = None
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_3(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_4(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = None
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_5(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.lower()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_6(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = None
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_7(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(None, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_8(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, None)
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_9(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get((None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_10(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, )
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_11(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 1.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_12(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None or time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_13(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_14(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() + ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_15(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts <= _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_16(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = None
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_17(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_18(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"XXtickerXX": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_19(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"TICKER": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_20(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "XXlimitXX": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_21(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "LIMIT": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_22(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "XXapiKeyXX": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_23(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apikey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_24(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "APIKEY": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_25(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_26(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_27(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_28(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_29(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_30(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_31(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=16) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_32(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status == 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_33(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 201:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_34(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = None
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_35(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = None
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_36(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") and []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_37(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get(None) or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_38(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("XXresultsXX") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_39(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("RESULTS") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_40(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = None
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_41(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get(None)
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_42(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("XXdateXX")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_43(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("DATE")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_44(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = None
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_45(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get(None)
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_46(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("XXshort_volume_ratioXX")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_47(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("SHORT_VOLUME_RATIO")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_48(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None and svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_49(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is not None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_50(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is not None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_51(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                break
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_52(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                None
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_53(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "XXdateXX": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_54(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "DATE": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_55(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "XXshort_volume_ratioXX": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_56(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "SHORT_VOLUME_RATIO": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_57(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(None),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_58(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "XXshort_volumeXX": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_59(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "SHORT_VOLUME": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_60(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(None),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_61(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") and 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_62(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get(None) or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_63(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("XXshort_volumeXX") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_64(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("SHORT_VOLUME") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_65(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 1),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_66(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "XXtotal_volumeXX": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_67(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "TOTAL_VOLUME": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_68(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(None),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_69(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") and 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_70(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get(None) or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_71(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("XXtotal_volumeXX") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_72(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("TOTAL_VOLUME") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_73(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 1),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_74(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=None)
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_75(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: None)
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_76(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["XXdateXX"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_77(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["DATE"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_78(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = None
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_79(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug(None, t, e)
        return []


async def x_get_polygon_short_volume__mutmut_80(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", None, e)
        return []


async def x_get_polygon_short_volume__mutmut_81(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, None)
        return []


async def x_get_polygon_short_volume__mutmut_82(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug(t, e)
        return []


async def x_get_polygon_short_volume__mutmut_83(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", e)
        return []


async def x_get_polygon_short_volume__mutmut_84(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_volume %s: %s", t, )
        return []


async def x_get_polygon_short_volume__mutmut_85(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("XX[polygon] short_volume %s: %sXX", t, e)
        return []


async def x_get_polygon_short_volume__mutmut_86(ticker: str, limit: int = 30) -> list[dict]:
    """Fetch daily FINRA short-volume from Polygon (/stocks/v1/short-volume).

    Alt-data, orthogonal to OHLCV: ``short_volume_ratio`` (% of consolidated volume
    that was short-marked) measures intraday short pressure. On an oversold MR name a
    high ratio is ambiguous (squeeze fuel vs. falling-knife conviction), so it is worth
    testing/gating. Returns list of {date, short_volume_ratio, short_volume,
    total_volume} sorted ascending by date. ``limit`` rows (most recent first from the
    API, re-sorted ascending). History available ~2024-02 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_vol_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_VOL_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-volume"
    params = {"ticker": t, "limit": limit, "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("date")
            svr = r.get("short_volume_ratio")
            if d is None or svr is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_volume_ratio": float(svr),
                    "short_volume": int(r.get("short_volume") or 0),
                    "total_volume": int(r.get("total_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_vol_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[POLYGON] SHORT_VOLUME %S: %S", t, e)
        return []

mutants_x_get_polygon_short_volume__mutmut['_mutmut_orig'] = x_get_polygon_short_volume__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_1'] = x_get_polygon_short_volume__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_2'] = x_get_polygon_short_volume__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_3'] = x_get_polygon_short_volume__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_4'] = x_get_polygon_short_volume__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_5'] = x_get_polygon_short_volume__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_6'] = x_get_polygon_short_volume__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_7'] = x_get_polygon_short_volume__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_8'] = x_get_polygon_short_volume__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_9'] = x_get_polygon_short_volume__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_10'] = x_get_polygon_short_volume__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_11'] = x_get_polygon_short_volume__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_12'] = x_get_polygon_short_volume__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_13'] = x_get_polygon_short_volume__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_14'] = x_get_polygon_short_volume__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_15'] = x_get_polygon_short_volume__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_16'] = x_get_polygon_short_volume__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_17'] = x_get_polygon_short_volume__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_18'] = x_get_polygon_short_volume__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_19'] = x_get_polygon_short_volume__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_20'] = x_get_polygon_short_volume__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_21'] = x_get_polygon_short_volume__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_22'] = x_get_polygon_short_volume__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_23'] = x_get_polygon_short_volume__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_24'] = x_get_polygon_short_volume__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_25'] = x_get_polygon_short_volume__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_26'] = x_get_polygon_short_volume__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_27'] = x_get_polygon_short_volume__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_28'] = x_get_polygon_short_volume__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_29'] = x_get_polygon_short_volume__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_30'] = x_get_polygon_short_volume__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_31'] = x_get_polygon_short_volume__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_32'] = x_get_polygon_short_volume__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_33'] = x_get_polygon_short_volume__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_34'] = x_get_polygon_short_volume__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_35'] = x_get_polygon_short_volume__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_36'] = x_get_polygon_short_volume__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_37'] = x_get_polygon_short_volume__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_38'] = x_get_polygon_short_volume__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_39'] = x_get_polygon_short_volume__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_40'] = x_get_polygon_short_volume__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_41'] = x_get_polygon_short_volume__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_42'] = x_get_polygon_short_volume__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_43'] = x_get_polygon_short_volume__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_44'] = x_get_polygon_short_volume__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_45'] = x_get_polygon_short_volume__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_46'] = x_get_polygon_short_volume__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_47'] = x_get_polygon_short_volume__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_48'] = x_get_polygon_short_volume__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_49'] = x_get_polygon_short_volume__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_50'] = x_get_polygon_short_volume__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_51'] = x_get_polygon_short_volume__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_52'] = x_get_polygon_short_volume__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_53'] = x_get_polygon_short_volume__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_54'] = x_get_polygon_short_volume__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_55'] = x_get_polygon_short_volume__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_56'] = x_get_polygon_short_volume__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_57'] = x_get_polygon_short_volume__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_58'] = x_get_polygon_short_volume__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_59'] = x_get_polygon_short_volume__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_60'] = x_get_polygon_short_volume__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_61'] = x_get_polygon_short_volume__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_62'] = x_get_polygon_short_volume__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_63'] = x_get_polygon_short_volume__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_64'] = x_get_polygon_short_volume__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_65'] = x_get_polygon_short_volume__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_66'] = x_get_polygon_short_volume__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_67'] = x_get_polygon_short_volume__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_68'] = x_get_polygon_short_volume__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_69'] = x_get_polygon_short_volume__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_70'] = x_get_polygon_short_volume__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_71'] = x_get_polygon_short_volume__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_72'] = x_get_polygon_short_volume__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_73'] = x_get_polygon_short_volume__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_74'] = x_get_polygon_short_volume__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_75'] = x_get_polygon_short_volume__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_76'] = x_get_polygon_short_volume__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_77'] = x_get_polygon_short_volume__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_78'] = x_get_polygon_short_volume__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_79'] = x_get_polygon_short_volume__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_80'] = x_get_polygon_short_volume__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_81'] = x_get_polygon_short_volume__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_82'] = x_get_polygon_short_volume__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_83'] = x_get_polygon_short_volume__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_84'] = x_get_polygon_short_volume__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_85'] = x_get_polygon_short_volume__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_polygon_short_volume__mutmut['x_get_polygon_short_volume__mutmut_86'] = x_get_polygon_short_volume__mutmut_86 # type: ignore # mutmut generated


_short_int_cache: dict[str, tuple[list, float]] = {}
_SHORT_INT_TTL = 21_600  # 6 h — FINRA short interest settles bi-weekly
mutants_x_get_polygon_short_interest__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_short_interest__mutmut)
async def get_polygon_short_interest(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_orig(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_1(ticker: str, limit: int = 1001) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_2(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = None
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_3(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_4(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = None
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_5(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.lower()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_6(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = None
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_7(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(None, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_8(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, None)
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_9(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get((None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_10(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, )
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_11(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 1.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_12(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None or time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_13(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_14(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() + ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_15(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts <= _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_16(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = None
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_17(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_18(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"XXtickerXX": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_19(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"TICKER": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_20(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "XXlimitXX": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_21(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "LIMIT": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_22(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "XXsortXX": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_23(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "SORT": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_24(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "XXsettlement_date.ascXX", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_25(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "SETTLEMENT_DATE.ASC", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_26(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "XXapiKeyXX": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_27(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apikey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_28(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "APIKEY": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_29(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_30(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_31(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_32(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_33(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_34(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_35(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=16) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_36(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status == 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_37(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 201:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_38(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = None
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_39(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = None
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_40(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") and []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_41(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get(None) or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_42(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("XXresultsXX") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_43(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("RESULTS") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_44(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = None
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_45(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get(None)
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_46(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("XXsettlement_dateXX")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_47(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("SETTLEMENT_DATE")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_48(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = None
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_49(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get(None)
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_50(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("XXshort_interestXX")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_51(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("SHORT_INTEREST")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_52(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None and si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_53(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is not None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_54(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is not None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_55(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                break
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_56(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                None
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_57(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "XXdateXX": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_58(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "DATE": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_59(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "XXshort_interestXX": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_60(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "SHORT_INTEREST": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_61(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(None),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_62(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "XXdays_to_coverXX": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_63(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "DAYS_TO_COVER": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_64(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(None) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_65(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["XXdays_to_coverXX"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_66(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["DAYS_TO_COVER"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_67(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get(None) is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_68(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("XXdays_to_coverXX") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_69(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("DAYS_TO_COVER") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_70(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_71(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "XXavg_daily_volumeXX": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_72(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "AVG_DAILY_VOLUME": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_73(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(None),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_74(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") and 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_75(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get(None) or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_76(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("XXavg_daily_volumeXX") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_77(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("AVG_DAILY_VOLUME") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_78(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 1),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_79(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=None)
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_80(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: None)
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_81(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["XXdateXX"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_82(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["DATE"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_83(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = None
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_84(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug(None, t, e)
        return []


async def x_get_polygon_short_interest__mutmut_85(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", None, e)
        return []


async def x_get_polygon_short_interest__mutmut_86(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, None)
        return []


async def x_get_polygon_short_interest__mutmut_87(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug(t, e)
        return []


async def x_get_polygon_short_interest__mutmut_88(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", e)
        return []


async def x_get_polygon_short_interest__mutmut_89(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[polygon] short_interest %s: %s", t, )
        return []


async def x_get_polygon_short_interest__mutmut_90(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("XX[polygon] short_interest %s: %sXX", t, e)
        return []


async def x_get_polygon_short_interest__mutmut_91(ticker: str, limit: int = 1000) -> list[dict]:
    """Fetch bi-weekly FINRA short-interest from Polygon (/stocks/v1/short-interest).

    Alt-data, orthogonal to OHLCV and to daily short-volume: ``short_interest`` is the
    aggregate settled short position (shares) reported by FINRA twice a month, and
    ``days_to_cover`` = short_interest / avg_daily_volume is the squeeze-fuel metric.
    On an oversold MR name a rising days-to-cover is ambiguous (squeeze potential vs.
    falling-knife conviction), so it is worth testing/gating. Returns list of
    {date, short_interest, days_to_cover, avg_daily_volume} sorted ascending by date
    (``date`` is the FINRA settlement_date). History available ~2017-12 onward.
    """
    api_key = _get_api_key()
    if not api_key:
        return []
    t = ticker.upper()
    cached, ts = _short_int_cache.get(t, (None, 0.0))
    if cached is not None and time.monotonic() - ts < _SHORT_INT_TTL:
        return cached
    url = f"{_BASE}/stocks/v1/short-interest"
    params = {"ticker": t, "limit": limit, "sort": "settlement_date.asc", "apiKey": api_key}
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=15) as resp:
                if resp.status != 200:
                    return []
                data = await resp.json()
        rows = []
        for r in data.get("results") or []:
            d = r.get("settlement_date")
            si = r.get("short_interest")
            if d is None or si is None:
                continue
            rows.append(
                {
                    "date": d,
                    "short_interest": int(si),
                    "days_to_cover": float(r["days_to_cover"]) if r.get("days_to_cover") is not None else None,
                    "avg_daily_volume": int(r.get("avg_daily_volume") or 0),
                }
            )
        rows.sort(key=lambda x: x["date"])
        _short_int_cache[t] = (rows, time.monotonic())
        return rows
    except Exception as e:
        log.debug("[POLYGON] SHORT_INTEREST %S: %S", t, e)
        return []

mutants_x_get_polygon_short_interest__mutmut['_mutmut_orig'] = x_get_polygon_short_interest__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_1'] = x_get_polygon_short_interest__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_2'] = x_get_polygon_short_interest__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_3'] = x_get_polygon_short_interest__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_4'] = x_get_polygon_short_interest__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_5'] = x_get_polygon_short_interest__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_6'] = x_get_polygon_short_interest__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_7'] = x_get_polygon_short_interest__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_8'] = x_get_polygon_short_interest__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_9'] = x_get_polygon_short_interest__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_10'] = x_get_polygon_short_interest__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_11'] = x_get_polygon_short_interest__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_12'] = x_get_polygon_short_interest__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_13'] = x_get_polygon_short_interest__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_14'] = x_get_polygon_short_interest__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_15'] = x_get_polygon_short_interest__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_16'] = x_get_polygon_short_interest__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_17'] = x_get_polygon_short_interest__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_18'] = x_get_polygon_short_interest__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_19'] = x_get_polygon_short_interest__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_20'] = x_get_polygon_short_interest__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_21'] = x_get_polygon_short_interest__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_22'] = x_get_polygon_short_interest__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_23'] = x_get_polygon_short_interest__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_24'] = x_get_polygon_short_interest__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_25'] = x_get_polygon_short_interest__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_26'] = x_get_polygon_short_interest__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_27'] = x_get_polygon_short_interest__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_28'] = x_get_polygon_short_interest__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_29'] = x_get_polygon_short_interest__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_30'] = x_get_polygon_short_interest__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_31'] = x_get_polygon_short_interest__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_32'] = x_get_polygon_short_interest__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_33'] = x_get_polygon_short_interest__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_34'] = x_get_polygon_short_interest__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_35'] = x_get_polygon_short_interest__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_36'] = x_get_polygon_short_interest__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_37'] = x_get_polygon_short_interest__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_38'] = x_get_polygon_short_interest__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_39'] = x_get_polygon_short_interest__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_40'] = x_get_polygon_short_interest__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_41'] = x_get_polygon_short_interest__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_42'] = x_get_polygon_short_interest__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_43'] = x_get_polygon_short_interest__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_44'] = x_get_polygon_short_interest__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_45'] = x_get_polygon_short_interest__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_46'] = x_get_polygon_short_interest__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_47'] = x_get_polygon_short_interest__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_48'] = x_get_polygon_short_interest__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_49'] = x_get_polygon_short_interest__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_50'] = x_get_polygon_short_interest__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_51'] = x_get_polygon_short_interest__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_52'] = x_get_polygon_short_interest__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_53'] = x_get_polygon_short_interest__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_54'] = x_get_polygon_short_interest__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_55'] = x_get_polygon_short_interest__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_56'] = x_get_polygon_short_interest__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_57'] = x_get_polygon_short_interest__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_58'] = x_get_polygon_short_interest__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_59'] = x_get_polygon_short_interest__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_60'] = x_get_polygon_short_interest__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_61'] = x_get_polygon_short_interest__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_62'] = x_get_polygon_short_interest__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_63'] = x_get_polygon_short_interest__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_64'] = x_get_polygon_short_interest__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_65'] = x_get_polygon_short_interest__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_66'] = x_get_polygon_short_interest__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_67'] = x_get_polygon_short_interest__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_68'] = x_get_polygon_short_interest__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_69'] = x_get_polygon_short_interest__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_70'] = x_get_polygon_short_interest__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_71'] = x_get_polygon_short_interest__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_72'] = x_get_polygon_short_interest__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_73'] = x_get_polygon_short_interest__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_74'] = x_get_polygon_short_interest__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_75'] = x_get_polygon_short_interest__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_76'] = x_get_polygon_short_interest__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_77'] = x_get_polygon_short_interest__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_78'] = x_get_polygon_short_interest__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_79'] = x_get_polygon_short_interest__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_80'] = x_get_polygon_short_interest__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_81'] = x_get_polygon_short_interest__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_82'] = x_get_polygon_short_interest__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_83'] = x_get_polygon_short_interest__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_84'] = x_get_polygon_short_interest__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_85'] = x_get_polygon_short_interest__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_86'] = x_get_polygon_short_interest__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_87'] = x_get_polygon_short_interest__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_88'] = x_get_polygon_short_interest__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_89'] = x_get_polygon_short_interest__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_90'] = x_get_polygon_short_interest__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_polygon_short_interest__mutmut['x_get_polygon_short_interest__mutmut_91'] = x_get_polygon_short_interest__mutmut_91 # type: ignore # mutmut generated


# ── §81 Block Print Detection ──────────────────────────────────────────────────
# Easley & O'Hara (1987): large trades carry more information than small trades.
# Block prints (>=min_block_size shares) at the ask near day-low = institutional
# accumulation; at the bid near day-high = institutional distribution.
_block_print_cache: dict[str, tuple[dict, float]] = {}
_BLOCK_PRINT_TTL = 600  # 10 min — refreshed each scan cycle

# ── OFI (Order Flow Imbalance) ────────────────────────────────────────────────
# Lee-Ready (1991) approximation on 1-min Polygon aggregate bars.
# ofi_t = V_t × sign(C_t − C_{t−1}): buyer-initiated if price ticked up, else seller.
# Normalized by session ADV so the metric is comparable across tickers.
_ofi_cache: dict[str, tuple[dict, float]] = {}
_OFI_TTL = 600  # 10 min
mutants_x_get_recent_block_prints__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_recent_block_prints__mutmut)
async def get_recent_block_prints(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_orig(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_1(
    ticker: str,
    min_block_size: int = 5001,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_2(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = None
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_3(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.lower()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_4(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = None
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_5(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(None)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_6(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached or time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_7(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() + cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_8(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[2] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_9(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] <= _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_10(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[1]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_11(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = None
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_12(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_13(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = None
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_14(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(None)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_15(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is not None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_16(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = None
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_17(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = None
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_18(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") and {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_19(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get(None) or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_20(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("XXdayXX") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_21(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("DAY") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_22(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = None
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_23(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(None)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_24(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") and 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_25(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get(None) or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_26(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("XXlXX") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_27(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("L") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_28(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 1)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_29(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = None
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_30(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(None)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_31(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") and 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_32(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get(None) or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_33(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("XXhXX") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_34(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("H") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_35(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 1)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_36(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = None
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_37(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") and {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_38(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get(None) or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_39(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("XXlastTradeXX") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_40(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lasttrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_41(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("LASTTRADE") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_42(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = None
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_43(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(None)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_44(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") and 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_45(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get(None) or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_46(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("XXpXX") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_47(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("P") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_48(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 1)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_49(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price < 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_50(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 1:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_51(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = None
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_52(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_53(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "XXlimitXX": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_54(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "LIMIT": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_55(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 501,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_56(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "XXsortXX": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_57(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "SORT": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_58(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "XXtimestampXX",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_59(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "TIMESTAMP",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_60(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "XXorderXX": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_61(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "ORDER": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_62(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "XXdescXX",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_63(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "DESC",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_64(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "XXapiKeyXX": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_65(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apikey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_66(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "APIKEY": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_67(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_68(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_69(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_70(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_71(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_72(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_73(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=11) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_74(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status == 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_75(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 201:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_76(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = None

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_77(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = None
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_78(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 1
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_79(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = None
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_80(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 1
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_81(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = None
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_82(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 1.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_83(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = None

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_84(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 1.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_85(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") and []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_86(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get(None) or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_87(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("XXresultsXX") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_88(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("RESULTS") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_89(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = None
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_90(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(None)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_91(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") and 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_92(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get(None) or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_93(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("XXsizeXX") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_94(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("SIZE") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_95(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 1)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_96(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size <= min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_97(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                break
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_98(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = None
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_99(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(None)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_100(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") and 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_101(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get(None) or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_102(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("XXpriceXX") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_103(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("PRICE") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_104(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 1)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_105(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price < 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_106(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 1:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_107(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                break

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_108(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = None
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_109(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 or price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_110(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low >= 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_111(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 1 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_112(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price < day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_113(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low / 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_114(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 2.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_115(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = None

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_116(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 or price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_117(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high >= 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_118(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 1 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_119(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price > day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_120(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high / 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_121(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 1.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_122(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys = 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_123(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys -= 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_124(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 2
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_125(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol = size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_126(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol -= size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_127(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells = 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_128(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells -= 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_129(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 2
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_130(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol = size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_131(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol -= size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_132(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = None
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_133(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "XXblock_buysXX": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_134(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "BLOCK_BUYS": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_135(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "XXblock_sellsXX": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_136(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "BLOCK_SELLS": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_137(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "XXblock_buy_volumeXX": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_138(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "BLOCK_BUY_VOLUME": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_139(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "XXblock_sell_volumeXX": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_140(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "BLOCK_SELL_VOLUME": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_141(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = None
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_142(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug(None, t, e)
        return {}


async def x_get_recent_block_prints__mutmut_143(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", None, e)
        return {}


async def x_get_recent_block_prints__mutmut_144(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, None)
        return {}


async def x_get_recent_block_prints__mutmut_145(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug(t, e)
        return {}


async def x_get_recent_block_prints__mutmut_146(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", e)
        return {}


async def x_get_recent_block_prints__mutmut_147(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[polygon] block_prints %s: %s", t, )
        return {}


async def x_get_recent_block_prints__mutmut_148(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("XX[polygon] block_prints %s: %sXX", t, e)
        return {}


async def x_get_recent_block_prints__mutmut_149(
    ticker: str,
    min_block_size: int = 5_000,
) -> dict:
    """Return recent block-trade summary for `ticker`.

    Fetches the last 500 trades via Polygon /v3/trades and classifies each
    print as accumulation (at ask, near day-low) or distribution (at bid,
    near day-high).

    Returns:
        {
            "block_buys": int,
            "block_sells": int,
            "block_buy_volume": float,
            "block_sell_volume": float,
        }
    An empty dict is returned on error or when Polygon is unavailable.
    """
    t = ticker.upper()
    cached = _block_print_cache.get(t)
    if cached and time.monotonic() - cached[1] < _BLOCK_PRINT_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    # Get current price context from snapshot cache
    snap_entry = _snapshot_cache.get(t)
    if snap_entry is None:
        return {}
    snap, _ = snap_entry
    day = snap.get("day") or {}
    day_low = float(day.get("l") or 0)
    day_high = float(day.get("h") or 0)
    last_trade = snap.get("lastTrade") or {}
    mid_price = float(last_trade.get("p") or 0)
    if mid_price <= 0:
        return {}

    url = f"{_BASE}/v3/trades/{t}"
    params = {
        "limit": 500,
        "sort": "timestamp",
        "order": "desc",
        "apiKey": api_key,
    }
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=10) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()

        block_buys = 0
        block_sells = 0
        block_buy_vol = 0.0
        block_sell_vol = 0.0

        for trade in data.get("results") or []:
            size = int(trade.get("size") or 0)
            if size < min_block_size:
                continue
            price = float(trade.get("price") or 0)
            if price <= 0:
                continue

            # Classify by price relative to intraday range:
            # Near day-low (within 1%): buyer-initiated = accumulation
            # Near day-high (within 1%): seller-initiated = distribution
            at_low = day_low > 0 and price <= day_low * 1.01
            at_high = day_high > 0 and price >= day_high * 0.99

            if at_low:
                block_buys += 1
                block_buy_vol += size
            elif at_high:
                block_sells += 1
                block_sell_vol += size

        result = {
            "block_buys": block_buys,
            "block_sells": block_sells,
            "block_buy_volume": block_buy_vol,
            "block_sell_volume": block_sell_vol,
        }
        _block_print_cache[t] = (result, time.monotonic())
        return result
    except Exception as e:
        log.debug("[POLYGON] BLOCK_PRINTS %S: %S", t, e)
        return {}

mutants_x_get_recent_block_prints__mutmut['_mutmut_orig'] = x_get_recent_block_prints__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_1'] = x_get_recent_block_prints__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_2'] = x_get_recent_block_prints__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_3'] = x_get_recent_block_prints__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_4'] = x_get_recent_block_prints__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_5'] = x_get_recent_block_prints__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_6'] = x_get_recent_block_prints__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_7'] = x_get_recent_block_prints__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_8'] = x_get_recent_block_prints__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_9'] = x_get_recent_block_prints__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_10'] = x_get_recent_block_prints__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_11'] = x_get_recent_block_prints__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_12'] = x_get_recent_block_prints__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_13'] = x_get_recent_block_prints__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_14'] = x_get_recent_block_prints__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_15'] = x_get_recent_block_prints__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_16'] = x_get_recent_block_prints__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_17'] = x_get_recent_block_prints__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_18'] = x_get_recent_block_prints__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_19'] = x_get_recent_block_prints__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_20'] = x_get_recent_block_prints__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_21'] = x_get_recent_block_prints__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_22'] = x_get_recent_block_prints__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_23'] = x_get_recent_block_prints__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_24'] = x_get_recent_block_prints__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_25'] = x_get_recent_block_prints__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_26'] = x_get_recent_block_prints__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_27'] = x_get_recent_block_prints__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_28'] = x_get_recent_block_prints__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_29'] = x_get_recent_block_prints__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_30'] = x_get_recent_block_prints__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_31'] = x_get_recent_block_prints__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_32'] = x_get_recent_block_prints__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_33'] = x_get_recent_block_prints__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_34'] = x_get_recent_block_prints__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_35'] = x_get_recent_block_prints__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_36'] = x_get_recent_block_prints__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_37'] = x_get_recent_block_prints__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_38'] = x_get_recent_block_prints__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_39'] = x_get_recent_block_prints__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_40'] = x_get_recent_block_prints__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_41'] = x_get_recent_block_prints__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_42'] = x_get_recent_block_prints__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_43'] = x_get_recent_block_prints__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_44'] = x_get_recent_block_prints__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_45'] = x_get_recent_block_prints__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_46'] = x_get_recent_block_prints__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_47'] = x_get_recent_block_prints__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_48'] = x_get_recent_block_prints__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_49'] = x_get_recent_block_prints__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_50'] = x_get_recent_block_prints__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_51'] = x_get_recent_block_prints__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_52'] = x_get_recent_block_prints__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_53'] = x_get_recent_block_prints__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_54'] = x_get_recent_block_prints__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_55'] = x_get_recent_block_prints__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_56'] = x_get_recent_block_prints__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_57'] = x_get_recent_block_prints__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_58'] = x_get_recent_block_prints__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_59'] = x_get_recent_block_prints__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_60'] = x_get_recent_block_prints__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_61'] = x_get_recent_block_prints__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_62'] = x_get_recent_block_prints__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_63'] = x_get_recent_block_prints__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_64'] = x_get_recent_block_prints__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_65'] = x_get_recent_block_prints__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_66'] = x_get_recent_block_prints__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_67'] = x_get_recent_block_prints__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_68'] = x_get_recent_block_prints__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_69'] = x_get_recent_block_prints__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_70'] = x_get_recent_block_prints__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_71'] = x_get_recent_block_prints__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_72'] = x_get_recent_block_prints__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_73'] = x_get_recent_block_prints__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_74'] = x_get_recent_block_prints__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_75'] = x_get_recent_block_prints__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_76'] = x_get_recent_block_prints__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_77'] = x_get_recent_block_prints__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_78'] = x_get_recent_block_prints__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_79'] = x_get_recent_block_prints__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_80'] = x_get_recent_block_prints__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_81'] = x_get_recent_block_prints__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_82'] = x_get_recent_block_prints__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_83'] = x_get_recent_block_prints__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_84'] = x_get_recent_block_prints__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_85'] = x_get_recent_block_prints__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_86'] = x_get_recent_block_prints__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_87'] = x_get_recent_block_prints__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_88'] = x_get_recent_block_prints__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_89'] = x_get_recent_block_prints__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_90'] = x_get_recent_block_prints__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_91'] = x_get_recent_block_prints__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_92'] = x_get_recent_block_prints__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_93'] = x_get_recent_block_prints__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_94'] = x_get_recent_block_prints__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_95'] = x_get_recent_block_prints__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_96'] = x_get_recent_block_prints__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_97'] = x_get_recent_block_prints__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_98'] = x_get_recent_block_prints__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_99'] = x_get_recent_block_prints__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_100'] = x_get_recent_block_prints__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_101'] = x_get_recent_block_prints__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_102'] = x_get_recent_block_prints__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_103'] = x_get_recent_block_prints__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_104'] = x_get_recent_block_prints__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_105'] = x_get_recent_block_prints__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_106'] = x_get_recent_block_prints__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_107'] = x_get_recent_block_prints__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_108'] = x_get_recent_block_prints__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_109'] = x_get_recent_block_prints__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_110'] = x_get_recent_block_prints__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_111'] = x_get_recent_block_prints__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_112'] = x_get_recent_block_prints__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_113'] = x_get_recent_block_prints__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_114'] = x_get_recent_block_prints__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_115'] = x_get_recent_block_prints__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_116'] = x_get_recent_block_prints__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_117'] = x_get_recent_block_prints__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_118'] = x_get_recent_block_prints__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_119'] = x_get_recent_block_prints__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_120'] = x_get_recent_block_prints__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_121'] = x_get_recent_block_prints__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_122'] = x_get_recent_block_prints__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_123'] = x_get_recent_block_prints__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_124'] = x_get_recent_block_prints__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_125'] = x_get_recent_block_prints__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_126'] = x_get_recent_block_prints__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_127'] = x_get_recent_block_prints__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_128'] = x_get_recent_block_prints__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_129'] = x_get_recent_block_prints__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_130'] = x_get_recent_block_prints__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_131'] = x_get_recent_block_prints__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_132'] = x_get_recent_block_prints__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_133'] = x_get_recent_block_prints__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_134'] = x_get_recent_block_prints__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_135'] = x_get_recent_block_prints__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_136'] = x_get_recent_block_prints__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_137'] = x_get_recent_block_prints__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_138'] = x_get_recent_block_prints__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_139'] = x_get_recent_block_prints__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_140'] = x_get_recent_block_prints__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_141'] = x_get_recent_block_prints__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_142'] = x_get_recent_block_prints__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_143'] = x_get_recent_block_prints__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_144'] = x_get_recent_block_prints__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_145'] = x_get_recent_block_prints__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_146'] = x_get_recent_block_prints__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_147'] = x_get_recent_block_prints__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_148'] = x_get_recent_block_prints__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_recent_block_prints__mutmut['x_get_recent_block_prints__mutmut_149'] = x_get_recent_block_prints__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_ofi_signals__mutmut)
async def get_ofi_signals(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_orig(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_1(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = None
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_2(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.lower()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_3(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = None
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_4(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(None)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_5(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached or time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_6(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() + cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_7(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[2] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_8(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] <= _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_9(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[1]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_10(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = None
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_11(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_12(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = None
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_13(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(None)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_14(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = None
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_15(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime(None)
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_16(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc + timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_17(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=None)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_18(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=3)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_19(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("XX%Y-%m-%dXX")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_20(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_21(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%M-%D")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_22(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = None
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_23(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime(None)
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_24(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("XX%Y-%m-%dXX")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_25(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_26(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%M-%D")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_27(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = None
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_28(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = None

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_29(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "XXadjustedXX": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_30(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "ADJUSTED": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_31(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "XXtrueXX",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_32(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "TRUE",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_33(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "XXsortXX": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_34(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "SORT": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_35(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "XXascXX",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_36(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "ASC",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_37(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "XXlimitXX": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_38(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "LIMIT": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_39(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1001,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_40(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "XXapiKeyXX": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_41(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apikey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_42(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "APIKEY": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_43(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(None, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_44(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=None, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_45(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=None) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_46(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_47(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_48(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, ) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_49(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=6) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_50(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status == 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_51(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 201:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_52(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = None

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_53(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = None
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_54(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") and []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_55(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get(None) or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_56(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("XXresultsXX") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_57(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("RESULTS") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_58(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) <= 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_59(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 11:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_60(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = None
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_61(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array(None)
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_62(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(None) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_63(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") and 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_64(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get(None) or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_65(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("XXcXX") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_66(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("C") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_67(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 1) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_68(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = None

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_69(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array(None)

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_70(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(None) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_71(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") and 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_72(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get(None) or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_73(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("XXvXX") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_74(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("V") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_75(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 1) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_76(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = None
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_77(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(None, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_78(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=None)
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_79(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_80(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, )
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_81(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[1])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_82(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = None
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_83(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(None, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_84(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, None, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_85(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, None)
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_86(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_87(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_88(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, )
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_89(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff >= 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_90(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 1, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_91(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 2.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_92(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(None, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_93(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, None, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_94(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, None))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_95(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(-1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_96(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_97(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, ))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_98(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff <= 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_99(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 1, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_100(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, +1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_101(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -2.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_102(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 1.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_103(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = None

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_104(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs / volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_105(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = None
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_106(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) and 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_107(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(None) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_108(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 2.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_109(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = None
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_110(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) * session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_111(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(None) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_112(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[+30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_113(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-31:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_114(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = None
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_115(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) * session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_116(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(None) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_117(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[+120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_118(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-121:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_119(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = None

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_120(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) * session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_121(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(None) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_122(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = None
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_123(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(None) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_124(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] + 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_125(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] * closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_126(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[+1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_127(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-2] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_128(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[1] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_129(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 2) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_130(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[1] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_131(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] >= 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_132(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 1 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_133(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 1.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_134(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = None

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_135(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 / ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_136(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today / -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_137(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * +1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_138(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -2.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_139(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = None
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_140(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "XXofi_30mXX": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_141(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "OFI_30M": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_142(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(None, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_143(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, None),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_144(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_145(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, ),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_146(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 5),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_147(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "XXofi_2hXX": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_148(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "OFI_2H": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_149(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(None, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_150(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, None),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_151(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_152(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, ),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_153(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 5),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_154(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "XXofi_1dXX": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_155(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "OFI_1D": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_156(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(None, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_157(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, None),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_158(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_159(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, ),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_160(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 5),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_161(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "XXofi_divergenceXX": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_162(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "OFI_DIVERGENCE": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_163(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(None, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_164(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, None),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_165(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_166(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, ),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_167(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 7),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_168(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = None
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, e)
        return {}


async def x_get_ofi_signals__mutmut_169(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug(None, t, e)
        return {}


async def x_get_ofi_signals__mutmut_170(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", None, e)
        return {}


async def x_get_ofi_signals__mutmut_171(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, None)
        return {}


async def x_get_ofi_signals__mutmut_172(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug(t, e)
        return {}


async def x_get_ofi_signals__mutmut_173(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", e)
        return {}


async def x_get_ofi_signals__mutmut_174(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[polygon] ofi %s: %s", t, )
        return {}


async def x_get_ofi_signals__mutmut_175(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("XX[polygon] ofi %s: %sXX", t, e)
        return {}


async def x_get_ofi_signals__mutmut_176(ticker: str) -> dict:
    """
    Order Flow Imbalance from Polygon 1-minute aggregate bars.

    Returns:
        ofi_30m        — normalized OFI over last 30 bars (half-hour)
        ofi_2h         — normalized OFI over last 120 bars (two hours)
        ofi_1d         — normalized OFI over full session
        ofi_divergence — price_ret * -1 * ofi_1d; positive when price fell
                         but buyers were net absorbing (accumulation signal)

    All values are normalized by session total volume; range roughly [-1, +1].
    Empty dict returned on error or when Polygon key is absent.
    """
    t = ticker.upper()
    cached = _ofi_cache.get(t)
    if cached and time.monotonic() - cached[1] < _OFI_TTL:
        return cached[0]

    api_key = _get_api_key()
    if not api_key:
        return {}

    try:
        import numpy as _np

        # Fetch today's 1-minute bars (full session = up to 390 bars)
        now_utc = datetime.now(timezone.utc)
        # Use yesterday as from-date so pre-market bars are included
        from_dt = (now_utc - timedelta(days=2)).strftime("%Y-%m-%d")
        to_dt = now_utc.strftime("%Y-%m-%d")
        url = f"{_BASE}/v2/aggs/ticker/{t}/range/1/minute/{from_dt}/{to_dt}"
        params = {
            "adjusted": "true",
            "sort": "asc",
            "limit": 1000,
            "apiKey": api_key,
        }

        async with shared_session() as session:
            async with session.get(url, params=params, timeout=5) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json(content_type=None)

        results = data.get("results") or []
        if len(results) < 10:
            return {}

        closes = _np.array([float(r.get("c") or 0) for r in results])
        volumes = _np.array([float(r.get("v") or 0) for r in results])

        # Lee-Ready sign: price ticked up → buyer-initiated; down → seller
        price_diff = _np.diff(closes, prepend=closes[0])
        signs = _np.where(price_diff > 0, 1.0, _np.where(price_diff < 0, -1.0, 0.0))
        ofi_series = signs * volumes

        session_vol = float(volumes.sum()) or 1.0
        ofi_30m = float(ofi_series[-30:].sum()) / session_vol
        ofi_2h = float(ofi_series[-120:].sum()) / session_vol
        ofi_1d = float(ofi_series.sum()) / session_vol

        # Divergence: positive when price fell but net flow was buying
        price_ret_today = float(closes[-1] / closes[0] - 1) if closes[0] > 0 else 0.0
        ofi_divergence = price_ret_today * -1.0 * ofi_1d

        result = {
            "ofi_30m": round(ofi_30m, 4),
            "ofi_2h": round(ofi_2h, 4),
            "ofi_1d": round(ofi_1d, 4),
            "ofi_divergence": round(ofi_divergence, 6),
        }
        _ofi_cache[t] = (result, time.monotonic())
        return result

    except Exception as e:
        log.debug("[POLYGON] OFI %S: %S", t, e)
        return {}

mutants_x_get_ofi_signals__mutmut['_mutmut_orig'] = x_get_ofi_signals__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_1'] = x_get_ofi_signals__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_2'] = x_get_ofi_signals__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_3'] = x_get_ofi_signals__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_4'] = x_get_ofi_signals__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_5'] = x_get_ofi_signals__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_6'] = x_get_ofi_signals__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_7'] = x_get_ofi_signals__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_8'] = x_get_ofi_signals__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_9'] = x_get_ofi_signals__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_10'] = x_get_ofi_signals__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_11'] = x_get_ofi_signals__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_12'] = x_get_ofi_signals__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_13'] = x_get_ofi_signals__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_14'] = x_get_ofi_signals__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_15'] = x_get_ofi_signals__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_16'] = x_get_ofi_signals__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_17'] = x_get_ofi_signals__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_18'] = x_get_ofi_signals__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_19'] = x_get_ofi_signals__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_20'] = x_get_ofi_signals__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_21'] = x_get_ofi_signals__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_22'] = x_get_ofi_signals__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_23'] = x_get_ofi_signals__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_24'] = x_get_ofi_signals__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_25'] = x_get_ofi_signals__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_26'] = x_get_ofi_signals__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_27'] = x_get_ofi_signals__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_28'] = x_get_ofi_signals__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_29'] = x_get_ofi_signals__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_30'] = x_get_ofi_signals__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_31'] = x_get_ofi_signals__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_32'] = x_get_ofi_signals__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_33'] = x_get_ofi_signals__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_34'] = x_get_ofi_signals__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_35'] = x_get_ofi_signals__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_36'] = x_get_ofi_signals__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_37'] = x_get_ofi_signals__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_38'] = x_get_ofi_signals__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_39'] = x_get_ofi_signals__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_40'] = x_get_ofi_signals__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_41'] = x_get_ofi_signals__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_42'] = x_get_ofi_signals__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_43'] = x_get_ofi_signals__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_44'] = x_get_ofi_signals__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_45'] = x_get_ofi_signals__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_46'] = x_get_ofi_signals__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_47'] = x_get_ofi_signals__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_48'] = x_get_ofi_signals__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_49'] = x_get_ofi_signals__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_50'] = x_get_ofi_signals__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_51'] = x_get_ofi_signals__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_52'] = x_get_ofi_signals__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_53'] = x_get_ofi_signals__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_54'] = x_get_ofi_signals__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_55'] = x_get_ofi_signals__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_56'] = x_get_ofi_signals__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_57'] = x_get_ofi_signals__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_58'] = x_get_ofi_signals__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_59'] = x_get_ofi_signals__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_60'] = x_get_ofi_signals__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_61'] = x_get_ofi_signals__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_62'] = x_get_ofi_signals__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_63'] = x_get_ofi_signals__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_64'] = x_get_ofi_signals__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_65'] = x_get_ofi_signals__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_66'] = x_get_ofi_signals__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_67'] = x_get_ofi_signals__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_68'] = x_get_ofi_signals__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_69'] = x_get_ofi_signals__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_70'] = x_get_ofi_signals__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_71'] = x_get_ofi_signals__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_72'] = x_get_ofi_signals__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_73'] = x_get_ofi_signals__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_74'] = x_get_ofi_signals__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_75'] = x_get_ofi_signals__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_76'] = x_get_ofi_signals__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_77'] = x_get_ofi_signals__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_78'] = x_get_ofi_signals__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_79'] = x_get_ofi_signals__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_80'] = x_get_ofi_signals__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_81'] = x_get_ofi_signals__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_82'] = x_get_ofi_signals__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_83'] = x_get_ofi_signals__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_84'] = x_get_ofi_signals__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_85'] = x_get_ofi_signals__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_86'] = x_get_ofi_signals__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_87'] = x_get_ofi_signals__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_88'] = x_get_ofi_signals__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_89'] = x_get_ofi_signals__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_90'] = x_get_ofi_signals__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_91'] = x_get_ofi_signals__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_92'] = x_get_ofi_signals__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_93'] = x_get_ofi_signals__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_94'] = x_get_ofi_signals__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_95'] = x_get_ofi_signals__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_96'] = x_get_ofi_signals__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_97'] = x_get_ofi_signals__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_98'] = x_get_ofi_signals__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_99'] = x_get_ofi_signals__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_100'] = x_get_ofi_signals__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_101'] = x_get_ofi_signals__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_102'] = x_get_ofi_signals__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_103'] = x_get_ofi_signals__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_104'] = x_get_ofi_signals__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_105'] = x_get_ofi_signals__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_106'] = x_get_ofi_signals__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_107'] = x_get_ofi_signals__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_108'] = x_get_ofi_signals__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_109'] = x_get_ofi_signals__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_110'] = x_get_ofi_signals__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_111'] = x_get_ofi_signals__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_112'] = x_get_ofi_signals__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_113'] = x_get_ofi_signals__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_114'] = x_get_ofi_signals__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_115'] = x_get_ofi_signals__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_116'] = x_get_ofi_signals__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_117'] = x_get_ofi_signals__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_118'] = x_get_ofi_signals__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_119'] = x_get_ofi_signals__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_120'] = x_get_ofi_signals__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_121'] = x_get_ofi_signals__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_122'] = x_get_ofi_signals__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_123'] = x_get_ofi_signals__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_124'] = x_get_ofi_signals__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_125'] = x_get_ofi_signals__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_126'] = x_get_ofi_signals__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_127'] = x_get_ofi_signals__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_128'] = x_get_ofi_signals__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_129'] = x_get_ofi_signals__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_130'] = x_get_ofi_signals__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_131'] = x_get_ofi_signals__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_132'] = x_get_ofi_signals__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_133'] = x_get_ofi_signals__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_134'] = x_get_ofi_signals__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_135'] = x_get_ofi_signals__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_136'] = x_get_ofi_signals__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_137'] = x_get_ofi_signals__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_138'] = x_get_ofi_signals__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_139'] = x_get_ofi_signals__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_140'] = x_get_ofi_signals__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_141'] = x_get_ofi_signals__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_142'] = x_get_ofi_signals__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_143'] = x_get_ofi_signals__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_144'] = x_get_ofi_signals__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_145'] = x_get_ofi_signals__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_146'] = x_get_ofi_signals__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_147'] = x_get_ofi_signals__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_148'] = x_get_ofi_signals__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_149'] = x_get_ofi_signals__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_150'] = x_get_ofi_signals__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_151'] = x_get_ofi_signals__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_152'] = x_get_ofi_signals__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_153'] = x_get_ofi_signals__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_154'] = x_get_ofi_signals__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_155'] = x_get_ofi_signals__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_156'] = x_get_ofi_signals__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_157'] = x_get_ofi_signals__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_158'] = x_get_ofi_signals__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_159'] = x_get_ofi_signals__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_160'] = x_get_ofi_signals__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_161'] = x_get_ofi_signals__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_162'] = x_get_ofi_signals__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_163'] = x_get_ofi_signals__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_164'] = x_get_ofi_signals__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_165'] = x_get_ofi_signals__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_166'] = x_get_ofi_signals__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_167'] = x_get_ofi_signals__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_168'] = x_get_ofi_signals__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_169'] = x_get_ofi_signals__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_170'] = x_get_ofi_signals__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_171'] = x_get_ofi_signals__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_172'] = x_get_ofi_signals__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_173'] = x_get_ofi_signals__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_174'] = x_get_ofi_signals__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_175'] = x_get_ofi_signals__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_ofi_signals__mutmut['x_get_ofi_signals__mutmut_176'] = x_get_ofi_signals__mutmut_176 # type: ignore # mutmut generated
